   version stable 1.0.35
  optional PAM        --with-pam   ''              for pluggable authentication
  optional xinetd     ''           ''              for running server
# optional libcap     ''           ''              to limit capabilities of a process
  optional mysql      --with-mysql --without-mysql for MySQL user authentication
  optional openldap   ''           ''              for light weight directory access protocol support
  optional postgresql --with-pgsql --without-pgsql for postgres user authentication
  optional openssl    --with-tls   --without-tls   enable FTP through SSL
  category network/ftp
 attribute server console
    source ftp://ftp.pureftpd.org/pub/pure-ftpd/releases/pure-ftpd-$VERSION.tar.bz2
      info good 20120701
      info last 20111202
      info home http://www.pureftpd.org/
  eprovide ftpd
   protect /etc/pureftpd.passwd
   protect /etc/pureftpd.pdb
      desc 'an efficient, lightweight, and secure FTP server
pure-ftpd is a fast, production quality,
standards-conformant FTP server based on Troll-FTPd.
It has no known buffer overflows, is trivial to set up,
and is especially designed for modern kernels.
Features include PAM support, IPv6, chroot()ed home directories,
virtual domains, built-in ls, FXP protocol, anti-warez system,
bandwidth throttling, bounded ports for passive downloads,
an LDAP backend, XML output, and more.

The openssl --with-tls option requires the
/etc/ssl/private/pure-ftpd.pem
certificate to be installed.'

build(){
./configure          \
--sysconfdir=/etc    \
--prefix=/usr        \
--with-largefile     \
--with-everything    \
--without-banner     \
--with-virtualchroot \
$OPTS                &&
make                 &&
make install DESTDIR=$DESTDIR &&
mkdir   -pvm 755 $DESTDIR/{etc,usr/share} &&
install  -vm 644 -o root -g root pam/ftpusers $DESTDIR/etc &&
cp -rv contrib $DESTDIR/usr/share/pure-ftpd
}

# Wonder where I am errnously receiving the 1.0 version from?
    stable 0.21.1.src 0.21.src 0.18.src
   require MesaLib SDL_image SDL_mixer SDL_net SDL_ttf wxWidgets
  category game
 attribute x11
       url $SOURCEFORGE_URL
    source scourge/scourge-$VERSION.tar.gz
    source scourge/scourge-${VERSION/src/data}.tar.gz
  homepage http://scourge.sourceforge.net
      opts --disable-sdltest --with-data-dir=/usr/share/scourge
  estimate 3500
      desc 'scourge is an OpenGL RPG.'

build(){
 install_data(){
  mkdir -p                 /usr/share/scourge
  cp    -rv scourge_data/* /usr/share/scourge
 }

 aclocal
 automake -afc
 autoconf
 libtoolize --force
 default_build &&
 install_data
}

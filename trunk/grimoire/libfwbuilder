    stable 3.0.3 2.1.18
   require qt-x11-opensource
  optional net-snmp '' '' for net-snmp integration
  optional openssl  '' '' for secure socket connections
  category network
 attribute kde4 library
       url $SOURCEFORGE_URL
    source fwbuilder/libfwbuilder-$VERSION.tar.gz
  homepage http://www.fwbuilder.org
      opts --prefix=/opt/kde4 --with-qt-dir=/opt/kde4 --disable-debug --enable-final
  estimate 500
      desc 'libfwbuilder provides libraries required by fwbuilder.'

build(){
 echo /opt/kde4 > qtdir
(
 export PATH+=:/opt/kde4/bin
 ./autogen.sh &&
 default_build
)
}

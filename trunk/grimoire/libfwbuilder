# is 4.2.0.3530 a stable or unstable release?
# No 4.2.0 release?

   version stable 4.1.3 4.1.2 4.1.1
   require qt-everywhere-opensource
  optional net-snmp '' '' for net-snmp integration
  optional openssl  '' '' for secure socket connections
      opts --prefix=/opt/kde4 --with-qt-dir=/opt/kde4 --disable-debug --enable-final
  category network
 attribute kde4 library makej
 attribute multilib-fail
    source $SOURCEFORGE_URL fwbuilder/Current_Packages/$VERSION/libfwbuilder-$VERSION.tar.gz
      info last  20110727
      info home  http://www.fwbuilder.org/
      info docs  http://www.fwbuilder.org/4.0/documentation.html
      desc 'libraries required by fwbuilder'

build(){
(
 if    [[ $HOSTTYPE == x86_64 ]]; then
  if   [[ $CFLAGS   =~ -m32   ]]
  then export PATH+=:/opt/kde4/i686/bin; echo /opt/kde4/i686 > qtdir
  else export PATH+=:/opt/kde4/bin;      echo /opt/kde4      > qtdir
  fi
 else  export PATH+=:/opt/kde4/bin;      echo /opt/kde4      > qtdir
 fi

 ./autogen.sh &&
 INSTALL_ROOT=$DESTDIR default_build
)
}

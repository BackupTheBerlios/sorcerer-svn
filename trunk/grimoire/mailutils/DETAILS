   VERSION=( "0.4" )
  CATEGORY="email"
 ATTRIBUTE="console client|server"
    SOURCE="mailutils/mailutils-$VERSION"
       URL="$GNU_URL"
  HOMEPAGE="http://www.gnu.org/software/mailutils"
       REQ="guile libtool ncurses readline"
   PROVIDE="email-client pop3 pop3s imap imaps"
#   OPT[?]="gss heimdal:--with-gssapi::for gssapi/kerberos 5 authentication"

    OPT[0]="Linux-PAM::--disable-pam:for pluggable authentication"
    OPT[1]="gnutls:--with-gnutls::to enable TSL/SSL server-side encryption via gnutls in IMAP4/POP3 daemons"
    OPT[2]="mysql:--with-mysql::to enable authnetication using mysql "
    OPT[3]="postgresql:--with-postgres::to enable authneticaion via postgresql"
    OPT[4]="emacs:--enable-mh-utils::to provide an interface between Mailutils and Emacs using mh-e module"

    OPT[5]="stunnel:::for imaps or pops services"
    OPT[6]="xinetd:::for running servers"

# Not sure if this disables building the servers or not.
#   OPT[7]="::--disable-imap:for imap4 server"
#   OPT[8]="::--disable-pop:for pop3 server"
  
 FRESHMEAT="mailutils"
  ESTIMATE="1152"
#  FEATURE="$FEATURE libar"
     SHORT="mailutils contains a series of useful mail clients, servers, and libraries."
cat << EOF
mailutils contains a series of useful mail clients, servers and libraries.
These are the primary mail utilities of the GNU system.
It contains a POP3 server, an IMAP4 server, and a Sieve mail filter.
It also provides a POSIX 'mailx' client and a collection of other tools.

Using pop3d or imap4d through stunnel has not 
been tested with this spell and may not work.
If it does not, please fix and report it
or use pine's SSL mail servers instead.
EOF

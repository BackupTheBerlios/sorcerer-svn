    stable 0.7.2 0.7.1 0.7.0 0.6.2 0.6.0
   require file gtk+
  category utility/file-manager
 attribute x11
    source http://emelfm2.net/rel/emelfm2-$VERSION.tar.bz2
      vurl http://emelfm2.net/
  homepage http://emelfm2.net/
  estimate 250
      desc 'file manager
emelfm2 is a file manager that
implements the popular two-pane design.
It features a simple gtk interface,
a flexible filetyping scheme,
and a built-in command line for executing
commands without opening an xterm.'


build(){
make            PREFIX=/usr &&
prepare_install &&
make    install PREFIX=/usr
}

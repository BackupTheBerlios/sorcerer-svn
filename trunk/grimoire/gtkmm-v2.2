    stable 2.2.12 2.2.9 2.2.8 2.2.7 2.2.6
   require gtk+ libsigc++-v1.2
    ignore '^2\.[3-9]\.\|^2\.1[0-9]\.\|gui\|24-\|hello\|documentation'
  category graphic
 attribute library x11
 attribute broke
       url $GNOME_URL
    source sources/gtkmm/${VERSION:0:3}/gtkmm-$VERSION.tar.bz2
  homepage http://gtkmm.sourceforge.net
# case $VERSION in
#  2.2.12) use_gcc gcc-v4.0 ;;
# esac
# No longer compiles, not sure entirely why.
  estimate 1364
      desc 'gtkmmm-v2.2 is a C++ interface for the popular GUI library gtk+.'

build(){
if   [ -d gtkmm-$VERSION ]
then cd   gtkmm-$VERSION
fi
default_build
}

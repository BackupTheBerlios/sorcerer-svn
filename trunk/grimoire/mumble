# 1.1.4 does not compile.
# No successful compiles for mumble, yet

    stable 1.1.4
  category network/chat
 attribute kde4
 attribute broke
       url $SOURCEFORGE_URL
    source mumble/mumble-$VERSION.tar.gz
  homepage http://mumble.sourceforge.net/
      opts -DCMAKE_INSTALL_PREFIX:PATH=/opt/kde4
      opts -DCMAKE_INSTALL_RPATH:PATH=/opt/kde4/lib:/opt/kde4/lib64
      opts -DCMAKE_SKIP_BUILD_RPATH:BOOL=TRUE
      opts -DCMAKE_BUILD_WITH_INSTALL_RPATH:BOOL=TRUE
      opts -DCMAKE_INSTALL_RPATH_USE_LINK_PATH:BOOL=TRUE
      opts -DCMAKE_BUILD_TYPE=Release
  estimate 1000
      desc 'mumble is a chat program similar to roger wilco and team speak'

build(){
 sed -i 's:/usr/local:/usr:' src/mumble/mumble.pro
 export PATH+=:/opt/kde4/bin
 qmake main.pro
 make &&
 prepare_install &&
 make    install
}

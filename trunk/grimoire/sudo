    stable 1.6.9p19 1.6.9p18 1.6.9p17 1.6.9p16 1.6.9p15 1.6.9p14
  optional Linux-PAM '' '' for pluggable authentication
  optional openldap  '' '' for storing sudoers info in LDAP
  category utility
 attribute console
  homepage http://www.sudo.ws/sudo/
    source http://www.sudo.ws/sudo/dist/sudo-$VERSION.tar.gz
   protect /etc/sudoers
  estimate 250
      desc 'sudo grant limited superuser ability to users.'

build(){
EDITOR=`which $EDITOR 2>/dev/null` ||
EDITOR=`which nano`
OPTS+=" --with-editor=$EDITOR"

if   spell_installed Linux-PAM ||
     spell_held      Linux-PAM
then OPTS+=' --with-pam'
else OPTS+=' --without-pam'
fi

./configure     \
--prefix=/usr   \
$OPTS           &&
make            &&
prepare_install &&
make    install
}

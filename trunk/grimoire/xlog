   VERSION=( "0.9.6" "0.9.5" "0.9.4" "0.9.3" "0.9.1" "0.9" )
  CATEGORY="ham"
 ATTRIBUTE="x11"
    SOURCE="xlog-$VERSION.tar.gz"
       URL="http://www.qsl.net/pg4i/download"
  HOMEPAGE="http://www.qsl.net/pg4i/linux/xlog.html"
       REQ=( "flex gtk+ libgnomeprint"
             "hamlib:--enable-hamlib::to read frequence of rig over serial port" )
  ESTIMATE="81"
      DESC="xlog is a ham radio logging program for X

xlog will display a log with the most 
recent contacts added to the top.
You can add, delete and edit log entries.
The log is saved into a text file.
When clicking on the log column called Call, 
you can sort the log either ascending, descending or by entry.
Xlog is able to do DXCC lookups and will display country 
information, CQ and ITU zone, location in latitude and longitude 
and distance and heading in kilometers or miles, 
both for short and long path."


build() {
./configure  --prefix=/usr         \
             --sysconfdir=/etc     \
             --localstatedir=/var  \
             $OPTS                 &&
make                               &&
prepare_install                    &&
make    install-strip
}

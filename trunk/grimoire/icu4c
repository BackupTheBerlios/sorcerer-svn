# source is written incorrectly.
# double check this spell when updating.

    stable 4_2
  category font/library
 attribute library new
    source http://download.icu-project.org/files/icu4c/4.2/icu4c-4_2-src.tgz
  homepage http://icu-project.org/
  estimate 2000
   require glibc
      desc 'set of C/C++ and Java libraries that provide Unicode and Globalization
support for software applications'

build(){
 cd source/ &&
 ./runConfigureICU Linux --prefix=/usr &&
 make &&
 prepare_install &&
 make    install
}

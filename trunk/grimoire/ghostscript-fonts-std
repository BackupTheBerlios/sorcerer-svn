   VERSION=( "6.0" )
    IGNORE="8\.11"
# Wed Aug 27 23:06:53 UTC 2003
# only getting partial file transfer of 8.11
# was upload to sourceforge servers aborted before finished?
  CATEGORY="font"
 ATTRIBUTE="data"
    SOURCE="ghostscript/ghostscript-fonts-std-$VERSION.tar.gz"
       URL="$SOURCEFORGE_URL"
  HOMEPAGE="http://www.ghostscript.com"
  ESTIMATE="3"
      DESC="ghostscript-fonts-std provides fonts for ghostscript."

build() {
prepare_install                                   &&
mkdir    -p         /usr/share/ghostscript/fonts  &&
install  -m 644  *  /usr/share/ghostscript/fonts
}

with version stable 15.8.4
with base    readline
with role    mathematics
with trait   console hidden
with source  http://mathomatic.org/mathomatic-$VERSION.tar.bz2
with info    last 20120510
with info    home http://www.mathomatic.org/math/
with info    cite 'command-line CAS and calculator software'

build(){
 make READLINE=1 &&
 make m4install prefix=$DESTDIR/usr &&
 cd primes &&
 make &&
 make install prefix=$DESTDIR/usr &&
 sed -i "s:$DESTDIR::" $DESTDIR/usr/bin/matho
}

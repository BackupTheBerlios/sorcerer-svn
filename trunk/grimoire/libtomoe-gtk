# Replaced by tomoe-gtk
# Not actucally broke

   version stable 0.5.1
   require gucharmap-v2.22 tomoe
  category input
 attribute library x11 deprecated broke
    source http://keihanna.dl.sourceforge.jp/ \
           http://kyushu-u.dl.sourceforge.jp/ \
           http://osdn.dl.sourceforge.jp/ \
           tomoe/23909/libtomoe-gtk-$VERSION.tar.gz
  homepage http://sourceforge.jp/projects/tomoe/
      opts --enable-static=no
      desc 'handwriting identification library'

build(){
 case $VERSION in
  0.5.1) export PKG_CONFIG_PATH=/opt/gucharmap/2.23/lib/pkgconfig
         sed -i 's:test doc:doc:' Makefile.in ;;
 esac &&
 default_build
}

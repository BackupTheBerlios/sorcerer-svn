# gripes.c:(.text+0x2af): undefined reference to `catopen'
# catopen not implemented in uClibc

case $MACHTYPE in
 *-uclibc) attribute broke ;;
esac

   version stable 1.6g 1.6f 1.6e
   require bzip2 groff gzip less man-pages
  category documentation
 attribute console
    source http://primates.ximian.com/~flucifredi/man/man-$VERSION.tar.gz
  homepage http://primates.ximian.com/~flucifredi/man/
      desc 'displays the manual pages'

build(){
sed -i 's:-is:-irs:' configure
./configure -d &&
make &&
make install DESTDIR=$DESTDIR
}

   VERSION=( "1.4.0" )
  CATEGORY="graphic/library"
 ATTRIBUTE="library x11 new"
    SOURCE="glew/glew-$VERSION-src.tgz"
       URL="$SOURCEFORGE_URL"
  HOMEPAGE="http://glew.sourceforge.net/"
       REQ="MesaLib"
  ESTIMATE="20"
      DESC="glew is an OpenGL extension wrangler library."

build()  {
  make             &&
  prepare_install  &&
  make    install
}

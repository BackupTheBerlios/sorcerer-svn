# 1.5.1-src is polluted with carriage returns
# Circuliar requierment with MesaLib, this could be bad.

    stable 1.5.01.4.0
  unstable 1.5.1
   require libXi libXmu
  category graphic/library
 attribute library x11
    source $SOURCEFORGE_URL glew/glew/$VERSION/glew-$VERSION-src.tgz
  homepage http://glew.sourceforge.net/
      desc 'an OpenGL extension wrangler library.'

build(){
make &&
prepare_install &&
make    install
}

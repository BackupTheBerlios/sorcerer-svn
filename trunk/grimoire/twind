    stable 1.1.0
   require SDL_image SDL_mixer
  category game
 attribute new x11
       url $SOURCEFORGE_URL
    source twind/twind-$VERSION.tar.gz
  homepage http://www.linux-games.com
      desc 'twind is twin distress, a block removal game.'


build(){
sed -i "s:/local::
        s:-Wall -g:$CFLAGS:
        s:-lm:-lm $LDFLAGS:" Makefile
make            &&
prepare_install &&
make    install
}

   version stable 1.0.0 0.9.9 0.9.8
   require DirectFB ncurses
  optional PAM           --enable-pam       --disable-pam       for plugable authentication
  optional libXScrnSaver --enable-x-support --disable-x-support for x11 support only with xf86
  optional openssl       ''                 ''                  to encrypt communications with its user interface
  category utility/terminal-emulator
 attribute console
    source $SOURCEFORGE_URL qingy/qingy/qingy%20$VERSION/qingy-$VERSION.tar.bz2
      info home http://qingy.sourceforge.net
      desc 'qingy is a getty that uses DirectFB.
By using DirectFB qingy provides a GUI
without the overhead of the X Window System.
qingy allows the user the choice of starting
sessions such as text console, gnome, kde, wmaker, fluxbox, etc...'

build(){
 adjust_pam(){
  if   [   -f                      /etc/pam.d/qingy ]
  then sed -i 's:/lib/security/::' /etc/pam.d/qingy
  fi
 }

 default_build &&
 adjust_pam
}

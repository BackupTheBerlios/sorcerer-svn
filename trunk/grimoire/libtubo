with info    last 20131120
with version stable 5.0.11 5.0.10 5.0.8
with base    glib
with role    development
with trait   library multilib-concurrent
# 5.0.8 lacked makej compatible makefiles
with source  stable $SOURCEFORGE_URL xffm/libtubo/tubo-$VERSION.tar.gz
with info    home http://xffm.sourceforge.net/libtubo.html
with info    cite 'library for invoking background processes
It enables the foreground and background processes
to communicate via the stdout, stderr, and stdin file descriptors.'

build(){ default_build; rm -f "$DESTDIR"/usr/bin/example; }

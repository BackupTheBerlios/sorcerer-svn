    stable 0.3.11 0.3.5 0.3.4
   require avahi libsoup
  category utility
 attribute gnome
 attribute broke
    source $GNOME_URL sources/libepc/${VERSION:0:3}/libepc-$VERSION.tar.bz2
  homepage http://www.gnome.org
      opts --enable-static=no
  estimate 500
      desc 'publish and consume library'

build_old(){
case $VERSION in
 0.3.11) sed -i 's:GTK_WIDGET_VISIBLE:gtk_widget_get_visible:' libepc-ui/password-dialog.c ;;
esac
default_build
}

# vidinput_avc.cxx:166: error: 'raw1394_set_iso_handler' was not declared in thisscope
# 2.8.1 failed to compile

# 2.10.2 has compile failure for trying to redefine a variable
# already defined in an installed header file.

with version stable 2.10.7 2.10.2
with base    SDL alsa-lib expat lua openldap unixODBC v4l-utils
#with base    SDL alsa-lib expat libv4l lua openldap unixODBC
with also    --enable-shared --disable-static
case $VERSION in
 2.10.2) with also --disable-odbc ;;
esac
with elect   pulseaudio
with role    development
with trait   broke
with trait   gnome library
#trait makej multilib-concurrent
with source  $GNOME_URL sources/ptlib/${VERSION:0:4}/ptlib-$VERSION.tar.xz
with info    last 20120824
with info    home http://www.gnome.org
with info    cite 'moderately large class library
ptlib began as as PWLib (portable Windows Library)'

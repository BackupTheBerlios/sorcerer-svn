   VERSION=( "0.5.beta1" "0.4b1" "0.3.3" "0.3.2" "0.3.1" )
      SAFE=( "0.4b1" )
    IGNORE="^0\.4rc1\|pre"
# 0.4rc1 and 0.4 failed to compile
  CATEGORY="network"
 ATTRIBUTE="console"
    SOURCE="ipsec-tools/ipsec-tools-$VERSION.tar.bz2"
#   SOURCE="ipsec-tools/ipsec-tools-$VERSION.tar.gz"
       URL="$SOURCEFORGE_URL"
  HOMEPAGE="http://ipsec-tools.sourceforge.net"
       REQ="openssl"
  ESTIMATE="141"
 FRESHMEAT="ipsectools"
      DESC="ipsec-tools is a Linux port of the user-space tools from KAME.

It includes:

libipsec   a library with a PF_KEY implementation;
setkey     a tool for manipulating and dumping the kernel
             Security Policy Database and Security Association Database;
racoon     an Internet Key Exchange daemon for automatically
              keying IPsec connections"

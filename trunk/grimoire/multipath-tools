  stable   0.4.8
  CATEGORY='disk'
 ATTRIBUTE='console new'
    SOURCE="multipath-tools-$VERSION.tar.bz2"
  HOMEPAGE='http://christophe.varoqui.free.fr/'
       URL='http://christophe.varoqui.free.fr/multipath-tools/'
       REQ='device-mapper udev libaio ncurses readline'
  ESTIMATE='274'
      DESC='multipath-tools for multipathed storage environment (FC, SAN,...)
The multipath term simply means that a host can access a LU by multiple 
paths, the path being a route from one host HBA port to one storage 
controller port.'

build(){
  case $VERSION in
   0.4.8)  patch -p1 < $SCRIPT_DIR/kpartx.rules.diff  ;;
  esac

  make             &&
  prepare_install  &&
  make    install  &&
  install -v -m 0644 -o root -g root multipath.conf.annotated /etc/multipath.conf
}

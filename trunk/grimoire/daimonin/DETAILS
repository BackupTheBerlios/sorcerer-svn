   VERSION=( "096a" "95b2" "95b1" )
# Odd that both server and client expand to the same directory.
# could be a problem if both cast concurrently.
# This spell is incomplete, however the client should be playable.
# It is not at a stable release yet anyway, but maybe fun to play
# once it is?
  CATEGORY="game"
 ATTRIBUTE="concurrent new x11"
    ASSUME="off"
       URL="$SOURCEFORGE_URL"
  HOMEPAGE="http://daimonin.sourceforge.net"
       REQ="daimonin,client unzip"
    OPT[0]="daimonin,server"
     SHORT="daimonin is a Massive Multiuser Online Role Playing Game (MMORPG)"
stream << EOF
daimonin is a Massive Multiuser Online Role Playing Game (MMORPG).
It has 2D isometric tiles grafik, true color and alpha blending effects. 
It use the SDL library for high speed and cross platform clients.
It use WAV for sound effects and .ogg for background music.
EOF

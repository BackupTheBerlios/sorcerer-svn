# Can this download?
# No, tarball generation is disabled.
# Impossible to download without svn checkout?

   version stable svn
   require gtk+
#  require gtk+ hal
  category development
 attribute gnome broke
#   source http://anonsvn.mono-project.com/viewcvs/trunk/gnome-keyring-sharp.tar.gz
  homepage http://www.gnome.org
case $VERSION in
 svn) require automake ;;
esac
      desc 'implements gnome-keyring in C#'

build(){
 if ! [ -f configure ]; then
  aclocal
  automake -a
  autoconf
 fi
 default_build
}

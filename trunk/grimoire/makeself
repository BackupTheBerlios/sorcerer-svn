   version stable 2.1.5
  category development/emulator
 attribute console
    source http://www.megastep.org/makeself/makeself-$VERSION.run
      info home  http://www.megastep.org/makeself/
      desc 'shell script that generates a self-extractable tar.gz archive from a directory'

build(){
           BUILD_DIR=$SOURCE_DIR/makeself
 mkdir -p $BUILD_DIR
 cd       $BUILD_DIR
 sh $SOURCE
 cd       makeself-$VERSION
 mkdir  -pvm 755 $DESTDIR/usr/{bin,man/man1}
 install -vm 755 makeself.sh makeself-header.sh $DESTDIR/usr/bin &&
 ln      -vs     makeself.sh                    $DESTDIR/usr/bin/makeself &&
 install -vm 644 makeself.1 $DESTDIR/usr/man/man1
}

# Hmm, what files does it write/modify?
# not a DESTDIR compatibile spell?

    stable 20091107
   require texlive
  category documentation
 attribute console broke
  homepage http://www.tug.org/texlive/
   protect /usr/texmf /usr/texmf-dist
      desc 'Updates texmf indexes'

build(){
if [ -d /usr/texmf      ]; then mktexlsr /usr/texmf;      fi &&
if [ -d /usr/texmf-dist ]; then mktexlsr /usr/texmf-dist; fi
}

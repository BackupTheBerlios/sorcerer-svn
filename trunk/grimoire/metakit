# might compile with gcc < 4.6

   version stable 2.4.9.7 2.4.9.6 2.4.9.5
   require automake
case $VERSION in
 2.4.9.7)
   use_gcc gcc-v4.5 ;;
esac
  optional Python --enable-python '' to build Mk4py extension
  optional tcl    ''              '' to build Mk4tcl extension
  category db
 attribute library lto_off
    source http://www.equi4.com/pub/mk/metakit-$VERSION.tar.gz
      info home http://www.equi4.com/metakit/
      desc 'database library with a small footprint
metakit is an efficient database library with a small footprint.
It fills the gap between flat-file, relational databases, and OODBMS.
It features structured storage, transacted commit, on-demand loading
and portability.'

build(){
export LDFLAGS+=" $CFLAGS"
sed -i "s:-O2:$CXXFLAGS:"  unix/Makefile.in
cd  builds
../unix/configure \
--prefix=/usr \
--sysconfdir=/etc \
--localstatedir=/var \
$OPTS &&
make  &&
make install DESTDIR=$DESTDIR
}

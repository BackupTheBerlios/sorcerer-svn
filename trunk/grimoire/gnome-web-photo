# configure: error: libxul not found
# Requires a mozilla 2.0 element
# not sure why it can not find it in firefox.
   VERSION=( 0.4 )
  CATEGORY='development'
 ATTRIBUTE='gnome library broken'
    SOURCE="sources/gnome-web-photo/${VERSION:0:3}/gnome-web-photo-$VERSION.tar.bz2"
  HOMEPAGE='http://www.gnome.org'
       URL="$GNOME_URL"
       REQ=( '' )
      OPTS='--enable-static=no'
  ESTIMATE='7000'
      DESC='gnome-web-photo provides ...'

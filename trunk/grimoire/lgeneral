    stable 1.2beta-13
   require SDL_mixer
  category game
 attribute x11
       url $SOURCEFORGE_URL
    source lgeneral/lgeneral-$VERSION.tar.gz
    source lgeneral/pg-data.tar.gz
  homepage http://lgames.sourceforge.net
  estimate 700
      desc 'lgeneral is a port of Panzer General, a turn based strategy game.

lgeneral is a turn-based strategy engine heavily inspired by Panzer General.
You play single scenarios or whole campaigns
turn by turn against a human player or the AI.
Entrenchment, rugged defense, defensive fire,
surprise contacts, surrender, unit supply,
weather influence, reinforcements and other
implementations contribute to the
tactical and strategic depth of the game.'

build(){
install_data(){
 export SDL_VIDEODRIVER=dummy
 mkdir  -p            /usr/share/games/lgeneral
 lgc-pg -s pg-data -d /usr/share/games/lgeneral
}

./configure     \
--prefix=/usr   &&
make            &&
prepare_install &&
make    install &&
        install_data
}

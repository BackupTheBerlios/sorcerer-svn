    stable 2.1.3
   require expat gmp ncurses readline
  optional openssl '' '' for secure socket connections
  optional tk      '' '' for tk windowing system

  category development/interpreter
 attribute console x11
    source Python-$VERSION.tgz
       url "http://www.python.org/ftp/python/$VERSION/
             ftp://ftp.python.org/pub/python/$VERSION/"
  homepage http://www.python.org
  estimate 350
      desc 'Python-v2.1 is an interpreted, interactive, object-oriented programming language.'

build(){
 make_shared_lib()  {
  mkdir .extract
  cd    .extract
  ar xv ../libpython2.1.a 1>/dev/null 2>&1
  cd ..
  cc -shared -o libpython2.1.so .extract/*.o
  cp libpython2.1.so  /usr/lib
  ln -sf /usr/lib/libpython2.1.so \
         /usr/lib/python2.1/config/libpython2.1.so
 }

./configure \
--prefix=/usr \
$OPTS         &&
make "OPT=$CFLAGS" &&
prepare_install    &&
make altinstall    &&
make_shared_lib
}

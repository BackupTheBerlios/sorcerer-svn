# 3.4.1 failed compilation
# 3.4.0 no longer compiles

with version   stable 3.4.4 3.4.3 3.4.1 3.4.0
with version unstable 3.5.2 3.5.1 3.4.1
with base    evolution
with also    --enable-static=no
with role    utility/office
with trait   gnome library
with trait   broke
with source    stable $GNOME_URL sources/evolution-exchange/${VERSION:0:3}/evolution-exchange-$VERSION.tar.xz
with source  unstable $GNOME_URL sources/evolution-exchange/${VERSION:0:3}/evolution-exchange-$VERSION.tar.xz
with info    omit 20130109
with info    last 20120813
with info    home http://gnome.org/projects/evolution/stable.shtml
with info    cite 'Microsoft Exchange 2000 and 2003 support for evolution'

build_old(){
 export LDFLAGS=' -Wl,-rpath,/usr/lib/mozilla -Wl,-rpath-link,/usr/lib/mozilla'
 default_build
}

   version stable 1.9.7
   require gtkglext
  category graphic/viewer
 attribute x11
    source http://guichaz.free.fr/gliv/files/gliv-$VERSION.tar.bz2
      info home  http://guichaz.free.fr/gliv/
      desc 'OpenGL image viewer
Image loading is done via Gdk-pixbuf bundled with GTK+-2.4,
rendering with OpenGL, and the GUI uses GTK+ with GtkGLExt.
gliv is very fast and smooth at rotating,
panning and zooming if you have an OpenGL
accelerated graphics board.
It supports thumbnails and navigating
through them using two images menus.'

build(){
 export LDFLAGS+=" $( pkg-config --libs pangox )"
 default_build
}

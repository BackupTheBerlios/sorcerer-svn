   VERSION=( 2.3.20 2.3.19 2.3.18 2.3.17 )
  CATEGORY='graphic/library graphic/toolkit'
 ATTRIBUTE='gnome library'
    SOURCE="sources/libart_lgpl/${VERSION:0:3}/libart_lgpl-$VERSION.tar.bz2"
  HOMEPAGE='http://www.levien.com/libart'
       URL="$GNOME_URL"
     OPTS='--enable-static=no'
  ESTIMATE='300'
 FRESHMEAT='libart'
      DESC='libart_lgpl is a library for high performance 2D graphics.
It supports a very powerful imaging model, basically
the same as SVG and the Java 2D API.
It includes all PostScript imaging operations,
and adds antialiasing and alpha-transparency.
It is also highly tuned for incremental rendering.
It contains data structures and algorithms suited
to rapid, precise computation of Region of Interest,
and a two-phase rendering pipeline optimized for interactive display.'

current(){
 case $VERSION in
  2.3.20)
   ( grep -rl /opt/kde3/lib/libart_lgpl_2.so.2 $LDD_LOGS | basenames
     view_index "att${TAB}kde3" | sed p
   ) | LC_ALL=C sort | uniq -u | pipe_queue $CAST_QUEUE com
   grep libart-2.0.pc $MD5SUM_LOGS/libart_lgpl | md5sum -c &>/dev/null ;;
 esac
}

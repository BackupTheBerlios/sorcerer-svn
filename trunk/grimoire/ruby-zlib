    stable 0.6.0 0.4.0
   require ruby
  category development/interpreter/ruby
 attribute library
    source http://www.blue.sky.or.jp/atelier/ruby/ruby-zlib-$VERSION.tar.gz
  homepage http://www.blue.sky.or.jp/atelier/ruby/
      desc 'ruby-zlib provides a compression library for Ruby'

# What an odd build()
# Is it correct to run prepare_install from the start?
build(){
prepare_install &&
ruby extconf.rb &&
make &&
make    install
}

# Does not have a DESTDIR compatible install method

   version stable 0.6.0 0.4.0
   require ruby
  category development/interpreter/ruby
 attribute library
 attribute broke
    source http://www.blue.sky.or.jp/atelier/ruby/ruby-zlib-$VERSION.tar.gz
      info home http://www.blue.sky.or.jp/atelier/ruby/
      desc 'compression library for Ruby'

build(){
ruby extconf.rb &&
make &&
make install
}

# do not specify --with-pc-path=
# pkgconfig does the proper thing when ELF64 are in lib64 and ELF32 are in lib

# glib and pkgconfig have recursive requirements upon each other

   version stable 0.26 0.25 0.23
#  require glib
      opts --with-installed-glib --enable-shared=yes --enable-static=no
  category development
 attribute console makej
    source http://pkgconfig.freedesktop.org/releases/pkg-config-$VERSION.tar.gz
      info last  20110522
      info home http://www.freedesktop.org/
 freshmeat pkg-config
      desc 'system for managing library compile/link flags.
pkgconfig is a system for managing library compile/link
flags that works with automake and autoconf.
It replaces the ubiquitous *-config scripts.'

   VERSION=( "2.4.2" "2.4.1" "2.4.0" )
  CATEGORY="utility"
 ATTRIBUTE="gnome"
    SOURCE="sources/acme/${VERSION:0:3}/acme-$VERSION.tar.bz2"
  HOMEPAGE="http://www.gnome.org"
       URL="$GNOME_URL"
       REQ="libgnomeui libwnck startup-notification"
  GNOMEFTP="acme"
  ESTIMATE="114"
      DESC="acme is a tool to use multimedia buttons on fancy keyboards.

acme is a small GNOME tool to make use of 
the multimedia buttons present on most 
laptops and Internet keyboards: 
Volume, Brightness, Power, Eject, My Home, 
Search, E-Mail, Sleep, Screensaver, Finance, 
WWW, Calculator, Record, Close Window, 
Shade Window, Play, Stop, Pause, Previous, 
Next, Groups, Media, Refresh, and Help buttons.
It works on all the platforms GNOME supports (laptops and PCs).
It uses either OSS or ALSA for Volume control."


build()  {
  sed  -i  "s:wnck_window_close (focused):wnck_window_close (focused,0):"  \
       src/acme.c
  sed  -i  "s:EggVirtualModifierType   mask):GdkModifierType   mask):
            s:EggVirtualModifierType  \*mask):GdkModifierType  *mask):"  \
       src/eggcellrendererkeys.h
  default_build
}

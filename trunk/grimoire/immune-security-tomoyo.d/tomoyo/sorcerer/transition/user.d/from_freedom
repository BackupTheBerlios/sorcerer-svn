# Entry into
# <kernel> /domain/user
# <kernel> /media/root/real/domain/user
# permitted from
# <kernel> /domain/freedom
# <kernel> /media/root/real/domain/freedom

confirm(){
 [ -n "$(
  /sbin/tomoyo-pstree |
  /bin/sed "\,($PPID) <kernel> /domain/freedom$,p
            \,($PPID) <kernel> /media/root/real/domain/freedom$,p;d" )" ]
}

if    confirm; then
 if   [ -n "$1" ]
 then PROG="$1"; shift 1; exec $PROG "$@"
 else exec /bin/bash -
 fi
fi

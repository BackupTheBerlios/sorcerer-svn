if (( UID == 0 )) &&
   ! grep -q /domain/freedom /sys/kernel/security/tomoyo/self_domain ||
   (( UID > 1023 ))
then
 echo "Transitioning to the freedom domain can be achieved" 1>&2
 echo "by running sulogin or /usr/bin/sulogin or /domain/freedom" 1>&2
fi

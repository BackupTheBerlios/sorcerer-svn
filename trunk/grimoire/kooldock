# source failing to download 20080105

    stable 0.4.7 0.4.3
   require kdelibs-v3
  category utility
 attribute kde3
 attribute broke
#   source http://www.kde-look.org/content/files/50910-kooldock.tar.gz
  homepage http://www.kde-look.org/content/show.php?content=50910
  estimate 500
      opts --disable-debug --enable-final
      desc 'kooldock provides a dock that resembles the Mac OSX dock.'

build(){
make -f Makefile.dist &&
./configure     \
--prefix=/usr   \
$OPTS           &&
make            &&
prepare_install &&
make    install       
}

    stable 0.4.7 0.4.3
   require kdelibs-v3

  category utility
 attribute kde3 broke
# source failing to download 20080105
#   source 50910-kooldock.tar.gz
       url http://www.kde-look.org/content/files/
  homepage http://www.kde-look.org/content/show.php?content=50910
  estimate 509
      opts --disable-debug --enable-final
      desc 'kooldock provides a dock that resembles the Mac OSX dock.'

build(){
make -f Makefile.dist &&
./configure     \
--prefix=/usr   \
$OPTS           &&
make            &&
prepare_install &&
make    install       
}

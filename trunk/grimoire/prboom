   VERSION=( "2.2.6" "2.3.1" "2.3.0" "2.2.4" "2.2.3" )
    IGNORE="^2\.3\."
# 2.3.x versions are experimental.
  CATEGORY="game"
 ATTRIBUTE="x11"
    SOURCE=( "prboom/prboom-$VERSION.tar.gz"
             "doom-1.8.wad.gz" )
       URL=( "$SOURCEFORGE_URL"
             "ftp://ftp.idsoftware.com/idstuff/doom" )
  HOMEPAGE="http://prboom.sourceforge.net"
       REQ="SDL_mixer SDL_net"
  ESTIMATE="643"
      DESC="prboom is a portable POSIX Doom port.

prboom is a game engine - it provides a program to play Doom levels.
This spell also installs the doom 1.8 shareware levels.

Possibily due to a bug you may have to run 
it from the present working directory of:
/usr/share/games/doom
so it can find prboom.wad"


build() {
 default_build  &&
 (

 WAD="$(  guess_filename  ${SOURCE[1]}  )"
 mkdir  -p             /usr/share/games/doom
 bunzip2  -c  $WAD  >  /usr/share/games/doom/doom1.wad
 touch                 /usr/share/games/doom/doom1.wad

 )
}


pre_build() {
 default_pre_build
 unpack  ${SOURCE[0]}
}


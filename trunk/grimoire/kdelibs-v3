# When kdelibs-v3 compiles it runs
# /usr/src/sorcery/kdelibs-v3/kdelibs-3.5.10/kdecore/kconfig_compiler/.libs/lt-kconfig_compiler
# that is dynamically linked and causes the kdelibs-v3 libraries
# that are installed in /opt/kde3/lib/
# or in /lib.old to be loaded
# and those libraries are dynamically linked with 
# libxcb-xlib.so.0
# which is no longer installed and probably not run properly if it can be loaded.
# Solution is to dispel kdelibs-v3 and clear the library cache
# before re-compiling kdelibs-v3

   inherit kde-v3
case $VERSION in
 3.5.10) opts --disable-inotify
         conflicts kdelibs-v3 ;; # Added due to the complicated update of libxcb
esac
   require arts bzip2 docbook-xml-4.1.2 e2fsprogs libart_lgpl-v2.3.17 lua pcre
  optional aspell   ''              ''                for spell checking 
  optional avahi    ''              --disable-dnssd   for zero conf support
  optional cups     --enable-cups   --disable-cups    for intergration with common unix printing system
  optional gamin    --enable-libfam --disable-libfam  for file system change notification
  optional heimdal  ''              ''                for kerberos support
  optional jasper   ''              ''                for jpeg 2K support
  optional openexr  --with-openexr  --without-openexr for HDR image file format support
  optional openssl  ''              ''                for secure socket layer support
  optional openldap ''              ''                for ldap support
  optional tiff     --with-tiff     --without-tiff    for TIFF image support
  category development
# hspell no longer available?
#            'hspell:::for Hebrew spell checking support'
# has optional for sudo
#            'libart_lgpl-v2.3.17:--with-libart:--without-libart:for high performance 2d graphics'    
      opts --with-libart
  estimate 11100
      desc 'base applications that form the core of KDE
kdelibs contains the base applications that form the core of
the K Desktop Environment like the window manager, the terminal
emulator, the control center, the file manager, and the panel.'

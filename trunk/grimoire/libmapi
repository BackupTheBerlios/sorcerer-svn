# configure: WARNING: No version of pidl was found!
# augur install samba4 | grep -i /Pidl.pm
# /opt/samba4/share/perl5/Parse/Pidl.pm
# Not in a typical usable location for a perl module.
# I might fix this later.

   version stable 0.8.2-ROMULUS
      opts --with-samba=/opt/samba4
   require samba4
  category network/email
 attribute library broke
    source $SOURCEFORGE_URL openchange/libmapi/libmapi-0.8.2/libmapi-$VERSION.tar.gz
  homepage http://www.openchange.org/index.php?option=com_content&task=view&id=63&Itemid=71
      desc 'a library implementation of Microsoft Exchange protocols'

build(){
default_build &&
make install-configure DESTDIR=$DESTDIR
}

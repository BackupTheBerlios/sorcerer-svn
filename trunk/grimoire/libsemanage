# 2.0.33 is devel
# broke because ustr is not downloadable
   version stable 2.0.31 1.10.9
   require Python libselinux ustr
  category security
 attribute console library broke
    source http://userspace.selinuxproject.org/releases/20090403/devel/libsemanage-$VERSION.tar.gz
  homepage http://userspace.selinuxproject.org/trac
      vurl http://userspace.selinuxproject.org/trac/wiki/Releases
      desc 'library and interface for selinux management'

build(){
make "CFLAGS=$CFLAGS" &&
make install DESTDIR=$DESTDIR
}

# No idea where this went or what should replace it  20080105
   version stable 2.4
  category network security
 attribute console broke
#   source http://www.snort.org/pub-bin/downloads.cgi/Download/comm_rules/Community-Rules-$VERSION.tar.gz
      info home http://www.snort.org/
      desc 'some rules for snort'

build(){
[     -f     rules/LICENSE ] &&
chown -R 600 */* &&
mkdir -vp  $DESTDIR/etc/snort &&
cp    -a * $DESTDIR/etc/snort
}

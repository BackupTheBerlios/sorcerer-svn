# Please do not install freeglut, use MesaLib instead.
# MesaLib 8.0 and 8.0.1 do not provide MesaGlut

with version stable 2.8.0 2.6.0
with base    glu imake libXrandr xorg-cf-files
with also    --disable-static
with role    graphic
with trait   library x11
with source  $SOURCEFORGE_URL freeglut/freeglut/$VERSION/freeglut-$VERSION.tar.gz
with info    last 20120102
with info    home http://freeglut.sourceforge.net/
with info    docs http://freeglut.sourceforge.net/docs/api.php
with hold    glut
with info    cite 'graphic toolkit for OpenGL programs
freeglut is intended to be a 100% plug-compatible clone,
and is released under the Xfree license.
The GLUT (OpenGL Utility Toolkit) is a
widely used library for doing window control,
keyboard, and mouse input, and other basic
functions for OpenGL programs.'

build(){
 case $VERSION in
  2.8.0) sed -i 's:smooth_opengl3::' progs/demos/Makefile.in ;;
 esac
 default_build
}

current(){ echo MesaGLUT | pipe_queue "$MOIL_QUEUE"; }

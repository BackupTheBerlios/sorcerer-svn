# still under development in June 5th, 2010 a pre-release for 0.4.0
# Not certain how to do DESTDIR with scons
# when updating this to a newer version
# redo the build()

    stable 0.3.0 0.2.0 0.1.1 0.1.0 0.0.19
case $VERSION in
 0.3.0) use_gcc gcc-v4.2 ;;
esac
   require SDL_image SDL_mixer SDL_net fftw scons
  category game
  attribute x11 new broke

    source $SOURCEFORGE_URL dangerdeep/danger_from_the_deep-src/$VERSION/dangerdeep-$VERSION.tar.gz
    source $SOURCEFORGE_URL dangerdeep/danger_from_the_deep-data/$VERSION/dangerdeep-data-$VERSION.zip
  homepage http://dangerdeep.sourceforge.net
  estimate 150
      desc 'World War II german submarine simulation'

build(){
sed -i 's:/usr/local/:/usr/:' SConstruct
scons  &&
scons install &&
cp -r data /usr/share/dangerdeep
}

    stable 1.4.1 1.3.1 1.3.0 1.2.1
  optional Canna       --with-canna ''               for canna Japanese input system
  optional anthy       ''           --without-anthy  for anthy Japanese input system
  optional gnome-panel ''           --disable-applet for gnome applet indicator
  optional gtk+        ''           --without-gtk2   for gtk tools and immodule
  optional m17n-lib    ''           ''               for a multilingual text processing library
  optional mana        ''           --without-mana   for mana Japanese input system
  optional prime       ''           --without-prime  for prime Japanese input system

    ignore 'alpha\|beta'
  category input
 attribute console library x11 broke
    source uim-$VERSION.tar.bz2
       url http://uim.freedesktop.org/releases/uim/stable/
  homepage http://uim.freedesktop.org/wiki/
# In uim 1.4.1 there might be a kde-applet, but for kde 3.x?
#             ':--enable-kde-applet::for kde panel applet'
      opts --enable-dict --enable-static=no
case $VERSION in
 1.4.1) use_gcc gcc-v4.2 ;;
esac
  estimate 2000
      desc 'uim is a multilingual input method library.
The project goal is to provide a flexible
development platform and useful user environment for input methods.'

build(){
# The following case statement does not provide gcc 4.3.x compatibility
 case $VERSION in
  1.4.1) sed -i '1i#include <iostream>
                 1iusing namespace std' xim/ximserver.cpp ;;
 esac
 default_build
}

# if    [  -x  /etc/init.d/exim.sh  ]
# then         /etc/init.d/exim.sh  stop
# fi

PID="/var/spool/mail/exim-daemon.pid"
if  [   -e        $PID  ];  then
  kill  -15  $(<  $PID  )
  rm    -f        $PID
fi

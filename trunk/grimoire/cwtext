    stable 0.96 0.95 0.94 0.93 0.91
   require Python
  category utility/ham
 attribute console
    source $SOURCEFORGE_URL cwtext/cwtext/cwtext%20$VERSION/cwtext-$VERSION.tar.gz
  homepage http://cwtext.sourceforge.net
      desc 'converts text to morse code
Convert text to International Morse Code.
Input is ASCII text.
Output can be: - . -..- - on the console,
raw 8bit PCM suitable for piping to /dev/audio,
.wav files or even (mp3|ogg).
Good for headlines on your MP3 player or code practice.'

build(){
make &&
mkdir -p $DESTDIR/usr/bin &&
install -sc -vm 755 cwtext $DESTDIR/usr/bin &&
install -sc -vm 755 cwpcm  $DESTDIR/usr/bin
}

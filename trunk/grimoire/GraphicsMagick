   VERSION=( "1.1.7" )
  CATEGORY="printer"
 ATTRIBUTE="console x11 broken new"
# Compile error
    SOURCE="GraphicsMagick-$VERSION.tar.bz2"
       URL="ftp://ftp.graphicsmagick.org/pub/GraphicsMagick/"
  HOMEPAGE="http://www.graphicsmagick.org/"
#       REQ=( "bzip2 freetype jpeg libmng libpng libtool libxml2 tiff"
#             "librsvg::--without-rsvg:for SVG vector-graphic files support"
#             "perl::--without-perl:to make PerlMagick"
#             "x11:--with-x:--without-x:to enable ImageMagick's X11 support so that animate, display, and import will work" )

# Has an optional requirement on graphviz, but not clear why.
# what's the dot coder?
# Can create maybe a test program that links with gtk+
# but no part of the installed ImageMagick is linked with gtk+
# Therefore, gtk+ is not an optional requirement for ImageMagick
case  $VERSION  in
  1.1.7)  DICTIONARY="ImageMagick-6.3.0-0.tar"  ;;
esac
      OPTS="--enable-shared --with-modules --enable-ltdl-install=no"
  ESTIMATE="2030"
 FRESHMEAT=""
      DESC="GraphicsMagick is a fork of ImageMagick 5.5.2"

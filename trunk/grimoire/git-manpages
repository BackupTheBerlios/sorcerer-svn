   inherit git
  category development documentation
 attribute console
    source $KERNEL_URL pub/software/scm/git/git-manpages-$VERSION.tar.bz2
      desc 'man pages for git'

build(){
 MAN_DIR=/usr/share/man
 prepare_install &&
 install -vd $MAN_DIR &&
 cp -vr * $MAN_DIR
}

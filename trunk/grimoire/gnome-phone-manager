# broke because libbtctl is broke

   version stable 0.68 0.60 0.51 0.50
   require evolution-data-server gnokii gnome-bluetooth gstreamer gtkspell libbtctl
  category documentation
 attribute gnome new
 attribute broke
    source $GNOME_URL sources/gnome-phone-manager/${VERSION:0:4}/gnome-phone-manager-$VERSION.tar.xz
      info home http://www.gnome.org/softwaremap/projects/gnome-phone-manager
      desc 'gnome-phone-manager is a program for connecting a mobile phone to the computer.
This program will connect to your mobile phone over a serial port,
either via a cable, infra-red or Bluetooth connection.

It listens for text messages, and when they arrive, displays them on the desktop.
A visual indicator is displayed in the notification area,
if one is presently added to the panel.'

build(){
 export LDFLAGS+=' -Wl,-rpath,/usr/lib/mozilla -Wl,-rpath-link,/usr/lib/mozilla'
 default_build
}

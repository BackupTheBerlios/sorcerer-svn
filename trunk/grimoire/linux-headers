    stable 2.6.29.2 2.6.29.1
  category development
 attribute library solo
       url $KERNEL_URL
    source pub/linux/kernel/v${VERSION:0:3}/linux-$VERSION.tar.bz2

build(){
 make headers_install &&
 prepare_install &&
 cp -av usr/include/* /usr/include/
}

with info    edit 20131211
with inherit kde-v4
with base    kde-runtime
with role    mathematics
with trait   library makej multilib-concurrent
with trait   litelink
with source    stable $KDE_URL   stable/$VERSION/src/$TECHNIC-$VERSION.tar.xz
with source  unstable $KDE_URL unstable/$VERSION/src/$TECHNIC-$VERSION.tar.xz
with info    cite 'full featured Graph - Editor and a Programming Enviroment
that is connected to the Graph by doing it so,
it can be used for teachers to show the joy and mistery of Graph Theory,
and studants can use it to implement the various algorithms.'

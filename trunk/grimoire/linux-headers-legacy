# Keep the version of linux-headers the same
# as the lowest version supported by the linux-2.6* spells
# but do not back it up to 2.6.27 since 2.6.30 has been running fine.

   version stable	2.6.37.4	2.6.37.3
   version 2.6.36.x	2.6.36.4
   version 2.6.35.x	2.6.35.11
   version 2.6.34.x	2.6.34.8
   version 2.6.32.x	2.6.32.28
   version 2.6.30.x	2.6.30.10
   version 2.6.27.x	2.6.27.58

  category development
 attribute solo
    source $KERNEL_URL pub/linux/kernel/v${VERSION:0:3}/linux-$VERSION.tar.bz2

build(){
 local legacy=/opt/legacy
 rm -rf Documentation
 make headers_install &&
 mkdir -pvm 755       $DESTDIR$legacy/usr/include  &&
 cp -av usr/include/* $DESTDIR$legacy/usr/include/ &&
 find $DESTDIR$legacy/usr/include -type d | xargs --max-lines=256 chmod 755 &&
 find $DESTDIR$legacy/usr/include -type f | xargs --max-lines=256 chmod 644 &&
 find $DESTDIR$legacy/usr/include         | xargs --max-lines=256 chown root:root
}

    stable 1.50.0 1.12.0 1.11.0 1.10.0
  category game
 attribute client data x11
    source $SOURCEFORGE_URL crossfire/crossfire-client/$VERSION/crossfire-client-images-$VERSION.tar.gz
  homepage http://crossfire.real-time.com
      desc 'pre-downloaded images for crossfire client
This will reduce the amount of download that needs
to happen during play if the -cache option is used.'

build(){
rm -fr debian
prepare_install &&
mkdir -p   /usr/share/crossfire-client &&
cp    -a * /usr/share/crossfire-client
}

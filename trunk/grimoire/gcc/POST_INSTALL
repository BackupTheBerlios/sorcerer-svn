sed  -i  "\,/opt/gcc/,d"  /etc/ld.so.conf

for           GCC_DIR  in  /opt/gcc/*/lib
do    [  -d  $GCC_DIR  ]  &&
      echo  "$GCC_DIR"
done  >>  /etc/ld.so.conf

rm    -f  $CCACHE_CACHE/*
unlock_cast
cast  -c  libtool
true

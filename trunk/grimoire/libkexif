# 0.2.5 has compile error.
# stable 0.2.5 0.2.4
    stable         0.2.4
   require kdelibs-v3 libexif

    ignore '200[0-9]\|0\.2\.5\|i486'
  category graphic kde-extragear
 attribute kde3 library
    source kipi/libkexif-$VERSION.tar.bz2
       url "$SOURCEFORGE_URL"
  homepage http://extragear.kde.org/apps/kipi
  estimate 1000
      opts --prefix=/opt/kde3 --with-qt-dir=/opt/kde3 --disable-debug --enable-final
      desc 'libkexif is a library for EXIF handling in KIPI image plugings'

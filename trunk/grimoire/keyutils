# http://people.redhat.com/~dhowells/keyutils/keyutils-1.5.1.tar.bz2
# The above file exists.
# however the software author forgot to bzip2 compress it.
# So it is just an uncompressed tarball.
# leech does not want to download an uncompressed tarball
# because no compression lacks the CRC provided by
# compressing with bzip2, gzip, and xz.

   version stable 1.4
   version unstable 1.5.1
      opts --enable-static=no
  category security/encryption
 attribute library multilib-dual
    source http://people.redhat.com/~dhowells/keyutils/keyutils-$VERSION.tar.bz2
      info last  20100722
      desc 'for controlling the key management system built into linux'

build(){
 make &&
 if   [[ $HOSTTYPE == x86_64 ]] &&
    ! [[ $CFLAGS   =~ -m32   ]]
 then make install DESTDIR=$DESTDIR LIBDIR=/lib64 USRLIBDIR=/usr/lib64
 else make install DESTDIR=$DESTDIR
 fi
}

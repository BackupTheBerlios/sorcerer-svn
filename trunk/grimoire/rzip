    stable 2.1 2.0
   require bzip2

  category archive
 attribute console
    source rzip-$VERSION.tar.gz
       url http://rzip.samba.org/ftp/rzip/
  homepage http://rzip.samba.org/
  estimate 20
      desc 'rzip is a compression program for large and very large files.
It uses an effective history buffer of 900 MB which
allows it to spot redundancy over very long distances.
It is usually faster than bzip2 and usually produces smaller files.'

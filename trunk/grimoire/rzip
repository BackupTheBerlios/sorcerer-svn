   version stable 2.1 2.0
   require bzip2
      opts --prefix=$DEST_DIR/$SPELL/usr
  category archive
 attribute console
    source http://rzip.samba.org/ftp/rzip/rzip-$VERSION.tar.gz
      info home http://rzip.samba.org/
      desc 'compression program for large and very large files
It uses an effective history buffer of 900 MB which
allows it to spot redundancy over very long distances.
It is usually faster than bzip2 and usually produces smaller files.'

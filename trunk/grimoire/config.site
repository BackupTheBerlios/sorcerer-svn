with version stable 2.68
with role    development
with trait   archive_off data solo
with info    good 20120604
with info    home 'http://www.gnu.org/software/autoconf/manual/autoconf.html#Site-Defaults'
with info    cite 'installs a /usr/config.site with useful ./configure defaults'

build(){
 mkdir   -pvm 755 "$DESTDIR/usr/share"
 install  -vm 644  $SCRIPT_DIR/config.site "$DESTDIR/usr/share/"
}

current(){
 case $VERSION in
  2.68) [ -f /usr/share/config.site ] ;;
 esac
}

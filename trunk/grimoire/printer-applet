# README lists as requirement
# hal-cups-utils:  https://fedorahosted.org/hal-cups-utils/
# however hal is deprecated so is this an actual requirement?
# compiles fine without it.

# not included with the kde version 4.10.1 release
with inherit kde-v4
with base    pykde4 kde-runtime system-config-printer
with role    printer
with source    stable $KDE_URL   stable/$VERSION/src/${SPELL%-v4}-$VERSION.tar.xz
with source  unstable $KDE_URL unstable/$VERSION/src/${SPELL%-v4}-$VERSION.tar.xz
with info    cite 'system tray utility for displaying queued print jobs'

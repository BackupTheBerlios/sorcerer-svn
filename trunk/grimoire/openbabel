# /usr/bin/babel installs with a rpath of /usr/lib
# and has no easy method for disabling that in 2.2.3
# 2.3.0 has no obvious compile method other than cmake
# Not using 2.3.0 becuase it installs ELF 64-bit libraries into /usr/lib/

   version   stable 2.2.3
   version unstable 2.3.1
   require gcc-g++ libxml2
      opts --enable-static=no
  category utility/chemistry
 attribute library
    source   stable $SOURCEFORGE_URL openbabel/openbabel/$VERSION/openbabel-$VERSION.tar.gz
    source unstable $SOURCEFORGE_URL openbabel/openbabel/$VERSION/openbabel-$VERSION.tar.gz
      info last 20111006
      info home http://openbabel.sourceforge.net/wiki/Main_Page
      desc 'chemistry toolbox'

build(){
 case $VERSION in
  2.2.3) rm -f CMakeLists.txt ;;
 esac
 default_build
}

# let me know if anyone requires a spell that supports the below.

# has optional python bindings that can be installed with:
# cd scripts/python
# python setup.py build
# python setup.py install

# has optional perl bindings that can be installed with:
# cd scripts/perl
# perl Makefile.PL
# make
# make install

# Might not work if libraries are moved from /usr/lib to /usr/lib64
# not detected by kdeedu-v4 when installed.
# so how to make this work?

build_old(){
 default_build &&
 sed -i 's:-L${libdir}::' $DESTDIR/usr/lib/pkgconfig/*.pc
}

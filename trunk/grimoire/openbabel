  stable 2.2.0 2.1.1
    IGNORE='test\|112\|b\|api'
  CATEGORY='utility/chemistry'
 ATTRIBUTE='library'
    SOURCE="openbabel/openbabel-$VERSION.tar.gz"
  HOMEPAGE='http://openbabel.sourceforge.net/wiki/Main_Page'
       URL="$SOURCEFORGE_URL"
       REQ='libxml2'
      OPTS='--enable-static=no'
case $VERSION in
 2.1.1) USE_GCC=gcc-v4.2 ;;
esac 
 ESTIMATE='3500'
      DESC='openbabel is a chemistry toolbox'

# let me know if anyone requires a spell that supports the below.

# has optional python bindings that can be installed with:
# cd scripts/python
# python setup.py build
# python setup.py install

# has optional perl bindings that can be installed with:
# cd scripts/perl
# perl Makefile.PL
# make
# make install

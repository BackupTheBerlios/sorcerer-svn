    stable 151
   require SDL

    ignore '1337'
  category development/emulator game
 attribute x11 new
    source zsnes/zsnes${VERSION}src.tar.bz2
       url "$SOURCEFORGE_URL"
  homepage http://www.zsnes.com/
      opts --enable-release
  estimate 200
      desc 'zsnes is super nintendo emulator.'

build(){
cd src
default_build
}

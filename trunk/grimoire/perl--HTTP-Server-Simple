     PNAME=${SPELL#perl--}
   VERSION=( "0.20" )
  CATEGORY="perl"
 ATTRIBUTE="perl-module new"
    SOURCE="J/JE/JESSE/$PNAME-$VERSION.tar.gz"
       URL="http://www.cpan.org/authors/id/"
  HOMEPAGE="http://search.cpan.org/search?dist=$PNAME"
       REQ=( "perl" )
  ESTIMATE="25"
      DESC="$SPELL is a very simple standalone HTTP daemon
with no non-core module dependencies. It's ideal for
building a standalone http-based UI to existing tools.
HTTP::Server::Simple
HTTP::Server::Simple::CGI
HTTP::Server::Simple::CGI::Environment"


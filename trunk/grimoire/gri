# 2.12.22 failed due to documentation

    stable 2.12.22 2.12.21 2.12.20
#  require teTeX texinfo ImageMagick ghostscript
   require texinfo ImageMagick ghostscript texlive
  category graphic science
 attribute new console
 attribute broke
    source $SOURCEFORGE_URL gri/gri/$VERSION/gri-$VERSION.tar.gz
  homepage http://gri.sourceforge.net
  estimate 1500
      desc 'plotting language for scientific graphics programming
The word language is important: Gri is command-driven, not point/click.
Some users consider Gri similar to LaTeX, since both 
provide extensive power as a reward for tolerating a 
learning curve.
Gri can make x-y graphs, contour graphs, and image 
graphs, in PostScript and (someday) SVG formats. Control 
is provided over all aspects of drawing, e.g. line widths, 
colors, and fonts.
A TeX-like syntax provides common mathematical symbols.'

build(){
 LISPDIR=/usr/lib/xemacs/xemacs-packages/lisp

 ./configure          \
 --prefix=/usr        \
 --sysconfdir=/etc    \
 --localstatedir=/var \
 --enable-frepo	      \
 $OPTS &&
 make  &&
 make check &&
 make install DESTDIR=$DESTDIR &&
 if [ -d $LISPDIR ]; then
  mkdir -p $DESTDIR/$LISPDIR
 /usr/bin/install -m 644 src/gri-mode.el $DESTDIR/$LISPDIR
 fi
}

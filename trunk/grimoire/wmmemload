    stable 0.1.6 0.1.5
   require automake libXpm

  category dockapps
 attribute x11 new
       url http://markstaggs.net/wmmemload-download/
    source wmmemload-$VERSION.tar.gz
  homepage http://markstaggs.net/wmmemload/
  estimate 200
      desc 'wmmemload is a dockapp monitors memory/swap usage
wmmemload is a simple dockapp for WindowMaker on
X Windows that displays memory and swap space usage.
It is very heavily based on WMMemMon
and WMCPULoad by Seiichi SATO.
99% of the code is take from his applications.
However, it is modified to provide numbers
instead of a graphical display for memory statistics.'

build(){
aclocal &&
./configure \
--prefix=/usr \
$OPTS &&
make  &&
prepare_install &&
make    install PREFIX=/usr
}

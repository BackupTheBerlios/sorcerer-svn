# 2.0.85 is devel
    stable 2.0.79 1.34.15
   require libsepol
  category security
 attribute console solo
    source http://userspace.selinuxproject.org/releases/20090403/devel/libselinux-$VERSION.tar.gz
  homepage http://userspace.selinuxproject.org/trac
      vurl http://userspace.selinuxproject.org/trac/wiki/Releases
   protect /selinux
  estimate 100
      desc 'library and simple utilities for use with selinux
While libselinux will be installed on all boxes
libselinux has no effect upon security and program execution when
selinux is not enabled in the kernel.
With linux-modular the kernel is selinux enabled.
selinux can be disabled at boot by using the linux kernel parameter of:
selinux=0'

build(){
 make "CFLAGS=$CFLAGS" &&
 prepare_install &&
 make    install &&
 mkdir -pm 500 /selinux
}

# for the moment using a patched sysvinit to mount /selinux
# and load the policy during boot.
#current(){
# case $VERSION in
#  1.34.15) [ -f /etc/init.d/selinuxfs ] ;;
# esac
#}

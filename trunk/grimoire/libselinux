  stable 1.34.15 1.34.13 1.34.7 1.34.0 1.32 1.30
  CATEGORY='security'
 ATTRIBUTE='console solo'
    SOURCE="libselinux-$VERSION.tgz"
  HOMEPAGE='http://www.nsa.gov/selinux/'
       URL='http://www.nsa.gov/selinux/archives/'
      VURL='http://www.nsa.gov/selinux/code/download-stable.cfm'
       REQ='libsepol'
   PROTECT='/selinux'
  ESTIMATE='120'
      DESC='libselinux provides a library and simple utilities for use with selinux.
While libselinux will be installed on all boxes
libselinux has no effect upon security and program execution when
selinux is not enabled in the kernel.
With linux-modular the kernel is selinux enabled.
selinux can be disabled at boot by using the linux kernel parameter of:
selinux=0'

build(){
 make "CFLAGS=$CFLAGS" &&
 prepare_install &&
 make    install &&
 mkdir -pm 500 /selinux
}

# for the moment using a patched sysvinit to mount /selinux
# and load the policy during boot.
#current(){
# case $VERSION in
#  1.34.15) [ -f /etc/init.d/selinuxfs ] ;;
# esac
#}

   version stable 4.8.0 4.6.2 4.6.1 4.6.0
   require libXpm libwnck libxfce4ui xfce4-panel
  optional xfwm4-themes '' '' for additional themes for the window manger
  category window-manager
 attribute makej x11
     wmbin /usr/bin/xfwm4
    source http://archive.xfce.org/xfce/4.8/src/xfwm4-$VERSION.tar.bz2
      desc 'xfce4 window manager'

build(){
 add_pkg_config_path(){
  if   [ -n "$PKG_CONFIG_PATH" ]
  then export PKG_CONFIG_PATH="/opt/libwnck-v2/$1/pkgconfig:$PKG_CONFIG_PATH"
  else export PKG_CONFIG_PATH="/opt/libwnck-v2/$1/pkgconfig"
  fi
  echo "PKG_CONFIG_PATH=$PKG_CONFIG_PATH"
 }

 wnck_fix(){
  if   [[ $HOSTTYPE == x86_64 ]] && ! [[ $CFLAGS =~ -m32 ]]
  then add_pkg_config_path lib64
  else add_pkg_config_path lib
  fi
 }

 case $VERSION in
  4.8.0) wnck_fix ;;
 esac

 default_build
}

    stable 3110 3100 393
  category graphic
 attribute library
       url $SOURCEFORGE_URL
    source freeimage/FreeImage$VERSION.zip
  homepage http://freeimage.sourceforge.net
  estimate 1000
      desc 'FreeImage provides an image loading codecs library'

build(){
make &&
make            -f Makefile.fip &&
prepare_install &&
make    install &&
make    install -f Makefile.fip
}


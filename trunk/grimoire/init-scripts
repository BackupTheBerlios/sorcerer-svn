# Copyright Kyle Sallee 2000 through 2011.
# All rights reserved.
# For use with the distribution sorcerer only!

# findutils added as requirement for /etc/init.d/fstab
# find also used by /etc/init.d/depmod

   version stable 20120106
   require cgroup coreutils findutils grep init-functions sed util-linux vm
  category administration/boot profile
 attribute archive_off console profile
      info last 20120106
      info home http://sorcerer.silverice.org
      desc 'several important scripts used during boot and shutdown'

build(){
 mkdir -pvm 755             $DESTDIR/bin
 for REPLY in            $SCRIPT_DIR/bin/*
 do  install -vm 755 $REPLY $DESTDIR/bin/
 done

 mkdir -pvm 755             $DESTDIR/sbin
 for REPLY in            $SCRIPT_DIR/sbin/*
 do  install -vm 755 $REPLY $DESTDIR/sbin/
 done

 mkdir -pvm 644             $DESTDIR/etc
 for REPLY in            $SCRIPT_DIR/etc/*
 do  install -vm 755 $REPLY $DESTDIR/etc/
 done
}

post_install(){
 eliminate_init_aliens(){ rm -f /etc/init.d/{move,move_dev,lock}; }
 eliminate_init_aliens		#     20110607
 default_post_install
}

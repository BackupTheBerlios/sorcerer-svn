#!/bin/dash

### BEGIN INIT INFO
# Provides: lock
# Required-Start: run
# Default-Start: S 1 2 3 4 5
# Short-Description: creates symbolic links /var/{lock,run}
### END INIT INFO

. /lib/lsb/init-functions

if ! [ -d /run/lock ]; then mkdir -p /run/lock; fi
if ! [ -d /var      ]; then mkdir    /var; fi
if ! [ -h /var/lock ]; then rm -r /var/lock; ln -s /run/lock /var/lock; fi
if ! [ -h /var/run  ]; then rm -r /var/run;  ln -s /run      /var/run; fi

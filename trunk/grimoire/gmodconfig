   VERSION=( "0.6" "0.5" "0.4" "0.3" "0.2" "0.1" )
  CATEGORY="kernel"
 ATTRIBUTE="broken gnome x11"
# Probably requires rpm and is of no use in sorcerer.
    SOURCE="gmodconfig/gmodconfig-$VERSION"
       URL="$SOURCEFORGE_URL"
  HOMEPAGE="http://gmodconfig.sourceforge.net"
       REQ="libgnomeui"
 FRESHMEAT="gmodconfig"
  ESTIMATE="251"
     SHORT="gmodconfig provies a GUI for configuring linux modules."
stream << EOF
gmodconfig provides a simple way for end-users 
to download, install, configure, and update 
Linux kernel modules through an easy-to-use GUI.
The backend consists of XML files that 
contain module information and translations.
EOF

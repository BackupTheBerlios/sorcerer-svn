# This spell will require more work and maybe init-scripts
# This has a problem building man-pages and docs
# probably installed doc book is not exactly correct.
# replaces DeviceKit-power
# 0.9.5 has problem with it's introspection file, too old
# 0.9.10 fails to compile

   version stable 0.9.16 0.9.15 0.9.14
  optional libmobiledevice  '' '--without-idevice' 'for Apple USB protocols'
   require polkit udev
      opts --disable-gtk-doc --disable-gtk-doc-html --disable-gtk-doc-pdf --disable-man-pages --enable-static=no
  category disk
 attribute client library makej multilib-concurrent server
    source http://upower.freedesktop.org/releases/upower-$VERSION.tar.xz
      info last 20120430
      info home http://upower.freedesktop.org/
 conflicts DeviceKit-power
      desc 'abstraction for enumerating power devices,
listening to device events and querying history and statistics.
Any application or service on the system can access the
org.freedesktop.UPower service via the system message bus.
Some operations (such as suspending the system) are restricted using polkit.'

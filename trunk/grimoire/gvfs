   VERSION=( 0.2.3 0.2.2 0.2.1 0.2.0.1 0.2.0 )
  CATEGORY='development'
 ATTRIBUTE='gnome library'
    SOURCE="sources/gvfs/${VERSION:0:3}/gvfs-$VERSION.tar.bz2"
  HOMEPAGE='http://www.gnome.org'
       URL="$GNOME_URL"
       REQ=( 'GConf libarchive libsoup'
             'avahi::--disable-avahi:for zeroconf support'
             'fuse::--disable-fuse:for filesystems in userspace support'
             'gnome-keyring::--disable-keyring:for gnome keyring support'
             'hal::--disable-hal:for hardware abstraction layer support'
             'libgphoto2::--disable-gphoto2:for gphoto2 digitial camera support'
             'obex-data-server::--disable-obexftp:for ObexFTP backend'
             'samba::--disable-samba:for samba windoze support' )
#     OPTS='--enable-cdda'
# --disable-cdda           build without CDDA backend
# libcdio provides cdda support
# Oddly passing --enable-cdda caused all the other optionals
# to default from on to default to off
# or something like that.

  ESTIMATE='1000'
      DESC='gvfs provides a virtual filesystem library designed to work with gio
From the gvfs/README

gvfs is a userspace virtual filesystem designed to work
with the i/o abstractions of gio
(a library availible in glib >= 2.15.1).
It installs several modules that are automatically
used by applications using the APIs of libgio.
There is also fuse support that allows
applications not using gio to access the gvfs filesystems.'

inherit crossfire
# May be unusable?  Segfaults when attempting online play?
    VERSION=( "1.7.0" "1.6.1" "1.6.0" )
    SOURCE="crossfire/crossfire-client-$VERSION.tar.gz"
       REQ="gtk+-v1.2 libpng"
    OPT[0]="crossfire-client-images:::for cached images for smoother Internet game play."
    OPT[1]="crossfire-client-sounds:::for sound effects"
    OPT[2]="crossfire-doc:::for documentation if you did not install the server"
    OPT[3]="SDL_image::--disable-sdl:for SDL support"
      OPTS="$OPTS --with-sound-dir=/usr/share/crossfire-client/sounds"
  ESTIMATE="68"
  EPROVIDE="crossfire,client"
      DESC="crossfire-client provides the client for a multi-player graphical MUD."


build()  {
  SD="/usr/share/crossfire-client/sounds"
  sed  -i  "s:/usr/local/lib/sounds:$SD:"  sound-src/sounds.dist
  OPTS="$OPTS --with-sound-dir=$SD --disable-alsa --disable-alsa9"
  default_build
}

   version stable 1.1
   version unstable 1.1.0.80
   require groff gzip
  category administration/log
 attribute server
    source  ftp://ftp.planix.com/pub/local/newsyslog-$VERSION.tar.gz
  homepage http://www.planix.com/~woods/projects/newsyslog.html
      desc 'rotates and archives log files
newsyslog is an updated version of a package put
together by Theodore Tso of MIT Project Athena
(which is included in NetBSD, FreeBSD, OpenBSD, etc.).
It manages the rotation and archiving of log files
(primarily those written to by syslogd).'

build(){
 install_conf_scripts(){
  mkdir -p $DESTDIR/etc
  sed 's:dialer:ppp:' newsyslog.conf > $DESTDIR/etc/newsyslog.conf
 }

sed -i 's:psman htmlman:psman:' Makefile.in

./configure          \
--prefix=/usr        \
--sysconfdir=/etc    \
--localstatedir=/var \
--bindir=/usr/sbin   \
--with-gzip          \
$OPTS                &&
make SHELL=/bin/bash &&
make install DESTDIR=$DESTDIR &&
install_conf_scripts
}

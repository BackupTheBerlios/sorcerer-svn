    stable v19
  optional alsa-lib '' --without-alsa for alsa support
  optional jackit   '' --without-jack for jack audio mixer support
  category audio/library
 attribute library
    source http://www.portaudio.com/archives/pa_snapshot_$VERSION.tar.gz
  homepage http://www.portaudio.com/
      opts --enable-cxx --prefix=/opt/portaudio
  estimate 600
      desc 'pa_snapshot_v19 is the developmental version of port audio.'

build(){
sed -i 's:-o lib/:-lpthread -o lib/:' Makefile.in
default_build &&
local LP='/lib/pkgconfig' &&
mkdir -p                       /usr/$LP &&
cp    -a  /opt/portaudio/$LP/* /usr/$LP
}

   VERSION=( "1.0.2" "1.0.1" "1.0.0" "0.9.6" "0.9.5" "0.9.4" )
  CATEGORY="audio"
 ATTRIBUTE="library"
    SOURCE="libcddb/libcddb-$VERSION.tar.gz"
       URL="$SOURCEFORGE_URL"
       REQ=( "libcdio" )
#   "libcdio:::for directly controlling the CDROM drive"
  HOMEPAGE="http://libcddb.sourceforge.net"
  ESTIMATE="85"
      DESC="libcddb is a library to access data on a CDDB server.
libcddb is a library that implements the different protocols
(CDDBP, HTTP, SMTP) to access data on a CDDB server
(e.g. http://freedb.org).
It tries to be as cross-platform as possible."

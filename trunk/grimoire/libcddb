   VERSION=( "0.9.5" "0.9.4" )
  CATEGORY="audio"
 ATTRIBUTE="library"
    SOURCE="libcddb/libcddb-$VERSION.tar.gz"
       URL="$SOURCEFORGE_URL"
       REQ="libcdio"
#   OPT[0]="libcdio:::for directly controlling the CDROM drive"
  HOMEPAGE="http://libcddb.sourceforge.net"
  ESTIMATE="77"
     SHORT="libcddb is a library to access data on a CDDB server."
      LONG="
libcddb is a library that implements the different protocols 
(CDDBP, HTTP, SMTP) to access data on a CDDB server 
(e.g. http://freedb.org).
It tries to be as cross-platform as possible.
"

   inherit linux-custom
 attribute broke deprecated
      desc 'the linux spell now provides pre-configured modular kernels
while linux-custom provides a kernel that the SA configures'

current(){ echo linux | pipe_queue "$CAST_QUEUE" com; }

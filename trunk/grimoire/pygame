   version stable 1.9.1release 1.8.1release 1.8.0release 1.7.1release 1.6.2 1.6 1.5.6
   require Python SDL_image SDL_mixer SDL_ttf libX11
# optional smpeg '' -smpeg to play mpeg movies and mp3 audio
  category graphic
 attribute library python-module x11
    source http://pygame.org/ftp/ ftp://pygame.org/pub/pygame/ pygame-$VERSION.tar.gz
      info home http://www.pygame.org/
      desc 'SDL wrapper for Python programs

pygame is a set of Python extension modules designed for writing games. 
The core of pygame is wrapped on top of the SDL library.'

build(){
chmod 700 setup.py &&
yes | ./setup.py install "--root=$DESTDIR"
}

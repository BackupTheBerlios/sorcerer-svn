   VERSION=( "1.7.1release" "1.6.2" "1.6" "1.5.6" )
  CATEGORY="graphic"
 ATTRIBUTE="library python-module x11"
    SOURCE="pygame-$VERSION.tar.gz"
  HOMEPAGE="http://www.pygame.org"
       URL="http://www.pygame.org/ftp"
       REQ=( "Python SDL_image SDL_mixer SDL_ttf"
             "smpeg::-smpeg:to play mpeg movies and mp3 audio" )
  ESTIMATE="5"
      DESC="pygame makes SDL accessible to Python programs.

pygame is a set of Python extension modules designed for writing games. 
The core of pygame is wrapped on top of the SDL library."


build() {
prepare_install  &&
yes |  ./setup.py
}

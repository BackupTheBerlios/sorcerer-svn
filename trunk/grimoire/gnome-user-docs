# Requires libxml2 compiled and installed after Python
# so that libxml2 gains bindings for Python

with version   stable 3.6.2 3.6.1 3.6.0
with version unstable 3.7.90
with base    gnome-doc-utils itstool
#with base    gnome-doc-utils itstool rarian
with role    documentation
with trait   gnome data
with source    stable $GNOME_URL sources/gnome-user-docs/${VERSION:0:3}/gnome-user-docs-$VERSION.tar.xz
with source  unstable $GNOME_URL sources/gnome-user-docs/${VERSION:0:3}/gnome-user-docs-$VERSION.tar.xz
with info    last 20121117
with info    home http://www.gnome.org
with hold    gnome2-user-docs
with info    cite 'documentation for GNOME
gnome-user-docs contains documents which will be packaged together 
and shipped as gnome-user-docs in the core GNOME distribution.
They should be documents targetted for end-users of GNOME.'

# Running commands in
# <kernel> /domain/freedom
# <kernel> /media/root/domain/freedom
# permitted from /usr/sbin/cast

if [ -f /proc/$PPID/cmdline ]; then
 read < /proc/$PPID/cmdline
 if   [ "$REPLY" == /usr/sbin/cast ]
 then PROG="$1"; shift 1; exec "$PROG" "$@"
 fi
fi

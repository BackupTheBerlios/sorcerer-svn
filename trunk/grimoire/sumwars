# 0-5-2 and 0-5-1 have compile error
# 0-5-2 has odd broke configure script?
# from config.log:
# configure:2858: $PKG_CONFIG --exists --print-errors "OGRE >= 1.4.0 OIS CEGUI CEGUI-OGRE enet freealut openal vorbisfile"
# Package enet was not found in the pkg-config search path.
# Perhaps you should add the directory containing `enet.pc'
# Bad snytax for pkgconfig check?

   version stable 0-5-2 0-5-1
   require CEGUI Raknet enet ogre ois
  category game
 attribute x11
 attribute broke
    source http://www.sumwars.org/data/sumwars_${VERSION}_src.tgz
      info home  http://www.sumwars.org
      desc 'multiplayer diablo style CRPG'

build(){
./configure \
 --sysconfdir=/etc \
 --localstatedir=/var \
 --prefix=/usr \
$OPTS &&
make  &&
make install DESTDIR=$DESTDIR
}

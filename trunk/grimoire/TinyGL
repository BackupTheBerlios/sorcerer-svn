   VERSION=( "0.4" )
  CATEGORY="graphic"
 ATTRIBUTE="library x11 broken"
# zbuffer.c: In function 'ZB_copyFrameBufferRGB24':
# zbuffer.c:268: error: invalid lvalue in assignment
# Source not up to date with gcc.
# made no attempt to fix it.
# Probably very simple to fix.
    SOURCE="TinyGL-0.4.tar.gz"
       URL="http://fabrice.bellard.free.fr/TinyGL"
  HOMEPAGE="http://fabrice.bellard.free.fr/TinyGL"
       REQ="x11"
  ESTIMATE="740"
      DESC="TinyGL provides a fast software implementation of a subset of OpenGL."

build() {
  make             &&
  prepare_install  &&
  make    install
}

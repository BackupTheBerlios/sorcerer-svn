    stable 1.95.8
  unstable 2.0.1-src
  category development
 attribute library
    source $SOURCEFORGE_URL expat/expat/$VERSION/expat-$VERSION.tar.gz
  homepage http://expat.sourceforge.net
      desc 'libexpat provides only a library file.
ati_firegl requires an old libexpat library.
This spell installs it.'

build(){
sed -i "s:\$(INSTALL_DATA):true:" Makefile.in
./configure          \
--prefix=/usr        \
--sysconfdir=/etc    \
--localstatedir=/var \
$OPTS                &&
make                 &&
prepare_install      &&
make    installlib
}

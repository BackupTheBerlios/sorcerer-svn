# selinux-doc is no longer available for download?
   version stable 1.26 1.24 1.22 1.20
  category security
 attribute console
 attribute broke
#   source http://www.nsa.gov/selinux/archives/selinux-doc-$VERSION.tgz
      vurl http://www.nsa.gov/selinux/code/download-stable.cfm
  homepage http://www.nsa.gov/selinux/
      desc 'documentation for SELinux'

build(){
mkdir -p    $DESTDIR/usr/share/SELinux
cp    -vr * $DESTDIR/usr/share/SELinux
}

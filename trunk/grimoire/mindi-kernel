    stable 2.6
  category administration/bootloader
 attribute console noheal
    source ftp://ftp.mondorescue.org/src/mindi-kernel-$VERSION.tgz
  homepage http://www.mondorescue.org/
  estimate 100
      desc 'a precompiled linux kernel'

build(){
sed -i 's:local=/usr/local:local=/usr:' install.sh
prepare_install &&
./install.sh
}

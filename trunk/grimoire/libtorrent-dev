   VERSION=( "0.11.6" "0.11.5" "0.11.4" "0.11.2" "0.11.1" "0.11.0" )
  CATEGORY="network/file-share"
 ATTRIBUTE="library"
    SOURCE="libtorrent-$VERSION.tar.gz"
       URL="http://libtorrent.rakshasa.no/downloads/"
       REQ=( "" "openssl:::for encrypted secure connections" )
# I have no idea why rtorrent would require openssl,
# but it links with the library. Maybe for hash generation?
  HOMEPAGE="http://libtorrent.rakshasa.no/"
       REQ="libsigc++"
  ESTIMATE="640"
 FRESHMEAT="none"
 CONFLICTS="libtorrent rtorrent"
      DESC="libtorrent-dev is a high performance BitTorrent library."

current()  { echo "libtorrent" | pipe_queue "$CAST_QUEUE"; }

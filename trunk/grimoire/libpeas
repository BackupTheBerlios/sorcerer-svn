# checking for PEAS_GTK... configure: error: Package requirements (gtk+-3.0 >= 2.90) were not met:
# No version 0.4.x
# Requirement list currently not known

    stable 0.7.0 0.5.5
  category development
 attribute gnome new
 attribute broke
    source $GNOME_URL sources/libpeas/${VERSION:0:3}/libpeas-$VERSION.tar.bz2
  homepage http://www.gnome.org
  estimate 150
      desc 'gobject-based plugins engine
It is targetted at giving every application the chance
to assume its own extensibility
It is currently used by several Gnome applications like gedit and Totem.'

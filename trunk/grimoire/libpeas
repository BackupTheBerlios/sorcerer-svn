   version   stable 1.6.0 1.5.0 1.4.0
## --enable-introspection=no has no effect
#case $VERSION in
# 1.2.0)
#      opts --enable-introspection=no ;;
#esac
   require gjs gtk+-3.0 perl--Encode pygobject-3.0 seed vala
  category development
 attribute gnome
 attribute library makej multilib-concurrent
    source $GNOME_URL sources/libpeas/${VERSION:0:3}/libpeas-$VERSION.tar.xz
      info last 20120929
      info home http://www.gnome.org
      desc 'gobject-based plugins engine
It is targetted at giving every application the chance
to assume its own extensibility
It is currently used by several Gnome applications like gedit and Totem.'

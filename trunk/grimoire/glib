#optional gamin '' '' for file modification notice
# glib and gamin have recrusive requirements on each other,
# how unfortunate.

# glib and pkgconfig have recursive requirements upon each other

# 2.30.0 may cause too many compilation errors in software such as
# poppler and gamin

# Evert reports that glib 2.30.0 breaks compilation of:
# gamin; silgraphite; libglade; poppler.

   version stable 2.30.0 2.28.8 2.28.7 2.28.6 2.28.5
   require libffi pcre pkgconfig zlib
#  require libselinux
      opts --with-pcre=system --with-threads=posix
  category development
 attribute gnome library makej multilib-concurrent
    source $GNOME_URL sources/glib/${VERSION:0:4}/glib-$VERSION.tar.xz
      info last  20110928
      info home  http://www.gtk.org/
      info docs  http://www.gtk.org/documentation.html
      desc 'library of useful C routines for trees, hashes, and lists'

current(){
 case $VERSION in
  2.28.5) [ -f /etc/init.d/glib-compile-schemas ] ;;
 esac
}

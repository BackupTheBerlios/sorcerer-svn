   version stable 2.05 2.04 2.0
   require automake libtool libmodplug unrar xmms
  category audio/codec audio/library
 attribute library x11
 attribute broke
    source $SOURCEFORGE_URL modplug-xmms/modplug-xmms/$VERSION/modplugxmms-$VERSION.tar.gz
      info home http://modplug-xmms.sourceforge.net
case $VERSION in
 2.05) use_gcc gcc-v4.2 ;;
esac
      desc 'ModPlug Plugin for XMMS v2.0
modplugxmms enables xmms to play Amiga mod files.'


build(){
# case $VERSION in
#  2.05) sed -i '1i#include <cstring>' $( find . -name \*.cpp ) ;;
# esac

aclocal
automake -acf
autoconf
libtoolize --force
default_build
}

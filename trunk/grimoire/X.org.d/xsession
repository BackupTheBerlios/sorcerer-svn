#!/bin/bash

if    [  -e    /etc/wm/default  ]  &&
      [  -x    /etc/wm/default  ]
then  exec     /etc/wm/default  "$@";  else
  for  WM  in  /etc/wm/*;  do
    if   [  -e  $WM  ]  &&
         [  -x  $WM  ]
    then  exec  $WM  "$@"
    fi
  done
fi

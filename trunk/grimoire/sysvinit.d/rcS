#!/bin/bash

trap ":" INT QUIT TSTP
export PATH="/sbin:/bin:/usr/sbin:/usr/bin"
umask 022

for SCRIPT in /etc/rc.d/rcS.d/S*; do
 if   [ -f "$SCRIPT" ] &&
      [ -x "$SCRIPT" ]
 then       $SCRIPT start
 fi
done

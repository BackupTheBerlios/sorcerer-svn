    stable 20070115
   require gnutls iproute2
  category network/ipv6
 attribute client daemon new
    source http://www.sixxs.net/archive/sixxs/aiccu/unix/aiccu_$VERSION.tar.gz
  homepage http://www.sixxs.net/
  estimate 260
      desc 'aiccu provide ipv6 tunnel software for the sixxs tunel broker'

build(){
make &&
prepare_install &&
mkdir -pv       /usr/share/doc/aiccu &&
cp    -av doc/* /usr/share/doc/aiccu &&
cp unix-console/aiccu /usr/sbin &&
cp -av $SCRIPT_DIR/networks.d /etc/ &&
cp doc/aiccu.conf /etc &&
chmod +x /etc/networks.d/links/aiccu
}

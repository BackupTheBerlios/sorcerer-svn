LOC=/etc/login.defs
WHY=Authentication
DES="provides the shadow password suite configuration file
/etc/login.defs
defines the site-specific configuration for the shadow password suite."
MAN=login.defs

LOC=/etc/bash.d/noninteractive/LANG
WHY=Environment
DES="sets the default language and character encoding
For a greater selection of languages and encodings please execute:

# cast -r -c glibc-locales"

method(){ :; }

LOC=/etc/hosts
WHY=Network
DES="specifies IP addresses to name mapping for known hosts
When DNS is slow or unavailable other
boxes can be located by name when their
IP address and name are specified in /etc/hosts"
MAN=hosts

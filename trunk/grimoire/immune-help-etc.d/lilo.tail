LOC=/etc/lilo/tail
WHY=Boot
DES="provides additional boot image selection

/etc/lilo.conf
is the configuration file for
lilo, the Linux boot loader.
It is also a symbolic link to
/etc/lilo/conf

Information for booting other operating systems
should be placed in:
/etc/lilo/tail
/etc/lilo/tail
is appended to
/etc/lilo/conf
when
/etc/lilo/conf
is generated"

MAN=lilo.conf

method(){
 default_method
 chmod 0600 $LOC
}

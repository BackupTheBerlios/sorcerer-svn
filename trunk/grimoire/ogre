   version stable v1-7-3 v1-6-4
   require Cg-x86 FreeImage MesaLib boost freetype libXaw libXrandr zziplib
if [[ $HOSTTYPE == x86_64 ]]; then
   require Cg-x86_64
fi
      opts -DCMAKE_BUILD_TYPE:STRING=Release
#     opts -DOGRE_BUILD_PLUGIN_CG:BOOl=False
#     opts --disable-cg
# --disable-freeimage
# Not sure about parameter for CEGUI
# might not be an optional requirement with ogre version v1-7-3
# optional CEGUI
  category graphic
# lto requires too much RAM
 attribute library lto_off makej multilib-concurrent
    source $SOURCEFORGE_URL ogre/ogre/${VERSION:1:1}.${VERSION:3:1}/ogre_src_$VERSION.tar.bz2
      info last 20110408
      info home http://www.ogre3d.org/
    CFLAGS='-O2'
  CXXFLAGS='-O2'
   LDFLAGS='-Wl,-O1'
      desc 'ogre is the object oriented graphic rendering engine.
ogre is the (Object-Oriented Graphics Rendering Engine)
is a class library written in C++ designed to
make it easier and more intuitive for developers
to produce games and demos utilising 3D hardware.
It exposes a number of useful objects allowing you
to create and manipulate a 3D environment and render
it to your specifications, while removing the
unnecessary complexities of the underlying system
libraries like Direct3D and OpenGL.'


build(){
 if   [[ $HOSTTYPE == x86_64 ]] && ! [[ $CFLAGS =~ -m32 ]]
 then OPTS+=" -DLIB_SUFFIX=64"
 fi
 default_build
}

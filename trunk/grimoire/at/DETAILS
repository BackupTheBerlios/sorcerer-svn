   VERSION=( "3.1.8-11" )
  CATEGORY="time"
 ATTRIBUTE="console"
 SOURCE[0]="at_$VERSION"
# SOURCE[1]="at_$VERSION-11.diff.gz"
    URL[0]="ftp://ftp.debian.org/debian/pool/main/a/at"
#   URL[1]="ftp://security.debian.org/debian-security/dists/potato/updates/main/source"
  HOMEPAGE="unknown"
     SHORT="at including batch are periodic schedulers."
cat << EOF
at contains at and batch which are daemons that allow processes 
to be schedule to run at a later date or when system load permits.
EOF

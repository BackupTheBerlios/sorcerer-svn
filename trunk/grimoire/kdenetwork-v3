# qca-tls does not compile on x86_64

   inherit kde-v3
case $HOSTTYPE in
 x86_64) require gnupg kdelibs-v3 libXScrnSaver libXtst procps openslp ortp speex ;;
      *) require gnupg kdelibs-v3 libXScrnSaver libXtst procps openslp ortp qca-tls speex ;;
esac

  optional openssl        --with-ssl  --without-ssl  for SSL support
  optional samba          ''          ''             for nmblookup if you want lisa to discover windoze boxes
  optional wireless_tools --with-wifi --without-wifi for wireless networking support
  optional xmms           --with-xmms --without-xmms for kopete chat client to display what you are listening to with xmms
  category network/email network/news

#            'ortp:--enable-jingle:--disable-jingle:for Jabber Jingle voice support'
# Compiling with jingle enabled causes compile error during kopete

#            ':--enable-sametime-plugin:--disable-sametime-plugin:build the Kopete Sametime plugin' )
# sametime plugin no longer exists

# speex may only be required for jabber jingle support?  Not sure.
# kopete jabber jingle may have optional requirement on ilbc
# something in kdenetwork may have optional requirement on gsm

      desc 'Chat, email, news clients
kdenetwork contains network applications.
Currently contains the mail program, 
the news reader, and several other 
network-related programs like kopete.'

build(){
 default_build &&
 mkdir   -p                         $DESTDIR/etc &&
 install -vm 600 $SCRIPT_DIR/lisarc $DESTDIR/etc
}

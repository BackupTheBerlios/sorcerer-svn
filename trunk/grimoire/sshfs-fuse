with version stable 2.4 2.3 2.2
with base    glib fuse
with role    network/file-share
with trait   console client
with source  $SOURCEFORGE_URL fuse/sshfs-fuse/$VERSION/sshfs-fuse-$VERSION.tar.gz
with info    last 20120311
with info    home http://fuse.sourceforge.net/sshfs.html
with info    cite 'ssh-fuse is a filesystem that uses SFTP.
It is based on Filesystem in Userspace (FUSE), and hence root
privilege is not required for mounting a remote filesystem.
No setup is necessary and it is very easy to use.'

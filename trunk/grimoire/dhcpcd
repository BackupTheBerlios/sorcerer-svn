    stable 4.0.1 4.0.0 3.2.3 3.2.1 3.1.9 2.0.1 2.0.0
    secure 3.2.3
  unstable 4.0.1 4.0.0-rc5 4.0.0-rc4
  category network
 attribute console client
    source http://roy.marples.name/dhcpcd/dhcpcd-$VERSION.tar.bz2
  homepage http://roy.marples.name/dhcpcd
      opts --sbindir=/sbin
      desc 'dhcpcd is a DHCP client/daemon that works with @home
dhcpcd is an RFC2131 and RFC1541 compliant DHCP client daemon.
It gets an IP address and other information from the DHCP server,
automatically configures the network interface, and tries to
renew the lease time.'

build(){
make &&
prepare_install &&
make    install
}

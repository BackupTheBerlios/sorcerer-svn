    stable 5.0.3 5.0.1 4.0.13 4.0.12 4.0.11 4.0.10 4.0.8 4.0.7 4.0.6
    secure 3.2.3
  unstable 4.99.18 4.99.16 4.99.15 4.99.14
   require linux-headers
  category network
 attribute console client
    source http://roy.marples.name/downloads/dhcpcd/dhcpcd-$VERSION.tar.bz2
  homepage http://roy.marples.name/projects/dhcpcd
      opts --sbindir=/sbin
      desc 'dhcpcd is a DHCP client/daemon that works with @home
dhcpcd is an RFC2131 and RFC1541 compliant DHCP client daemon.
It gets an IP address and other information from the DHCP server,
automatically configures the network interface, and tries to
renew the lease time.'

build(){
make            LIBEXECDIR=/usr/libexec &&
prepare_install &&
make    install LIBEXECDIR=/usr/libexec
}

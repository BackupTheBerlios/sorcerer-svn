   version stable 5.6.0 5.5.6
   require linux-headers
      opts --sbindir=/sbin
  category network
 attribute console client
    source http://roy.marples.name/downloads/dhcpcd/dhcpcd-$VERSION.tar.bz2
      info last 20120709
      info home http://roy.marples.name/projects/dhcpcd
      desc 'DHCP client and daemon that works with @home
dhcpcd is an RFC2131 and RFC1541 compliant DHCP client daemon.
It gets an IP address and other information from the DHCP server,
automatically configures the network interface, and tries to
renew the lease time.'

build(){
 sed -i "/rpath=/d" Makefile
 ./configure --libexecdir=/usr/libexec &&
 make &&
 make install "DESTDIR=$DESTDIR"
}

#!/bin/bash
if   [ -f /media/root/sbin/agetty ] &&
     read < /proc/cmdline           &&
     ! [[ $REPLY =~ boot=IR ]]
then exec /bin/chroot /media/root /sbin/agetty "$@"
else exec /sbin/agetty "$@"
fi

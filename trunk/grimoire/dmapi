    stable 2.2.10 2.2.9
   require xfsprogs
  category disk/file-system
 attribute console
    source ftp://oss.sgi.com/projects/xfs/cmd_tars/dmapi-$VERSION.tar.gz
  homepage http://oss.sgi.com/projects/xfs
  estimate 200
      desc 'Data Management API for XFS Filesystems'

# xfsprogs does not compile in uClibc
case $MACHTYPE in
 *-uclibc) attribute broke ;;
esac

build(){
export OPTIMIZER="$CFLAGS"
unset              CFLAGS
export DEBUG=-DNDEBUG
 export DIST_ROOT="$DESTDIR"

#make configure &&
make &&
make install install-dev
}

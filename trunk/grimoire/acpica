# acpiexec did not compile in version 20090123
# impossible to track?

    stable 20090521 20090123
  category utility
 attribute console new
    source http://www.acpica.org/download/acpica-unix-$VERSION.tar.gz
  homepage http://www.acpica.org/
  estimate 200
      desc 'Advanced Configuration and Power Supply Interface Specification interface by Intel'

build(){
 make_install(){ install -vm 755 compiler/iasl tools/acpisrc/acpisrc /usr/bin/; }

 cd compiler         && make &&
 cd ../tools/acpisrc && make &&
#cd ../acpiexec      && make &&
 cd ../..            && prepare_install && make_install
}

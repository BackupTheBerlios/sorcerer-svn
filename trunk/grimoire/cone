# 0.81 segfaults during tests during comilation; not zlib related
# 0.82 segfaults during tests
# 0.83 segfaults during tests
#   stable 0.83 0.82 0.81 0.80 0.79 0.78 0.77 0.75 0.74 0.73 0.72 0.71 0.70
    stable 0.80 0.79 0.78 0.77 0.75 0.74 0.73 0.72 0.71 0.70
   require gamin libxml2 openssl ncurses
  optional aspell   '' '' for spell checking
  optional openldap '' '' for ldap access
  category network/email
 attribute console
#attribute broke
    source $SOURCEFORGE_URL courier/cone/$VERSION/cone-$VERSION.tar.bz2
  homepage http://www.courier-mta.org/cone/index.html
   provide email-client
  estimate 5000
      desc 'text mail client with POP3, IMAP accounts and local mail folders
Cone seamlessly handles multiple POP3,
IMAP accounts, and local mail folders.
Cone is also a simple newsreader.
Cone is designed to be foolproof enough
to be used by inexperienced users,
but also offers advanced features for power users.'

build(){
default_build &&
make install-configure "DESTDIR=$DESTDIR"
}

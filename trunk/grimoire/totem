# tracker is an optional requirement for totem

# libpeas absolutely requires gobject-introspection.
# gobject-introspection is too buggy and breaks several compile.
# libpeas is broke while gobject-introspection is broke
# totem is broke while libpeas is broke

# 3.2.1 3.2.0 3.0.1 3.0.0 stalls during compilation during configure
# No version of totem is expected to compile

   version   stable 3.4.3 3.4.2 3.4.1
   require bluez clutter-gst clutter-gtk gst-ffmpeg gst-plugins-bad-v0.10 libarchive libXtst libepc libgdata libpeas mx totem-pl-parser
  optional lirc
  optional nautilus   --enable-nautilus --disable-nautilus for nautilus plugin
      opts --enable-static=no
  category video/dvd video/player
 attribute gnome makej
    source $GNOME_URL sources/totem/${VERSION:0:3}/totem-$VERSION.tar.xz
      info last 20120705
      info home http://www.gnome.org/projects/totem/
# This spell uses xine-lib instead of gstreamer for playing movies.
      desc 'movie player for gnome based on xine'

build(){
# export LDFLAGS+=' -L/usr/lib/mozilla -Wl,-rpath,/usr/lib/mozilla'
# export PKG_CONFIG_PATH='/usr/share/pkgconfig'

 sed -i 's:hardcode_into_libs=yes:hardcode_into_libs=no:' configure
 default_build
}

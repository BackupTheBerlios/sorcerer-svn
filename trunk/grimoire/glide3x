# If you want to use it try to fix it.

    stable 3_10_00_30303-src
  category xorg/driver
 attribute X11R7 broke
    source $SOURCEFORGE_URL glide/Glide3x%20[sources]/3.10.00.30303/glide3x-$VERSION.tgz
  homepage http://glide.sourceforge.net/
      opts --disable-fx-debug
  estimate 50
      desc 'Library for glide 3D graphics cards.'

build(){
aclocal
automake
autoconf
mkdir build &&
cd    build &&
../configure --prefix=/usr $OPTS &&
make -f makefile.autoconf all &&
prepare_install &&
make    install
}

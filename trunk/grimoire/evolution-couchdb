# broke because perl--XML-LibXML is broke
# which breaks couchdb-glib

# broke because couchdb-glib is broke
# requirement list unknown
# no successful compilation

   version stable 0.5.3 0.5.2 0.5.1 0.5.0
   require couchdb-glib
  category development
 attribute gnome new
 attribute broke
    source $GNOME_URL sources/evolution-couchdb/${VERSION:0:3}/evolution-couchdb-$VERSION.tar.bz2
      info last  20110403
  homepage http://www.gnome.org
      desc 'source tarball lacks description'

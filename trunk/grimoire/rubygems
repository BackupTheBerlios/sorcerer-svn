# Update source when updating stable version.
# Having this separate ruby only package manager install software
# on a box that is not tracked by sorcery
# could be a problem.
# Therefore, I marked this broke
# although it compiles and installs.

    stable 1.3.6 1.3.5
   require ruby
  category development/interpreter/ruby
 attribute library
 attribute broke
    source http://rubyforge.org/frs/download.php/60718/rubygems-1.3.5.tgz
      vurl http://rubyforge.org/frs/?group_id=126
  homepage http://docs.rubygems.org/
      desc 'package system for ruby modules'

build(){
[ -f setup.rb ] &&
ruby setup.rb --destdir=$DESTDIR
}

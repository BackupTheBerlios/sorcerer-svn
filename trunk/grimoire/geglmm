# Broke while gegl is broke
# Have yet to install and test this.

    stable 0.0.22 0.0.17.1
   require gegl
  category development
 attribute gnome library
 attribute broke
       url $GNOME_URL
    source sources/geglmm/${VERSION:0:3}/geglmm-$VERSION.tar.bz2
  homepage http://www.gnome.org
      opts --enable-static=no
  estimate 7000
      desc 'geglmm'

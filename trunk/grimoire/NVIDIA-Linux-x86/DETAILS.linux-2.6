unset  VERSION  FRESHMEAT
   VERSION=( "1.0-4496" )
    IGNORE="unified"
  CATEGORY="kernel"
 ATTRIBUTE="concurrent x11 x86"
 SOURCE[0]="NVIDIA-Linux-x86-${VERSION}-pkg2.run"
 SOURCE[1]="NVIDIA_kernel-${VERSION}-2.6.diff.bz2"
       DIR="NVIDIA-Linux-x86-${VERSION}"
    URL[0]="http://download.nvidia.com/XFree86/Linux-x86/$VERSION"
    URL[1]="http://www.minion.de/files"
  HOMEPAGE="http://www.minion.de"
       REQ="mktemp"
  ESTIMATE="12"
     SHORT="NVIDIA-Linux-x86,linux-2.6 provides drivers for use with linux 2.6.x kernel."
cat << EOF
$SHORT

From http://www.minion.de

The 4GB/4GB split VM layout patch has been integrated 
and enabled in all three configurations, however, 
which breaks the NVIDIA driver, as well.  
You will need to rebuild without CONFIG_X86_4G_VM_LAYOUT
EOF

  VERSION=( "4.0" )
 CATEGORY="graphics science"
ATTRIBUTE="kde new"
      URL="$SOURCEFORGE_URL"
   SOURCE="digitizer/digit-src-${VERSION//./_}.tar.gz"
 HOMEPAGE="http://digitizer.sourceforge.net"
      REQ="qt-x11-free fftw"
 ESTIMATE="105"
FRESHMEAT="digitizer"
     DESC="$SPELL converts an image showing a graph or map into numbers
Engauge Digitizer is a digitizing software that converts
an image file showing a graph or map, into numbers.
The image can come from a scanner, digital camera or 
screenshot. The numbers can be read on the screen, 
and written or copied to a spreadsheet.
"

build() {
  export QTDIR="/usr"

  sed -i "s:helpPath1(\".*\");:helpPath1(\"/usr/doc/${SPELL}/doc/usermanual\");:"  src/digitmain.cpp

  qmake -unix digitizer.pro		&&
  make					&&
  mv  samples  usermanual  doc		&&
  prepare_install			&&
  install -v -m 0755 -o root -g root  bin/engauge  /usr/bin	
}

   version stable 3.7.10 3.7.9 3.7.8 3.7.7 3.7.6
   require claws-mail curl libnotify
  optional libgtkhtml gtkhtml2_viewer '' to render HTML mail using gtkhtml2 rendering widget
  optional perl       perl            '' to extend filtering possibilities using perl interface
    switch acpi_notifer  '' for mail notification via LEDs on some laptops
    switch att_remover   '' for message attachment remover
    switch cachesaver    '' to avoid metadata loss on crashes by saving cache every minute
    switch etpan-privacy '' for signature verification and decryption of messages
    switch fetchinfo     '' to insert headers containing some download information
    switch maildir       '' to provide support for maildir++ mailboxes
    switch mailmbox      '' to provide support for mailboxes in mbox format
    switch newmail       '' to write message header summary to a log file
    switch notification  '' to provide methods for notifying user of new unread mail
    switch rssyl         '' to read RSS 1.0 2.0 and Atom newsfeeds in claws
    switch smime         '' to verify signatures and decrypt email
    switch synce         '' to keep addressbook of windows CE device in sync with claws
    switch vcalendar     '' for vCalendar message handling similar to Evolution or Outlook
      opts --enable-static=no
  category network/email network/news
 attribute client x11
    source $SOURCEFORGE_URL sylpheed-claws/extra%20plugins/$VERSION/claws-mail-extra-plugins-$VERSION.tar.bz2
      info last  20110831
  homepage http://www.claws-mail.org/
      desc 'extra plugins for sylpheed claws

acpi_notifer	for mail notification via LEDs on some laptops
att_remover	for message attachment remover
cachesaver	to avoid metadata loss on crashes by saving cache every minute
etpan-privacy	for signature verification and decryption of messages
fetchinfo	to insert headers containing some download information
gtkhtml2_viewer	to render HTML mail using gtkhtml2 rendering widget
maildir		to provide support for maildir++ mailboxes
mailmbox	to provide support for mailboxes in mbox format
newmail		to write message header summary to a log file
notification	to provide methods for notifying user of new unread mail
perl		to extend filtering possibilities using perl interface
rssyl		to read RSS 1.0 2.0 and Atom newsfeeds in claws
smime		to verify signatures and decrypt email
synce		to keep addressbook of windows CE device in sync with claws
vcalendar	for vCalendar message handling similar to Evolution or Outlook'


build(){
if [ -f RELEASE_NOTES ]; then
 mkdir -p $DESTDIR
 for OPT in $OPTS; do
  cd $BUILD_DIR
  cd $OPT*
  [ -f configure ]     &&
  ./configure          \
  --prefix=/usr        \
  --sysconfdir=/etc    \
  --localstatedir=/var &&
  make all install DESTDIR=$DESTDIR    
 done
 true
 else false
 fi
}

   version stable 0.92
   require perl
  category network/dns
 attribute console client
    source $SOURCEFORGE_URL hostupd/hostupd/hostupd-$VERSION/hostupd-$VERSION.tar.gz
      info home  http://hostupd.sourceforge.net/
      desc 'command line tool which allows the user to update a DNS server
hostupd is a commandline tool for updating a DNS server
which supports dynamic updates (RFC 2136), such as bind >= 8.
Its main goal is to simplify the DNS-updating task,
but to remain flexible.'

build(){ make install PREFIX=$DESTDIR/usr; }

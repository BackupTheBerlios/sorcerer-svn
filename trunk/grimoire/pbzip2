# Current spell can not be used as a replacement for bzip2
# because pbzip2 uses bzip2's ELF library and
# so does many other installed software projects.
    stable 1.1.1 1.1.0 1.0.5 1.0.4 1.0.3 1.0.2 1.0.1 1.0
   require bzip2 gcc-g++
  category archive
 attribute console
    source http://compression.ca/pbzip2/pbzip2-$VERSION.tar.gz
  homepage http://compression.ca/pbzip2/
      desc 'parallel implementation of the bzip2
pbzip2 uses pthreads and achieves near-linear speedup on SMP machines.
The output of this version is fully compatible with bzip2 1.0.2
(ie: anything compressed with PBZIP2 can be decompressed with bzip2)'

build(){
sed -i "s:-O3:$CFLAGS $LDFLAGS:" Makefile
make &&
prepare_install &&
install -vm 755 -o root -g root pbzip2 /usr/bin
}

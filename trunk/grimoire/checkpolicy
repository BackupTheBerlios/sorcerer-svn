    stable 2.0.19 1.34.7
   require bison flex libselinux
  category security
 attribute console
    source http://userspace.selinuxproject.org/releases/20090403/devel/checkpolicy-$VERSION.tar.gz
  homepage http://userspace.selinuxproject.org/trac
      vurl http://userspace.selinuxproject.org/trac/wiki/Releases
  estimate 30
      desc 'checkpolicy contains the policy compiler needed to build policies.'

build(){
make "CFLAGS=$CFLAGS" &&
prepare_install &&
make    install
}

   VERSION=( 2.5.0 2.4.3 2.4.2 2.4.1 2.4.0 2.3.0 2.2.2 2.2.1 )
    IGNORE='2004'
  CATEGORY='network/web network/ftp'
 ATTRIBUTE='server'
    SOURCE="ziproxy/ziproxy-$VERSION.tar.bz2"
       URL="$SOURCEFORGE_URL"
  HOMEPAGE='http://ziproxy.sourceforge.net/'
       REQ=( 'confuse giflib init-functions jpeg libpng'
             'jasper:--with-jasper::for recoding some images in JPEG-2000 format' )
  ESTIMATE='300'
      DESC='ziproxy is a non-caching proxy for expediting web browsing.
ziproxy will gzip text and HTML files.
It will convert images to lower quality smaller JEPGs.
It is confgurable and features transparent proxy mode,
preemeptive name resolution, and operation in daemon or xinetd mode.

Incase you are wondering firefox 2.0.0.1 does not suport JPEG-2000.
Therefore, do not enable jasper as an optional requirement
when using ziproxy with firefox.'

build(){
 install_conf(){
   sed     -i 's:/var/zip:/usr/share/zip:' etc/ziproxy.conf
   install -vm 0644 etc/ziproxy.conf      /etc/ziproxy.conf
   mkdir   -vm 0755 -p                    /usr/share/ziproxy/error
   install -vm 0644 var/ziproxy/error/*   /usr/share/ziproxy/error
 }

 sed -i 's:-Wl,-dn,::;s:,-dy::' configure
 default_build &&
 install_conf
}

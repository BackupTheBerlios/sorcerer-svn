    stable 1.34.15
   require refpolicy selinux-doc setools

  category profile
 attribute console profile
  homepage http://www.nsa.gov/selinux/
# polgen
# polgen has compile error
# slat'
# not slat because slat requires ocaml
  estimate 5
      desc 'selinux provides Security Enhanced linux from the NSA

The linux kernel already includes support for selinux
It can be activated during boot with the parameter:
selinux=1
or deactivated with
selinux=0'

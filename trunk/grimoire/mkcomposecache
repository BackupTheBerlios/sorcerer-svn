   VERSION=( "1.1" "1.0" )
  IVERSION=( "1.1" "1.0" )
inherit xorg
    SOURCE="app/mkcomposecache-$VERSION.tar.bz2"
       REQ="libX11"
  ESTIMATE="70"
      DESC="mkcomposecache is used for creating global (system-wide) Compose cache files.

Compose cache files help with application startup times and memory 
usage, especially in locales with large Compose tables
(e.g. all UTF-8 locales)."

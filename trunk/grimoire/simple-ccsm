   inherit compiz
   require ccsm
    source $VERSION/simple-ccsm-$VERSION.tar.bz2
  estimate 150
      desc 'simple-ccsm is a Python/GTK based settings manager for the CompizConfig system.'

build(){
 prepare_install &&
 python setup.py install --prefix=/usr
}

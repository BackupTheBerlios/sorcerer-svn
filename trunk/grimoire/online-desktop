# No package 'nss' found
# No package 'nspr' found
# Obviously, it requires mozilla
# Obviously, it can not find it.
# broke source tarball?

    stable 0.2.29 0.2.28
  category development
 attribute gnome library broke
    source sources/online-desktop/${VERSION:0:3}/online-desktop-$VERSION.tar.bz2
  homepage http://www.gnome.org
       url "$GNOME_URL"
      opts --enable-static=no
  estimate 1000
      desc 'online-desktop provides web apps as local desktop apps.'

build(){
 export LDFLAGS=' -Wl,-rpath,/usr/lib/mozilla -Wl,-rpath-link,/usr/lib/mozilla'
 default_build
}

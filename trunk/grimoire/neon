#  VERSION=( "0.24.7" "0.24.6" )
   VERSION=( "0.25.4" "0.25.3" "0.25.2" "0.25.1" "0.25.0" "0.24.7"  "0.24.6" )
# Switching to neon 0.24.x because both gnome-vfs 2.12.0
# and subversions 1.2.3 do not support the new and not backward
# compatible API of the neon 0.25.x series
  CATEGORY="web"
 ATTRIBUTE="library"
    SOURCE="neon-$VERSION.tar.gz"
  HOMEPAGE="http://www.webdav.org/neon"
       URL="http://www.webdav.org/neon"
       REQ=( "expat libxml2"
             "heimdal::--without-gssapi:for keberos 5 support"
             "openssl:--with-ssl:--without-ssl:for SSL/TLS support" )
#     OPTS="--enable-shared --disable-static"
      OPTS="--enable-shared --disable-static --without-gssapi"
# --without-gssapi added because neon 0.25.3 and 0.24.7
# are not compatible with heimdal 0.7.1
  ESTIMATE="51"
      DESC="neon is an HTTP and WebDAV client library, with a C interface.
neon is an HTTP and WebDAV client library,
with a C interface with features such as
High-level interface to HTTP and WebDAV methods,
low-level interface to HTTP request handling,
to allow implementing new-methods easily."

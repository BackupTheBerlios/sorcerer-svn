# if static library disabled compilation of subversions may fail
#     opts --enable-shared --disable-static

    stable 0.28.5 0.28.4 0.28.3 0.28.2 0.28.1 0.28.0
   require expat libxml2
  optional heimdal ''         --without-gssapi for kerberos 5 support
  optional openssl --with-ssl --without-ssl    for SSL/TLS support
  category network/web
 attribute library
    source http://www.webdav.org/neon/neon-$VERSION.tar.gz
  homepage http://www.webdav.org/neon/
      opts --enable-shared --enable-threadsafe-ssl=posix --disable-static
  estimate 300
      desc 'HTTP and WebDAV client library, with a C interface
neon is an HTTP and WebDAV client library,
with a C interface with features such as
High-level interface to HTTP and WebDAV methods,
low-level interface to HTTP request handling,
to allow implementing new-methods easily.'

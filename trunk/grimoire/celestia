   VERSION=( 1.5.1 1.5.0 )
    IGNORE='unix-\|10[1-7]\|win'
  CATEGORY='game'
 ATTRIBUTE='x11'
    SOURCE="celestia/celestia-$VERSION.tar.gz"
       URL="$SOURCEFORGE_URL"
      OPTS='--with-gtk'
  HOMEPAGE='http://www.shatters.net/celestia'
       REQ='gtkglext libtheora'
case $VERSION in
 1.5.[0-9]) USE_GCC=gcc-v4.2 ;;
esac
  ESTIMATE='1400'
      DESC='celestia is an OpenGL-based 3d space simulator.
celestia is an OpenGL-based 3D space simulation that lets you travel
through the solar system, to the stars, and even beyond the galaxy.
Visit over 100,000 stars, 100 solar system bodies, and all known
extrasolar planets.'

# build(){
#  case $VERSION in
#   1.5.0) sed -i '1i#include <cstring>
#                  1i#include <cstdlib>
#                  1i#include <climits>' $( find . -name \*.cpp ) ;;
#  esac
#  default_build
# }


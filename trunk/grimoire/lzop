   VERSION=( "1.01" "1.00" )
    IGNORE="101"
  CATEGORY="archive"
 ATTRIBUTE="console"
       URL="http://www.oberhumer.com/opensource/lzop/download"
  HOMEPAGE="http://www.oberhumer.com/opensource/lzop"
       REQ="lzo"
  ESTIMATE="343"
      DESC="lzop is a real-time file compressor.

lzop is a file compressor similar to gzip.
Its main advantages over gzip are much 
higher compression and decompression 
speed at the cost of some compression ratio.
lzop uses the LZO library for compression
services.
"


build() {
./configure  --prefix=/usr  &&
make                        &&
prepare_install             &&
make    install
}

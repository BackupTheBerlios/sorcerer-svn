    stable v7.6.112 v7.6.86 v7.6.24
   require zlib
  optional SDL '' '' for SDL x11 output support
  optional SDL_mixer '' '' for SDL audio output support
  optional fftw --with-fftw --without-fftw for fourier transformation
  optional freetype --with-freetype --without-freetype for freetype font support
  optional gdbm '' --without-gdbm for gdbm support
  optional giflib '' --without-gif for uncompressed gif graphic image support
  optional glut '' --without-GLUT for OpenGL support
  optional gtk+ '' --without-GTK for x11 output using the gtk+ library
  optional gtkglarea '' '' for gtk+ output using OpenGL
  optional gmp '' --without-gmp for bignums support
  optional gnome-libs '' '' for gnome 1.4 support
  optional jpeg '' --without-jpeglib for jpeg graphic image support
  optional lame '' '' for mp3 encoding support
  optional librsvg '' --without-svg for svg support
  optional mysql '' --without-mysql for mysql support
  optional nasm '' --without-nasm for nasm support
  optional nettle --with-nettle --without-nettle for encryption support
  optional openssl '' '' for encryption support
  optional pcre '' --without-pcre for perl regular expression support
  optional postgresql '' --without-postgres for postgresql support
  optional sane-backends '' --without-sane for scanner support
  optional tiff '' --without-tifflib for tiff graphic image support
  optional unixODBC '' '' for windows database access 

  category development/interpreter
 attribute console
    source Pike-$VERSION.tar.gz
# The webserver tries to hide the true download location for Pike
       url http://pike.ida.liu.se/pub/pike/latest-stable/
#      url http://pike.ida.liu.se/download/pub/pike/latest-stable/
#      url http://pike.ida.liu.se/download/pub/pike/latest-stable/
#      url http://pike.ida.liu.se/pub/pike/latest-stable/
  homepage http://pike.ida.liu.se
#   'linuxtv-dvb:--with-dvb:--without-dvb:for hardware decoding of mpeg2'
#   'gtkgladexml '' '' not sure where to find this'
# --with(out)-sybase               Include the Sybase database driver
# --with(out)-libpdf       Support PDF through PDFlib
# --without-libpanda ? What is libpanda?
# --without-krb5 ? which implementation of kerberos?
#            'libungif '' --without-gif:for uncompressed gif graphic image support'

      opts --without-ffmpeg
#optional ffmpeg '' --without-ffmpeg:for software decoding of mpeg2'
# Pike v7.6.86 does not work with ffmpeg 2006-5 from svn
 freshmeat pike
  estimate 3900
      desc 'Pike is a general purpose programming language.
Its application domain spans anything from the world of the
Net to the world of multimedia applications, or environments
where your shell could use some spicy text processing or
system administration tools.'

build(){
cd src &&
./configure   \
--prefix=/usr \
$OPTS &&
make  &&
prepare_install &&
make    install
#make   install_nodoc
}

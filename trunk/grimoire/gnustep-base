# using older versions because newer versions do not compile
# requires gcc with objc support
# can require either libffi or ffcall
# However, it seems that it prefers or only works with ffcall
# but ffcall is no longer maintained nor available for download

   version stable 1.18.0 1.20.0
   require binutils gnustep-make gnutls openssl libffi libxslt
      opts --with-layout=fhs
# below does not fix the problem
#     opts --disable-invocations
#     opts --with-ffi-include=/usr/include --with-ffi-library=/usr/lib
  category development
 attribute console
    source http://ftpmain.gnustep.org/pub/gnustep/core/gnustep-base-$VERSION.tar.gz
      info home http://www.gnustep.org
      desc 'foundation for GNUstep'

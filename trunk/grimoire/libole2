    stable 2.2.8 0.2.4
   require glib
  category documentation
 attribute library
    source $GNOME_URL sources/libole2/${VERSION:0:3}/libole2-$VERSION.tar.bz2
  homepage www.gnome.org
  estimate 70
      desc 'API to access OLE2 streams

libole2 provides an API to access OLE2 streams 
as used by Microsoft in their compound files.
Such common file formats as Excel, Word,
Powerpoint, and Visio use this system.'


build(){
default_build &&
mkdir -p $DESTDIR/usr/include &&
ln -sf libole2-2.0/libole2 $DESTDIR/usr/include/libole2
}

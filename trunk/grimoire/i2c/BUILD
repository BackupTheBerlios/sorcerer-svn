LID="LINUX_INCLUDE_DIR=/usr/include/i2c/linux"
  P="PREFIX=/usr"
  M="MANDIR=/usr/man"

make             "$LID" "$P" "$M"  all  &&
prepare_install                         &&
make    install  "$LID" "$P" "$M"

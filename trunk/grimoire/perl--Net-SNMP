   version stable v6.0.1
   require perl--Digest-SHA1 perl--Crypt-DES perl--Digest-HMAC
  category perl network
 attribute perl-module
    source http://www.cpan.org/authors/id/D/DT/DTOWN/Net-SNMP-$VERSION.tar.gz
      info good 20120629
      info last 20100909
      info home http://search.cpan.org/search?dist=Net-SNMP
      desc 'perl--Net-SNMP is a perl module for Simple Network Management Protocol.
The module Net::SNMP implements an object oriented
interface to the Simple Network Management Protocol.
Perl applications can use the module to retrieve or
update information on a remote host using the SNMP protocol.
Net::SNMP is implemented completely in Perl,
requires no compiling, and uses only standard Perl modules.
Both SNMPv1 and SNMPv2c (Community-Based SNMPv2)
are supported by the module.
The Net::SNMP module assumes that the user has a basic
understanding of the Simple Network Management Protocol
and related network management concepts.'

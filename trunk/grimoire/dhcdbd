# Do not further develop this spell because dhcdbd
# will no longer be a requirement of NetworkManager
# as of 0.7.x
    stable 3.0
  category network
 attribute library
    source dhcdbd-$VERSION.tar.bz2
       url http://dcantrel.fedorapeople.org/dhcdbd/
  homepage http://dcantrel.fedorapeople.org/dhcdbd/
# dhcdbd requirement departs with NetworkManager 0.7.x
  estimate 10
      desc 'dhcdbd provides dbus control for ISC dhclient software
From README:
    dhcdbd exists to:
        o  provide D-BUS control of the ISC dhclient software,
        o  store DHCP configuration parameters (options) persistently
        o  providing access to DHCP options over D-BUS
        o  notify applications of changes to DHCP IP interface configuration'

build(){
 cd src
 make &&
 prepare_install &&
 install -vm 1755 dhcdbd /usr/sbin/ &&
 for file in ../include/*
 do install -vm 0644 $file /usr/include/
 done
}

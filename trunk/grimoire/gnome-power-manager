# uncertain if xmlto is actually used.

   version   stable 3.4.0
   version unstable 3.5.90
## The case statement did not fix the problem
# case $VERSION in
#  3.0.0)
#       opts --enable-docbook-docs ;;
# esac
      opts --enable-compile-warnings=yes --disable-docbook-docs
   require GConf libcanberra libgnome-keyring libnotify upower
#  require upower gnome-panel gstreamer libcanberra libnotify libunique
  optional docbook2X '' --enable-docbook-docs  for generating manual pages and also requires Internet connection during compilation
# optional xmlto
  category utility
 attribute daemon gnome
    source   stable $GNOME_URL sources/gnome-power-manager/${VERSION:0:3}/gnome-power-manager-$VERSION.tar.xz
    source unstable $GNOME_URL sources/gnome-power-manager/${VERSION:0:3}/gnome-power-manager-$VERSION.tar.xz
      info last 20120326
      info home http://www.gnome.org/projects/gnome-power-manager/
      desc 'responds to HAL events with user-configurable reactions
Currently it supports UPSs, notebook batteries and AC adapters.'

build(){
 case $VERSION in
  3.*)        sed -i '/	man	/d' Makefile.in ;;
  2.30.*)     sed -i 's:docs man data:docs data:' Makefile.in ;;
  2.2[6-8].*) sed -i 's:docs man data:docs data:' Makefile.in ;;
 esac
 default_build
}

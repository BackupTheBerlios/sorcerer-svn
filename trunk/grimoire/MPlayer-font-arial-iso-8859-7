    stable 1.0
  category video/dvd video/player
 attribute data
    source $MPLAYER_URL releases/fonts/${SPELL:8}.tar.bz2
  eprovide MPlayer-font
      desc "default font for MPlayer's GUI"

build(){
 FONT="$DESTDIR/usr/share/mplayer/fonts"
 chmod a+r *
 mkdir -p    $FONT
 cp    -Rv * $FONT
}

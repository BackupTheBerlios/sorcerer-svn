with version stable 0.8.3 0.8.2 0.8.1
with base    libXpm
  sys_user games
with role    game
with trait   x11
with source  http://marc.mongenet.ch/OSS/XGalaga/xgalaga++_$VERSION.tar.gz
with info    last 20120423
with info    home http://marc.mongenet.ch/OSS/XGalaga/
with info    cite 'classic single screen verticle shoot em up inspired by xgalaga'

build(){
 mkdir -p $DESTDIR/usr/share/man/man6 \
          $DESTDIR/usr/bin \
          $DESTDIR/var/games &&
 make     all &&
 make install \
      installprefix=$DESTDIR/usr \
      bindir=$DESTDIR/usr/bin \
      HIGH_SCORES_FILE=$DESTDIR/var/games/xgalaga++.scores
}

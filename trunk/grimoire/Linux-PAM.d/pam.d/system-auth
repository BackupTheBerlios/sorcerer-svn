#
# Common configuration for system-auth services
#

#auth	  required	pam_env.so
auth	  sufficient	pam_unix.so nullok
auth	  required	pam_deny.so

account	  required	pam_unix.so
account	  sufficient	pam_succeed_if.so uid < 1000 quiet
account	  required	pam_permit.so

# With cracklib 
#password  requisite	pam_cracklib.so retry=3 difok=1
#password  sufficient	pam_unix.so shadow md5 nullok use_authtok

# Without cracklib
password  sufficient	pam_unix.so shadow md5 nullok

# With or without cracklib
password  required	pam_deny.so

session	  required	pam_limits.so
session	  required	pam_unix.so

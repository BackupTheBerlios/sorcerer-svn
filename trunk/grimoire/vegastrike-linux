# This provides both pre-compiled binaries and datafiles for vegastrike.
# However, it requires a build() to be installed properly.
# source does not support DESTDIR style install

    stable 0.5.0
  category game/3d/flightsim
 attribute x11 broke
#   source $SOURCEFORGE_URL vegastrike/vegastrike/$VERSION/vegastrike-linux-$VERSION.tar.bz2
  homepage http://vegastrike.sourceforge.net/
      desc 'data files for vegastrike'

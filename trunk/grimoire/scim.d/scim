if ! (( UID == 0 )); then
 export XMODIFIERS=@im=SCIM
 export GTK_IM_MODULE=scim
 export QT_IM_MODULE=scim
 export XIM_PROGRAM="scim -d"
 if ! [ -S /tmp/scim-socket-frontend-$USER ]
 then scim -d
 fi
fi

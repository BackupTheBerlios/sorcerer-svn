# Oddly, installs header files and no libraries.

   version stable 0.11.8.3.2 0.11.8.3.1 0.11.8.3
   require ncurses
      opts --enable-shared
  category network/chat
 attribute console
    source http://naim.googlecode.com/files/naim-$VERSION.tar.bz2
      info last 20090303
      info home http://naim.n.ml.org/
      desc 'console AIM, IRC, ICQ, and Lily client
naim is the original ncurses (console) AIM client,
which also supports IRC and Lily CMC.
For AIM, it uses the TOC protocol, and includes many
commonly-requested features found nowhere else,
while still preserving naim classic look and feel.'

build(){
 if [[ $HOSTTYPE == x86_64 ]] && ! [[ $CFLAGS =~ -m32 ]]
 then mkdir -pvm 755 $DESTDIR/usr/lib64
 else mkdir -pvm 755 $DESTDIR/usr/lib
 fi
 default_build
}

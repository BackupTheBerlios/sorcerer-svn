    stable 1.39-source 1.38-source 1.37-source
   require portaudio
  category audio/speech
 attribute console
       url $SOURCEFORGE_URL
    source espeak/espeak-$VERSION.zip
  homepage http://espeak.sourceforge.net/
  estimate 200
 freshmeat espeak
      desc 'speak also called espeak is a software speech synthesizer for English.'

build(){
 manual_install(){
  install -m 755 speak /usr/bin
  cd ..
  find -type f | xargs --max-lines=64 chmod 644
  find -type d | xargs --max-lines=64 chmod 755
  cp   -av espeak-data /usr/share
 }

 cd src
 make            &&
 prepare_install &&
  manual_install
}

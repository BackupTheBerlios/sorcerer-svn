# Compile log shows the error occurs because
# /usr/lib/libstdc++.so is listed explicitly.
# This problem is caused by libtool,
# because libtool sucks.

   VERSION=( 0.1.14 )
  CATEGORY='development'
 ATTRIBUTE='broken console'
    SOURCE="gift/gift-$VERSION.tar.gz"
  HOMEPAGE='http://www.gnu.org/software/gift'
       URL="$GNU_URL"
       REQ='e2fsprogs perl--Text-Iconv perl--XML-XQL'
 case $VERSION in
  0.1.14) USE_GCC='gcc-v4.0' ;;
 esac
  ESTIMATE='1840'
      DESC="gift is the GNU image finding tool.
gift uses the Content Based Image Retrieval System.
It enables queries by example on images."

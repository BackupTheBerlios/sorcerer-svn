    stable 6.0-0
   require aspell
       url $GNU_URL
    source aspell/dict/en/aspell6-en-$VERSION.tar.bz2
      desc 'dictionary for use with aspell'
build(){ ./configure && prepare_install && make install; }

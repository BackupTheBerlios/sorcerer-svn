    stable 1.6.1 1.6.0
   require ilmbase zlib
  category graphic
 attribute library
    source http://savannah.nongnu.org/download/openexr/openexr-$VERSION.tar.gz
  homepage http://www.openexr.com
      opts --enable-static=no
  estimate 600
      desc 'openexr is a high dynamic range (HDR) image file format.'

build(){
case $VERSION in
 1.6.1) sed -i '1i#include <cstring>' */main.cpp ;;
esac
default_build
}

# Whether this downloads or fails to download
# probably depends on the time of day
# when th download is attempted
# since I do not know exactly when
# the new Linux-HOWTOs tarball is rolled each day.

   Y="$( date -u +%Y )"
   m="$( date -u +%m )"
   d="$( date -u +%d )"
#  (( d-- ))
   VERSION=( "$Y$m" )
# TLDP does not provide old daily tarballs for download.
# Therefore, there can be no sdelta3 update patches.
# What a waste of bandwidth.
  CATEGORY='documentation'
 ATTRIBUTE='archive_off data stale'
    SOURCE="Linux-HOWTOs-$Y$m$d.tar.bz2"
       URL='http://www.ibiblio.org/pub/Linux/docs/HOWTO/'
  HOMEPAGE='http://www.tldp.org'
      DESC='Linux-HOWTOs provides detailed how-to documents on specific subjects
Linux-HOWTOs and mini-HOWTOs are detailed
how-to documents on specific subjects.'


build() {
prepare_install   
mkdir  -p     /usr/doc/Linux-HOWTOs
cp     -a  *  /usr/doc/Linux-HOWTOs
}

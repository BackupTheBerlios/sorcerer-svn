# 2.0.69 is devel
# 2.0.62 is broken
   version stable 2.0.77 1.34.16
   require Linux-PAM libsemanage
  category security
 attribute console
    source http://userspace.selinuxproject.org/releases/20091123/devel/policycoreutils-$VERSION.tar.gz
  homepage http://userspace.selinuxproject.org/trac
      vurl http://userspace.selinuxproject.org/trac/wiki/Releases
      desc 'set of policy core utilities
It includes load_policy, setfiles, newrole, and run_init.'

build(){
make "CFLAGS=$CFLAGS" &&
make install DESTDIR=$DESTDIR
}

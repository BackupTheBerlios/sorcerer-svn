   VERSION=( "1.0.2" "1.0.1" )
  CATEGORY="file-system"
 ATTRIBUTE="console"
    SOURCE="ms-sys/ms-sys-$VERSION"
       URL="$SOURCEFORGE_URL"
  HOMEPAGE="http://ms-sys.sourceforge.net"
 FRESHMEAT="ms-sys"
     SHORT="ms-sys installs Microsoft compatibile boot records"
cat << EOF
ms-sys is a Linux program for writing Microsoft compatible 
boot records.  The program does the same as Microsoft's 
"fdisk /mbr" to a hard disk or "sys d:" to a floppy or 
FAT32 partition, except that it does not copy any system 
files (only the boot record is written).
EOF

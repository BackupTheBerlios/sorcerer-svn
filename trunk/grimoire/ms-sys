   VERSION=( "2.1.2" "2.1.1" "2.1.0" )
  CATEGORY="disk"
 ATTRIBUTE="console"
    SOURCE="ms-sys/ms-sys-$VERSION.tgz"
       URL="$SOURCEFORGE_URL"
  HOMEPAGE="http://ms-sys.sourceforge.net"
  ESTIMATE="31"
      DESC="ms-sys installs Microsoft compatibile boot records.
ms-sys does the same as Microsoft's fdisk /mbr
to a hard disk or sys d: to a floppy or FAT32 partition,
except that it does not copy any system files
(only the boot record is written)."

build() {
  sed  -i  "s:/usr/local:/usr:
            s:EXTRA_CFLAGS =:EXTRA_CFLAGS = $CFLAGS:
            s?EXTRA_LDFLAGS =?EXTRA_LDFLAGS = $LDFLAGS?"  Makefile

  make             &&
  prepare_install  &&
  make    install
}

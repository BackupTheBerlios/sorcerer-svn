   version   stable 0.35beta2 0.35beta1 0.34b 0.34
   require gtk+
#  require xmame
  category game development/emulator
 attribute x11
    source $SOURCEFORGE_URL gxmame/gxmame/$VERSION/gxmame-$VERSION.tar.gz
      info good 20120609
      info last 20050411
      info home http://gxmame.sourceforge.net/
      info docs http://gxmame.sourceforge.net/docs.php
      desc 'GUI gtk+ frontend for mame
gxmame is a frontend for XMame using the GTK library,
the goal is to provide the same GUI than mame32.
For the moment it will just have the same gui,
the final goal is to be able to share config files with Mame32k'

build(){
 case $VERSION in
  0.3*) sed -i 's:glib/gprintf\.h:glib.h:
                s:glib/gutils\.h:glib.h:' src/gxmame.c ;;
 esac
 default_build
}

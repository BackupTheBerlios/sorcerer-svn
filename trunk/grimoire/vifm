# There is something very wrong with this.
# It shows libtool invocation starting X!

   VERSION=( 0.4 0.3a 0.3 0.2a 0.2 0.1 )
  CATEGORY='utility/file-manager'
 ATTRIBUTE='console broken'
    SOURCE="vifm/vifm-$VERSION.tar.bz2"
       URL="$SOURCEFORGE_URL"
  HOMEPAGE='http://vifm.sourceforge.net'
       REQ='automake ncurses'
  ESTIMATE='100'
      DESC='vifm is an ncurses based file manager with vi-like keybindings.'

build(){
sed -i 's:/usr/local:/usr:g' src/config.c
aclocal
automake -afc
default_build
}

# requires glib to ensure glib's init-scripts also are installed.
   inherit glib
   require glib
      opts --enable-static --disable-shared
      desc 'provides glib with both ELF and AR libraries'

build(){
   CFLAGS+=' -fPIC'
 CXXFLAGS+=' -fPIC'
 default_build
}

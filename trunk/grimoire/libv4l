   version stable 0.6.1 0.6.0 0.5.9
   version unstable 0.6.2-test
  category graphic/library
 attribute library
    source http://people.atrpms.net/~hdegoede/libv4l-$VERSION.tar.gz 
      info last 20090901
      info home http://hansdegoede.livejournal.com/3636.html
      desc 'libraries that add an API ontop of video4linux2'

build(){
 local LIBDIR
 if   [[ $HOSTTYPE == x86_64 ]] && ! [[ $CFLAGS =~ -m32 ]]
 then LIBDIR=/usr/lib64
 else LIBDIR=/lib
 fi

 make LIBDIR=$LIBDIR PREFIX=/usr &&
 make install LIBDIR=$LIBDIR PREFIX=/usr DESTDIR=$DESTDIR
}

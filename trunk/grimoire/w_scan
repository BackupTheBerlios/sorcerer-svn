    stable 20081106
   require linux-kernel glibc
  category video
 attribute console
    source http://wirbel.htpc-forum.de/w_scan/w_scan-${VERSION}.tar.bz2
  #REPOSITORY=( ""
  #                   "git clone --depth 1 git://anongit.freedesktop.org/git/hal hal"
  #                   "git pull" ) ;;
  homepage http://wirbel.htpc-forum.de/
  estimate 13
      desc 'w_scan is a dvb frequency channel scanner'

build(){
    make &&
    prepare_install &&
    cp w_scan /usr/bin
}


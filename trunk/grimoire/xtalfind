# The requirements on this is incomplete.
# It may require more spells written for perl modules.
# Please send the spells and fixes to the email list if you use xtalfind.
   VERSION=( 2.2 1.2 )
  CATEGORY='utility/ham'
 ATTRIBUTE='console'
    SOURCE="xtalfind-$VERSION.tar.gz"
       URL='http://spazioinwind.libero.it/ik1zyw/software/'
       REQ='perl'
  HOMEPAGE='http://freshmeat.net/projects/xtalfind'
      DESC='xtalfind is a crystal checking/math tool
xtalfind checks whether any XTAL is suitable for a HAM radio circuit (TX, RX).
Given a base XTAL frequency, it shows all possible frequencies within HAM bands.
It features CW TX, direct conversion RX, 455kHz, and 10.7MHz IF receivers.'

build(){
 [       -f     xtalfind.pl ] && prepare_install &&
 install -m 755 xtalfind.pl /usr/bin/
}

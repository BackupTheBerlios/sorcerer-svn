with version   stable 0.15.1 0.15
with version unstable 0.16a1
with base    bison libgcrypt
with elect   mhash --with-mhash ''             for mhash lib instead of built-in
with elect   zlib  --with-zlib  --without-zlib for compression support
with also    --disable-static
with role    security
with trait   console
with source    stable $SOURCEFORGE_URL aide/aide/$VERSION/aide-$VERSION.tar.gz
with source  unstable $SOURCEFORGE_URL aide/aide/$VERSION/aide-$VERSION.tar.gz
with info    good 20120601
with info    last 20100913
with info    home http://aide.sourceforge.net/
with info    cite '(Advanced Intrusion Detection Environment) is a file integrity checker
It creates a database from the regular expression
rules that it finds from the config file.
Once this database is initialized it can be
used to verify the integrity of the files.'

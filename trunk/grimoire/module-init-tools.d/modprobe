#!/bin/bash

if /sbin/modprobe "$@" &&
   [[ -n $PS1 ]] &&
   (( $# == 1 )) &&
   ! grep -qx "$1" /etc/modules
then
 read -t 10 -n 1 -p "Also load $1 during boot? [y|N]: "
 case $REPLY in
  y|Y) echo "$1" >> /etc/modules ;;
 esac
fi

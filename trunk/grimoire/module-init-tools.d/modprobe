#!/bin/bash

if /sbin/modprobe "$@" &&
   (( $# == 1 )) &&
   ! grep -qx "$1" /etc/modules
then
 read -n 1 -p "Also load $1 during boot? [y|N]: "
 case $REPLY in
  y|Y) echo "$1" >> /etc/modules ;;
 esac
fi


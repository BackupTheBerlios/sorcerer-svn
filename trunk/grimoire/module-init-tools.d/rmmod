#!/bin/bash

if /sbin/rmmod "$@" &&
   (( $# == 1 )) &&
   ! grep -qx "$1" /etc/modules.rm
then
 read -t 60 -n 1 -p "Also unload $1 during boot? [y|N]: "
 case $REPLY in
  y|Y) echo "$1" >> /etc/modules.rm ;;
 esac
fi


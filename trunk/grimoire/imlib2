   version stable 1.4.4 1.4.3
   require bzip2 freetype giflib libXext libid3tag libpng tiff
      opts --enable-static=no
  category graphic
 attribute library makej multilib-dual x11
    source $SOURCEFORGE_URL enlightenment/imlib2-src/$VERSION/imlib2-$VERSION.tar.gz
      info home  http://enlightenment.org/Libraries/Imlib2/
      desc 'successor to imlib graphics library
imlib2 is the successor to Imlib.
It is NOT a newer version -- it is a completely new library.
Imlib 2 can be installed with Imlib 1.x
without problems since they are different
libraries which have very similar functionality.
Please see the homepage for the
long description of the differences.'

build(){
 if    [[ $HOSTTYPE == x86_64 ]]; then
  if   [[ $CFLAGS   =~ -m32   ]]
  then opts --x-libraries=/usr/lib    --host=${MACHTYPE/x86_64/i686} 
  else opts --x-libraries=/usr/lib64
  fi
 else  opts --x-libraries=/usr/lib
 fi

# sed -i 's:$libdirs::;/-L@libdir@/d' imlib2-config.in
 ./configure --prefix=/usr --sysconfdir=/etc/imlib2 $OPTS &&
 make &&
 make install DESTDIR=$DESTDIR &&
 sed -i 's:-L/usr/lib64::;s:-L/usr/lib::' $DESTDIR/usr/bin/imlib2-config
}

    stable 0.3.2
   require gtk+

  category dictionary
 attribute x11 new
    source zdict-$VERSION.tar.gz
       url http://zwin.org/projects/zdict/
  homepage http://zwin.org/projects/zdict/
      desc 'zdict is a gtk+ dictionary viewer for the edict database.'

build(){
sed -i 's:/prog/zdict:/usr/bin:;s:/run::' Makefile
make &&
prepare_install &&
make    install
}

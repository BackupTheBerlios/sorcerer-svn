# broke because zvbi is broke

   version stable 0.9.6 0.9.5 0.9.4
   version unstable 0.10cvs6
   require libgnomeui rarian zvbi
  category video/television
 attribute gnome
 attribute broke
    source $SOURCEFORGE_URL zapping/zapping/$VERSION/zapping-$VERSION.tar.bz2
  homepage http://zapping.sourceforge.net
      desc 'tv viewer app
zapping is a TV viewer for GNOME that supports Video4Linux,
Video4Linux2, and XVideo.  It is extensible through plugins.'

build(){
sed -i 's:gconftool :gconftool-2 :' Makefile.in
default_build
}

#        1.0.8 1.0.7 have compile error with db 4.7.x
    stable 1.0.8 1.0.7
   require db-v4.6 rasqal
  optional mysql      '' '' for mysql support
  optional postgresql '' '' for postgresql support

  category development
 attribute library
    source redland-$VERSION.tar.gz
  homepage http://librdf.org/
       url http://download.librdf.org/source/
      opts --enable-static=no
      opts --with-bdb=/opt/db/4.6
  estimate 500
      desc 'redland provides the RDF API and triple stores.'

# It already has a -rpath,/usr/lib which cancels out the rpath below.
# it links with the wrong libdb, but maybe it will work anyway.
# build(){
#  export LDFLAGS+=' -Wl,-rpath,/opt/db/4.6/lib'
#  default_build
# }

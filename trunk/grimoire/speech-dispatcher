   version stable 0.7.1
  category network/chat
 attribute library multilib-concurrent
   require alsa-lib dotconf glib libsndfile
  optional libao
  optional nas
  optional pulseaudio
  optional speak
    source http://www.freebsoft.org/pub/projects/speechd/speech-dispatcher-$VERSION.tar.gz
      info good 20120709
      info last 20100910
      info home http://www.freebsoft.org/speechd
 conflicts speechd
      desc 'device independent layer for speech synthesis.
The goal of speech dispatcher is to make speech synthesis easier.'

build(){
 case $VERSION in
  0.7.1) aclocal     
         libtoolize -cf
         grep -lr "\-L/usr/X11R6/lib" * |
         xargs -r sed -i 's:-L/usr/X11R6/lib::' ;;
 esac
 default_build
}

with version stable 0.7.1
with role    network/chat
with trait   library multilib-concurrent
with base    alsa-lib dotconf glib libsndfile
with elect   libao
with elect   nas
with elect   pulseaudio
with elect   speak
with source  http://www.freebsoft.org/pub/projects/speechd/speech-dispatcher-$VERSION.tar.gz
with info    good 20120709
with info    last 20100910
with info    home http://www.freebsoft.org/speechd
with clash   speechd
with info    cite 'device independent layer for speech synthesis.
The goal of speech dispatcher is to make speech synthesis easier.'

build(){
 case $VERSION in
  0.7.1) aclocal     
         libtoolize -cf
         grep -lr "\-L/usr/X11R6/lib" * |
         xargs -r sed -i 's:-L/usr/X11R6/lib::' ;;
 esac
 default_build
}

# libogg-1.2.2.tar.xz 07-Dec-2010 10:46  275K 
    stable 1.2.2 1.2.1 1.2.0
      opts --enable-static=no
  category audio/codec audio/library
 attribute library
    source http://downloads.xiph.org/releases/ogg/libogg-$VERSION.tar.xz
      home http://www.xiph.org/ogg/
      docs http://www.xiph.org/doc/
  estimate 210
      desc 'bitstream and framing library for the Ogg project
It provides functions which are necessary to codec libraries like libvorbis.'

# case $VERSION in
#  1.2.2)
#    require automake libtool ;;
# esac

build(){
#  local  LANG
#  export LANG=C
#  case $VERSION in
#   1.2.2) aclocal; automake -acf; libtoolize -cf ;;
#  esac
sed -i 's:-O20 -ffast-math::' configure{,.in}
default_build
}

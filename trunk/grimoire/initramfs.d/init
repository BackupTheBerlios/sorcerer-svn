#!/bin/bash

/etc/init.d/rcS

if    [  -f     /media/root/sbin/init  ];  then
  cd            /media/root
  mount  --move /media/root /
  exec chroot . /sbin/init "$@"
else            /sbin/init "$@"
fi


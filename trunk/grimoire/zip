# No .tar.gz hosted at the URL?
# So what about version 30 it seems newer?

   version stable 30 232 231 23
   version secure 232
  optional bzip2
  category archive
 attribute console
    source ftp://ftp.info-zip.org/pub/infozip/src/zip$VERSION.tgz
#   source ftp://sunsite.icm.edu.pl/pub/unix/archiving/info-zip/src/zip${VERSION}.tar.gz
      info home http://www.info-zip.org/pub/infozip/Zip.html
      desc 'PKWARE compatible compressor
Info-ZIP provides free, portable,
high-quality versions of the Zip and UnZip
compressor-archiver utilities that are compatible
with the DOS-based PKZIP by PKWARE, Inc.'

build(){
make -f unix/Makefile generic &&
make -f unix/Makefile install prefix=$DESTDIR/usr INSTALL=install
}

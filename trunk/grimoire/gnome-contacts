# broke while vala is broke
with trait   broke
with info    edit 20140105
with info    last 20131023
with version   stable 3.10.1 3.10 3.8.3
with base    cheese folks
#with base     folks gnome-desktop
##with base    contacts folks gnome-desktop
with also    --enable-static=no --disable-rpath
with role    utility
# might sometimes not compile properly when using makej multilib-concurerent?
with trait   gnome library
#with trait   gnome library makej multilib-concurrent
with source    stable $GNOME_URL sources/gnome-contacts/${VERSION:0:4}/gnome-contacts-$VERSION.tar.xz
with source  unstable $GNOME_URL sources/gnome-contacts/${VERSION:0:4}/gnome-contacts-$VERSION.tar.xz
with info    home http://www.gnome.org
with clash   contacts
with info    cite 'the gnome contact center'

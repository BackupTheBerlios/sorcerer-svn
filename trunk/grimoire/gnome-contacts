# broke when folks is broke
# has unusual requirements such as cheese
# I wonder why.
# what does this actually require?

with version   stable 3.8.1 3.8.0 3.6.2
with version unstable 3.8.1
with info    last 20130417
with base    cheese folks
#with base     folks gnome-desktop
##with base    contacts folks gnome-desktop
with also    --enable-static=no
with role    utility
# might sometimes not compile properly when using makej multilib-concurerent?
with trait   gnome library
#with trait   gnome library makej multilib-concurrent
#with trait   broke
with source    stable $GNOME_URL sources/gnome-contacts/${VERSION:0:3}/gnome-contacts-$VERSION.tar.xz
with source  unstable $GNOME_URL sources/gnome-contacts/${VERSION:0:3}/gnome-contacts-$VERSION.tar.xz
with info    home http://www.gnome.org
with clash   contacts
with info    cite 'the gnome contact center'

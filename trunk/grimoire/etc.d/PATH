LOC=/etc/bash.d/noninteractive/PATH
WHY=Environment
DES="sets the location and order for searching directories for executables
The PATH file is used to set both the user and root PATH
The PATH variable must be exported
The PATH must be a colon ':' separated list of directories"

LOC=/etc/lilo.conf.tail
WHY=Boot
DES="provides additional boot image selection

Boxes may be configured to
boot other operating systems.
Information for booting other operating systems
should be placed in:
/etc/lilo.conf.tail
/etc/lilo.conf.tail
is automatically appended to
/etc/lilo.conf
when
/etc/lilo.conf
is generated"

MAN=lilo.conf

method(){
 default_method
 chmod 0600 $LOC
}


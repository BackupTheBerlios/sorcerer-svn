if  !  grep  -q  "DG_BOOT"  $SPELL_CONFIG; then
  if    query  "Invoke DansGuardian at boot? " y
  then  echo  "DG_BOOT=true"   >>  $SPELL_CONFIG
  else  echo  "DG_BOOT=false"  >>  $SPELL_CONFIG
  fi
fi

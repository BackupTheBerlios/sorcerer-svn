#!/bin/bash

### BEGIN INIT INFO
# Default-Mode: 510
# Required-Start: $networ
# Required-Stop: $network
# Default-Start: 2 3 4 5
# Default-Stop: 0 1 6
# Short-Description: updateclnt updates the client
### END INIT INFO

. /lib/lsb/init-functions

only start stop restart status
pid_file /vice/misc/updateclnt.pid
server /usr/sbin/updateclnt
name updateclnt

start(){
# run(){
#  [ -f        /vice/db/scm ] &&
#  read OPTS < /vice/db/scm
#  start_daemon -d -p "$PIDF" $SERV -h $OPTS
# }

 if   log_warning_msg "$NAME starting"; start_daemon -d -p "$PIDF" $SERV
 then log_success_msg "$NAME started"
 else log_failure_msg "$NAME not started"; status
 fi
}

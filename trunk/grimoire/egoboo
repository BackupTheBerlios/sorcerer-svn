# 2.6.5b does not compile because the Makefile contains carriage returns.

    stable 2.6.5b 2.6.1
  unstable 2.7.7
   require MesaLib SDL_mixer SDL_ttf egoboo-data
  category game
 attribute x11
 attribute broke
       url $SOURCEFORGE_URL
    source egoboo/egoboo-source-$VERSION.tar.gz
  homepage http://egoboo.sourceforge.net
  estimate 150
      desc 'egoboo is graphical nethack style game.'

build(){
cd game
make -f Makefile.unix &&
prepare_install &&
make -f Makefile.unix install
}

# might fail during install if doing multilib-concurrent

   version   stable 3.4.1 3.4.0 3.2.2 3.2.1 3.2.0 3.0.0
   version unstable 3.4.1 3.4.0 3.3.92 3.3.90
   require GConf clutter imake libcanberra startup-notification xorg-cf-files zenity
      opts --enable-static=no --enable-compiler-warnings=yes
  category development
 attribute gnome library makej multilib-dual optimize
#attribute gnome library makej multilib-concurrent optimize
    source $GNOME_URL sources/mutter/${VERSION:0:3}/mutter-$VERSION.tar.xz
      info last 20120419
      info home http://www.gnome.org
#   CFLAGS='-O2 -I/usr/include/gtk-2.0 -I/usr/include/glib-2.0'
     wmbin /usr/bin/mutter
      desc 'window manager similar to metacity'

build(){
 sed -i 's:hardcode_into_libs=yes:hardcode_into_libs=no:' configure
 case $VERSION in
  3.3.92) sed -i 's:-Werror::' configure ;;
  3.2.2)  sed -i 's:-Werror::' configure ;;
 esac
 default_build
}

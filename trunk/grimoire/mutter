# might fail during install if doing multilib-concurrent

with version   stable 3.6.2 3.6.1 3.6.0
with version unstable 3.7.4
with base    GConf clutter imake libcanberra startup-notification xorg-cf-files zenity
with also    --enable-static=no --enable-compiler-warnings=yes
with role    development
with trait   gnome library makej multilib-dual optimize
#trait gnome library makej multilib-concurrent optimize
with source    stable $GNOME_URL sources/mutter/${VERSION:0:3}/mutter-$VERSION.tar.xz
with source  unstable $GNOME_URL sources/mutter/${VERSION:0:3}/mutter-$VERSION.tar.xz
with info    last 20121117
with info    home http://www.gnome.org
with wmbin   /usr/bin/mutter
with info    cite 'window manager similar to metacity'

build(){
 sed -i 's:hardcode_into_libs=yes:hardcode_into_libs=no:' configure
 default_build
}

# Requirement list unknown since no successful compiles

    stable 2.31.5
  unstable 2.91.5 2.91.4
   require gtk+
  category development
 attribute gnome new
 attribute broke
    source $GNOME_URL sources/mutter/${VERSION:0:4}/mutter-$VERSION.tar.bz2
  homepage http://www.gnome.org
  estimate 150
      desc 'window manager similar to metacity'

build(){
 case $VERSION in
  2.31.5) sed -i 's:-Werror::' configure ;;
 esac
 default_build
}

# 0.73 has error during ./configure
# stable 0.73 0.72 0.71 0.70 0.69 0.68 0.65
    stable      0.72 0.71 0.70 0.69 0.68 0.65
   require ncurses
  optional gtk+ --enable-gtk2 --without-gtk for x11 output through gtk+

    ignore '^0\.73'
  category network
 attribute console x11
    source mtr-$VERSION.tar.gz
       url ftp://ftp.bitwizard.nl/mtr/
  homepage http://www.bitwizard.nl/mtr/
# While it supports gtk+-v1.2 what SA wants to use it instead of gtk+?
#            'gtk+-v1.2::--without-gtk:for x11 output through gtk+-v1.2' )
  estimate 130
      desc 'mtr is a combination of traceroute and ping.
mtr is a system administrators utility
to trace the route ip packets from the current system take
in getting to some destination system.
Superior to traceroute it also shows the
times packages take from that points like ping.'

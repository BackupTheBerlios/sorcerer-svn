# Never been tested because scons can not find png.
# scons is inadequate as a build tool!
    stable 2.5-src
   require scons
  category game
 attribute x11 broke
    source http://www.boswars.org/dist/releases/boswars-$VERSION.tar.gz
  homepage http://www.boswars.org/
  estimate 7700
      desc 'boswars is a real time futuristic real time strategy game.'

build(){
 scons release=1 &&
 prepare_install &&
 scons   install
}

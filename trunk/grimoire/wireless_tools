   version stable 29 28 27
  optional pcmciautils '' '' for PCMCIA Card Services
  optional crda
  category network
 attribute console library
    source http://hpl.hp.com/personal/Jean_Tourrilhes/Linux/wireless_tools.$VERSION.tar.gz
      info good 20120713
      info last 20070918
      info home http://hpl.hp.com/personal/Jean_Tourrilhes/Linux/Tools.html
      desc 'tools for wireless networking
Includes:
* iwconfig manipulate the basic wireless parameters
* iwlist (formerly part of iwspy) allow to list addresses, 
  frequencies, bit-rates...
* iwspy allow to get per node link quality
* iwpriv allow to manipulate the Wireless Extensions 
  specific to a driver (private)'


build(){
 local CC

 if   [[ $HOSTTYPE == x86_64 ]] && ! [[ $CFLAGS =~ -m32 ]]
 then LIB=/lib64
 else LIB=/lib
 fi

 if   [[ $HOSTTYPE == x86_64 ]] && [[ $CFLAGS =~ -m32 ]]
 then CC='gcc -m32'
 else CC='gcc'
 fi

 sed -i "s/-O2/$CFLAGS/
         s/-Os/$CFLAGS/
         s:/usr/local:/usr:" Makefile
 make CC="$CC" &&
 make install PREFIX=$DESTDIR/usr INSTALL_DIR=$DESTDIR/sbin/ INSTALL_LIB=$DESTDIR/$LIB
}

    stable 29 28 27 26 25 24 23
  optional pcmciautils '' '' for PCMCIA Card Services

  category network
 attribute console
    source wireless_tools.$VERSION.tar.gz
       url http://hpl.hp.com/personal/Jean_Tourrilhes/Linux/
#      url http://pcmcia-cs.sourceforge.net/ftp/contrib/
  homepage http://hpl.hp.com/personal/Jean_Tourrilhes/Linux/Tools.html
  estimate 30
      desc 'wireless_tools is for wireless networking.
Includes:
* iwconfig manipulate the basic wireless parameters
* iwlist (formerly part of iwspy) allow to list addresses, 
  frequencies, bit-rates...
* iwspy allow to get per node link quality
* iwpriv allow to manipulate the Wireless Extensions 
  specific to a driver (private)'


build(){
 case $HOSTTYPE in
  x86_64) LIB=/lib64 ;;
       *) LIB=/lib   ;;
 esac
 sed -i "s/-O2/$CFLAGS/
         s:/usr/local:/usr:" Makefile
 make            &&
 prepare_install &&
 make    install PREFIX=/usr INSTALL_DIR=/sbin/ INSTALL_LIB=$LIB
}

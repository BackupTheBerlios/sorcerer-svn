# http://soprano.sourceforge.ent/node/63 states
# Soprano 2.8.0 is the latest major release of the Qt RDF storage and parsing framework.
# It is targetted towards KDE 4.9 users and comes with a bunch of improvements over the 2.7 series:

# version 2.9.0 not yet tested

with version   stable 2.8.0 2.7.6 2.6.51
#with version unstable 2.9.0
with inherit kde-v4
with base    clucene-core qt-everywhere-opensource redland
with elect   doxygen
with role    db
with trait   kde4 library makej multilib-concurrent
with source    stable $SOURCEFORGE_URL soprano/Soprano/$VERSION/soprano-$VERSION.tar.bz2
with source  unstable $SOURCEFORGE_URL soprano/Soprano/$VERSION/soprano-$VERSION.tar.bz2
with info    good 20120702
with info    last 20120627
with info    home http://soprano.sourceforge.net/
with info    cite 'QT4 framework for RDF data'

build(){
 if    [[ $HOSTTYPE == x86_64 ]]; then
  if   [[ $CFLAGS   =~ -m32   ]]
  then unset JAVA_HOME
  else with also -DLIB_DESTINATION:STRING=/usr/lib64
  fi
 fi

 kde_build
}

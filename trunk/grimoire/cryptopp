# 561 fails during install
    stable 560 552
  unstable 561
  category security/encryption
 attribute library
    source $SOURCEFORGE_URL cryptopp/cryptopp/${VERSION:0:1}.${VERSION:1:1}.${VERSION:2:1}/cryptopp$VERSION.zip
      vurl http://www.cryptopp.com/
  homepage http://www.cryptopp.com/
  estimate 140
      desc 'a C++ class libraries of cryptographic algorithms'

build(){
sed "s:-DNDEBUG -g -O2:$CXXFLAGS:
     s:-march=native -mtune=native::" GNUmakefile > Makefile
rm GNUmakefile
make &&
make install "PREFIX=$DESTDIR/usr"
}

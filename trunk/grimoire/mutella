   version stable 0.4.5 0.4.3 0.4.1 0.4
   require ncurses readline zlib
  category network/file-share
 attribute console
    source $SOURCEFORGE_URL mutella/mutella/mutella-$VERSION/mutella-$VERSION.tar.gz
  homepage http://mutella.sourceforge.net
      desc 'mutella is a terminal based peer-to-peer client
mutella supports all the functionality required to 
participate as a full-featured node in the Gnutella network.
Full-featured implies support for file search, download and sharing.
Mutella is optimized for a high-bandwidth connection, but can also 
run on a modest-speed line.'


build(){
patch -p0 < $SCRIPT_DIR/gcc4x.patch
default_build
}

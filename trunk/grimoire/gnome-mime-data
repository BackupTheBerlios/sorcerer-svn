# desktop-file-utils here causes a recursive requirement loop, bad.

with version stable 2.18.0 2.4.3 2.4.2 2.4.1 2.4.0
with base    shared-mime-info
with role    db
with trait   archive_off gnome library makej multilib-dual
with source  $GNOME_URL sources/gnome-mime-data/${VERSION:0:4}/gnome-mime-data-$VERSION.tar.bz2
with info    good 20120608
with info    last 20070403
with info    home http://www.gnome.org
with info    cite 'the base MIME/app database for GNOME2
gnome-mime-data contains the base MIME and Application database for GNOME2.
It is meant to be accessed through the MIME functions in GnomeVFS.'

build(){
 sed -i 's:\(pkgconfigdir = $(\)datadir\()/pkgconfig\):\1libdir\2:' Makefile.in
 default_build
}

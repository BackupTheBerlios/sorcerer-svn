    stable 1.6.0
   require postgresql idlastro
  optional gsl '' '' to build sdsspixIDL
  category science/idl science/astronomy
 attribute library new
    source http://sdssidl.googlecode.com/files/sdssidl-v$VERSION.tar.gz
  homepage http://code.google.com/p/sdssidl
  estimate 1273
      desc 'sdssidl is a set of general IDL utilities
for plotting, data analysis, and database access. The
distribution includes C/C++ extensions for postgres
database interface, efficient file input/output, efficient
ra/dec searching using HTM.
There are many routines for working with Sloan Digital Sky 
Survey Data, especially the "flat files" and characterization
of the survey area.

IDL software must be installed before you install sdssidl.'

build(){
 SDSSIDL_DIR=/usr/local/sdssidl
 export IDL_DIR=/usr/local/itt/idl

 sed -i "s:some_path/sdssidl:$SDSSIDL_DIR:" setup/*  &&
 ./configure            &&
 make                   &&
 prepare_install        &&
 mkdir -p $SDSSIDL_DIR  &&
 cp -rv * $SDSSIDL_DIR
}

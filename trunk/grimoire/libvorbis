# warzone2100 crashes at start due to an audio error
# when libvorbis version 1.2.2 is installed
# Perhaps libvorbis 1.2.2 has an error in it?

with version stable 1.3.3
with base    libogg
with also    --enable-static=no
with role    audio/codec audio/library
with trait   library lto makej multilib-concurrent
with source  http://downloads.xiph.org/releases/vorbis/libvorbis-$VERSION.tar.gz
with info    good 20120726
with info    last 20120203
with info    home http://www.xiph.org/ogg/vorbis
with info    cite 'high-quality lossy audio codec library'

build(){
 sed -i 's:-O20 -ffast-math::' configure{,.in}
 default_build
}

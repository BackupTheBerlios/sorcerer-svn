# 0.10.1 has compile error
# 0.10.0 had .tgz extension, odd.
# 0.10.3 requires newer version of qt-everywhere-opensource
# than we have currently tested okay with.
# why does the software authors want an optional on openssl?
# 0.11.4a failed compilation

   version   stable 0.11.3 0.11.2
   version unstable 0.11.4a
   require SDL_mixer kdelibs-v4
  optional openssl
      opts -DCMAKE_INSTALL_PREFIX:PATH=/usr
      opts -DCMAKE_INSTALL_RPATH:PATH=/usr/lib64:/usr/lib
      opts -DCMAKE_SKIP_BUILD_RPATH:BOOL=TRUE
      opts -DCMAKE_BUILD_WITH_INSTALL_RPATH:BOOL=TRUE
      opts -DCMAKE_INSTALL_RPATH_USE_LINK_PATH:BOOL=TRUE
      opts -DCMAKE_BUILD_TYPE=Release
  category game graphic
 attribute kde4
    source   stable $SOURCEFORGE_URL stellarium/Stellarium-sources/${VERSION:0:6}/stellarium-$VERSION.tar.gz
    source unstable $SOURCEFORGE_URL stellarium/Stellarium-sources/${VERSION:0:6}/stellarium-$VERSION.tar.gz
      info last 20120827
      info home http://stellarium.org
      desc 'renders 3D photo-realistic skies using OpenGL
It displays stars, constellations,
planets, nebulas and others things
like ground, landscape, fog, etc.'

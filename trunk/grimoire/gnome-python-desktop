# Can also provide bindings for budbuddy

   version stable 2.32.0 2.30.2
   require evolution-data-server gnome-desktop libgnomeprintui libgtop librsvg totem-pl-parser
  optional totem '' '' for python bindings for totem
  category development/binding
 attribute gnome
    source $GNOME_URL sources/gnome-python-desktop/${VERSION:0:4}/gnome-python-desktop-$VERSION.tar.bz2
  homepage http://www.gnome.org
      desc 'additional python binding modules'

build(){
 sed -i 's:hardcode_into_libs=yes:hardcode_into_libs=no:' configure
 default_build
}

   version stable 2.2.1-201012301333 2.1.11-200804142058
   require bison findutils flex
  category security
 attribute console
    source http://www.grsecurity.net/gradm-$VERSION.tar.gz
      info home  http://www.grsecurity.net/
      info docs  http://grsecurity.net/papers.php 
 freshmeat grsecurity
      desc 'ACL administration tools for grsecurity enabled boxes'

build(){
 sed -i "s:-O2:$CFLAGS:
         s:-static:-static $LDFLAGS:
         s:/usr/bin/gcc:gcc:
         s:/sbin/\$(GRADM_BIN) -P:true:" Makefile
 make &&
 make install DESTDIR=$DESTDIR &&
 echo "Run '/sbin/gradm -P' to set the ACL password."
}

# This may no longer work considering udev
post_install(){
if   ! [   -c     /dev/gsec  ]
then mknod -m 622 /dev/grsec c 1 10
fi
}

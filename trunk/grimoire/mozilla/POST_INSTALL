recast()  {
  if       spell_installed  $1  &&
        !  spell_held       $1
  then  cast    -c          $1
  if  
}

if  !  [  "$IVERSION"  ==  "$VERSION"  ];  then

  unlock_cast
  recast  acroread
  recast  j2sdk
  recast  mozplugger

fi

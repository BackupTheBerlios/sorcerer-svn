    stable 0.9.1 0.5.12
  category development/interpreter/ruby
 attribute library
    source $SOURCEFORGE_URL rubyzip/rubyzip/$VERSION/rubyzip-$VERSION.tgz
  homepage http://rubyzip.sourceforge.net
      desc 'rubyzip is a zip utility for Ruby.
rubyzip is a ruby library for reading and writing zip
(pkzip format) files, with the restriction that only
uncompressed and deflated zip entries are supported.
All this library does is handling of the zip file format.
The actual compression/decompression is handled by zlib.'

build(){
prepare_install &&
ruby    install.rb
}

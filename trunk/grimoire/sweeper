with info    edit 20130903
with inherit kde-v4
with base    kde-runtime
with role    utility
with source    stable $KDE_URL   stable/$VERSION/src/${SPELL%-v4}-$VERSION.tar.xz
with source  unstable $KDE_URL unstable/$VERSION/src/${SPELL%-v4}-$VERSION.tar.xz
with info    cite 'deletes cached data by kde applications
However, an application such as firefox is not a kde application
and therefore might not have cached files removed by sweeper.'

# seems to be having a problem with docbook2man

    stable 2.32.0
  unstable 2.91.3 2.91.2
   require cups gnome-desktop lcms libgphoto2 libnotify libunique sane-backends vte
  category development
 attribute gnome new
#attribute broke
    source $GNOME_URL sources/gnome-color-manager/${VERSION:0:4}/gnome-color-manager-$VERSION.tar.bz2
  homepage http://projects.gnome.org/gnome-color-manager/
  estimate 1200
      desc 'session framework that for installing and generating color profiles in GNOME'

build(){
 case $VERSION in
  2.32.0) sed -i '/\tman\t/d' Makefile.in ;;
 esac
 default_build
}


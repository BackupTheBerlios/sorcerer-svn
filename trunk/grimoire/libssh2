# If both libgcrypt and openssl are installed
# libssh2 version 0.17 will use openssl

# version 1.4.0 formerly compiled, but now does not
# with both openssl and libgcrypt tried
# and without makej and multilib-concurrent

   version   stable 1.4.1 1.3.0 1.2.9 1.2.8 1.2.7 1.2.6
   require libgcrypt zlib
#     opts --with-libgcrypt
# optional libgcrypt '' '' libssh2 requires either libgcrypt or openssl
# optional openssl   '' '' libssh2 requires either libgcrypt or openssl
#     opts --enable-static=no
   require automake libtool
  category network/security
 attribute library makej multilib-dual
    source http://www.libssh2.org/download/libssh2-$VERSION.tar.gz
      info last 20120405
      info home http://www.libssh2.org/
      desc 'library that implements the ssh2 protocol'

# build(){
#  aclocal; automake -acf; libtoolize -cf; autoreconf -fi
#  default_build
# }

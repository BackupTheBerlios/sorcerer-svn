   VERSION=( "0.17" )
  CATEGORY="network/security"
 ATTRIBUTE="library"
    SOURCE="libssh2/libssh2-$VERSION.tar.gz"
       URL="$SOURCEFORGE_URL"
  HOMEPAGE="http://www.libssh2.org/"
       REQ=( "zlib"
             "libgcrypt:::libssh2 requires either libgcrypt or openssl"
             "openssl:::libssh2 requires either libgcrypt or openssl" )
# If both libgcrypt and openssl are installed
# libssh2 version 0.17 will use openssl
  ESTIMATE="580"
      DESC="libssh2 provides a library that implements the ssh2 protocol."

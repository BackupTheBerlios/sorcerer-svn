# ksquirrel has compile errors

    stable 0.8.0
   require kdebase-v3 ksquirrel-libs
  category graphic/viewer
 attribute kde3
 attribute broke
    source $SOURCEFORGE_URL ksquirrel/ksquirrel/ksquirrel-$VERSION/ksquirrel-$VERSION.tar.bz2
  homepage http://ksquirrel.sourceforge.net/
      opts --prefix=/opt/kde3 --with-qt-dir=/opt/kde3 --disable-debug --enable-final
  estimate 1600
      desc 'an image viewer, simple and convenient in usage'

build(){
 case $VERSION in
  0.8.0) sed -i '1i#include <cstring>
                 1i#include <cstdlib>' $( find . -name \*.cpp ) ;;
 esac
 default_build
}

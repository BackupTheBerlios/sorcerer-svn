#  VERSION=( "0.3" "0.2" )
   VERSION=( "0.2" )
    IGNORE="^0\.3"
# 0.3 has a link error, but not yet sure exactly why.
# It is probably a bug due to wavemixer being very developmental.
# I am not going to attempt to fix it.
  CATEGORY="audio-player audio-editor"
 ATTRIBUTE="x11 new"
    SOURCE="wavemixer/wavemixer_$VERSION.tar.gz"
#   SOURCE="wavemixer/wavemixer-$VERSION.tar.bz2"
       URL="$SOURCEFORGE_URL"
  HOMEPAGE="http://wavemixer.sf.net"
#      REQ=( "fftw gstreamer gtkmm-v2.2 libxml2 sox taglib"
       REQ=( "fftw gtkmm-v2.2 libxml2 sox taglib"
             "esound:::for output through esound" )
  ESTIMATE="66"
      DESC="wavemixer is a multitrack wave editor.
It can mix multiple sounds together from many formats,
with the option to add various different effects to the result."

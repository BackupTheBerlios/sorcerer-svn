# jack is broke in 0.19
#            'jackit:--with-jack:--without-jack:for jack low-latency audio server support' )
#     opts --without-faac
# faac support broken in 0.16 and causes compile error


   version stable 1.0
   require automake gcc-g++ lame libtool libvorbis
  optional alsa-lib --with-alsa --without-alsa for audio ouput using alsa
  optional faac     --with-faac --without-faac for encoding AAC streams
  optional libsamplerate
  optional twolame
      opts --without-jack
  category audio/server
 attribute server x11
    source http://darkice.googlecode.com/files/darkice-$VERSION.tar.gz
      vurl http://code.google.com/p/darkice/downloads/list
      info home  http://code.google.com/p/darkice/
      info mail  http://www.freelists.org/list/darkice
      desc 'icecast, icecast2, and shoutcast live audio streamer
It takes audio input from a sound card, encodes it into MP3/Ogg Vorbis,
and sends the stream to one or more servers
(Icecast/Shoutcast if it is MP3,
and Icecast2 if it is Ogg Vorbis).'

build(){
 aclocal -I .             &&
 autoheader               &&
 libtoolize --automake    &&
 automake   --add-missing &&
 autoconf                 &&
 default_build
}

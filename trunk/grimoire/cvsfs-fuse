    stable 0.5 0.4 0.3 0.2
   require cvs fuse
  category disk/file-system
 attribute linux-26-module
    source cvsfs/cvsfs-fuse-$VERSION.tar.gz
       url "$SOURCEFORGE_URL"
  homepage http://sourceforge.net/projects/cvsfs
  estimate 60
      desc 'cvsfs-fuse mounts a CVS project to a directory like a filesystem.
cvsfs-fuse is an attempt to let a user mount a CVS project like any filesystem.
It allows you to navigate and browse through a project tree.
No preparations are required on the CVS server side.'


build(){
export PATH="${PATH/\/usr\/ccache:/}"
default_build
}

/aux/can/git/
When software is acquired from a git repository
instead of by downloading a source tarball
then the files are stored somewhere within /aux/can/git/
This differs from most other directories because
the hierarchy represents a git URL
rather than having it identified by spell name.

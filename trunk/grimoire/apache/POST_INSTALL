PHP_PROVIDER="$(  get_provider php  )"
if    spell_installed  $PHP_PROVIDER  &&
    ! spell_held       $PHP_PROVIDER
then  break_lock       $CASTING
      cast             $PHP_PROVIDER
fi

/etc/init.d/apache.sh  start
true

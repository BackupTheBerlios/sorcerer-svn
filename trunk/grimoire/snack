# alsa support does not work with libsnack
# --disable-alsa required for use with amsn
# applying the patch fixes it.

# make    install exec_prefix=/usr
# above required to correct installation bug with snack 2.2.10
# where it installs to /lib instead of /usr/lib

    stable 2.2.10
   require Python tk
      opts --enable-shared
  category audio/library
 attribute library
  homepage http://www.speech.kth.se/snack/
    source http://www.speech.kth.se/snack/dist/snack$VERSION.tar.gz
  estimate 100
      desc 'audio toolkit for use with TCL/TK or Python'

# --sysconfdir=/etc        \
# --localstatedir=/var     \

build(){
patch -p0 < $SCRIPT_DIR/alsa.patch
cd unix
./configure --prefix=/usr $OPTS &&
make            &&
prepare_install &&
make    install exec_prefix=/usr &&
cd ../python  &&
python setup.py install
}

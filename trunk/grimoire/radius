   VERSION=( "1.5" "1.4" )
      SAFE=( "1.5" "1.4" )
  CATEGORY="security/authentication"
 ATTRIBUTE="console server new broken"
#ATTRIBUTE="console server new"
# 1.4 , 1.5 has compile error
    SOURCE="radius/radius-$VERSION.tar.bz2"
       URL="$GNU_URL"
  HOMEPAGE="http://www.gnu.org/software/radius"
# Requirement list not verified yet.
       REQ=( "gdbm libtool openssl"
             "Linux-PAM:::for pluggable authentication"
             "mysql:::for mysql backend"
             "openldap:::for authentication using ldap"
             "postgresql:::for interfacing with postgresql db"
             "unixODBC:::for interfacing with windoze databases" )
case  $VERSION  in
  1.1.[0-3])  OPTS="--without-rlm_krb5"  ;;
esac
# Has a problem with heimdal's installed kerberos.
# But the above bypasses the problem
  ESTIMATE="4050"
      DESC="radius provides provides a RADIUS authenticaton, accounting and logging server."

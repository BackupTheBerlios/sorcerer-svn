   version stable 3.0.13 3.0.12
  category disk/file-system
 attribute console
    source http://ftp.us.debian.org/debian/pool/main/d/dosfstools/dosfstools_$VERSION.orig.tar.xz
      info last 20120705
      info home ftp://ftp.uni-erlangen.de/pub/Linux/LOCAL/dosfstools/
      desc 'utilities to create and check MS-DOS filesystems
dosfstools contains utilities to create and check MS-DOS filesystems.
Includes :
  mkdosfs mkfs.msdos mkfs.vfat
  dosfsck fsck.msdos fsck.vfat'
 
build(){
 sed -i "s/-O2 -fomit-frame-pointer/$CFLAGS/" Makefile
 sed -i "1i#include <errno.h>" mkdosfs/mkdosfs.c

 MAKE_OPTS="PREFIX=/usr MANDIR=/usr/man/man8 DESTDIR=$DESTDIR"

 make         $MAKE_OPTS &&
 make install $MAKE_OPTS
}

# Possible gcc 4.4.2 patch at http://bugs.gentoo.org/254254
# 0.9.23.0 fails compilation

    stable 0.9.21b 0.9.20
   require gcc-g++
      opts --enable-static=no
  category development
 attribute library
 attribute broke
    source $SOURCEFORGE_URL clucene/clucene-core-stable/$VERSION/clucene-core-$VERSION.tar.bz2
#   source $SOURCEFORGE_URL clucene/clucene-core-unstable/${VERSION:0:6}/clucene-core-${VERSION}-Source.tar.bz2
      home http://clucene.sourceforge.net/
      docs http://sourceforge.net/apps/mediawiki/clucene/index.php?title=Main_Page
  estimate 700
      desc 'searching API'

build_old(){
 ./configure           \
  --sysconfdir=/etc    \
  --localstatedir=/var \
  --prefix=/usr        \
 $OPTS &&
 make &&
 make install "DESTDIR=$DESTDIR"
}

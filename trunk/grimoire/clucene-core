    stable 0.9.20
  unstable 0.9.23.0-Source
  category development
 attribute library
       url $SOURCEFORGE_URL
    source clucene/clucene-core-$VERSION.tar.bz2
  homepage http://clucene.sourceforge.net/
      opts --enable-static=no
  estimate 800
      desc 'clucene-core provides a searching API'

build(){
 unset LD_PRELOAD
 ./configure           \
  --build=$BUILD       \
  --sysconfdir=/etc    \
  --localstatedir=/var \
  --prefix=/usr        \
 $OPTS           &&
 make            &&
 prepare_install &&
 make    install
}

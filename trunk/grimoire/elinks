# 0.11.7 and 0.12pre5 fail compilation

with version   stable 0.12pre5 0.11.7
with version unstable 0.12pre5
with base    bzip2 zlib
with elect   expat   ''                  --disable-xbel                  for XML parser for XBEL bookmark support
with elect   gpm     ''                  '--disable-mouse --without-gpm' for mouse support
with elect   guile   ''                  --without-guile                 to write plugins using guile
with elect   lua     ''                  --without-lua                   for writing plugins using lua interface
#with elect   lua     ''                  --without-lua                   for writing plugins using lua interface
with elect   xulrunner --with-spidermonkey --without-spidermonkey        for javascript support using mozilla spidermonkey
with elect   openssl --with-ssl          --without-ssl                   for HTTPS encrypted connections
with elect   ruby    --with-ruby         --without-ruby                  for ruby support
#with elect   ruby    --with-ruby         --without-ruby                  for ruby support
with elect   samba   ''                  --disable-smb                   for smb protocol support
with also    --without-gnutls
with role    network/web
with trait   client console
with trait   broke
with source    stable http://elinks.or.cz/download/elinks-$VERSION.tar.bz2
with source  unstable http://elinks.or.cz/download/elinks-$VERSION.tar.bz2
with info    omit 20130101
with info    last 20090822
with info    home http://elinks.or.cz/
with info    docs http://elinks.or.cz/documentation/index.html
with info    cite 'text mode http/ftp brwoser
It can render both frames and tables, is highly customizable,
and can be extended via Lua, Guile, Perl, or Ruby scripts.
It has limited support for CSS and Javascript.'


build(){
 export  CFLAGS+=" $( pkg-config --cflags mozilla-js )"
 export LDFLAGS+=" $( pkg-config --libs   mozilla-js )"

 # export   CFLAGS+=" -I/usr/include/mozilla/js"
 # export CXXFLAGS+=" -I/usr/include/mozilla/js"
 # export  LDFLAGS+=" -L/usr/lib/mozilla -Wl,-rpath-link,/usr/lib/mozilla -Wl,-rpath,/usr/lib/mozilla"
 # sed -i "s:smjs:jsj:" configure
 default_build
}

# 0.11.7 and 0.12pre5 fail compilation

   version   stable 0.12pre5 0.11.7
   version unstable 0.12pre5
   require bzip2 zlib
  optional expat   ''                  --disable-xbel                  for XML parser for XBEL bookmark support
  optional gpm     ''                  '--disable-mouse --without-gpm' for mouse support
  optional guile   ''                  --without-guile                 to write plugins using guile
  optional lua     ''                  --without-lua                   for writing plugins using lua interface
# optional lua     ''                  --without-lua                   for writing plugins using lua interface
  optional xulrunner --with-spidermonkey --without-spidermonkey        for javascript support using mozilla spidermonkey
  optional openssl --with-ssl          --without-ssl                   for HTTPS encrypted connections
  optional ruby    --with-ruby         --without-ruby                  for ruby support
# optional ruby    --with-ruby         --without-ruby                  for ruby support
  optional samba   ''                  --disable-smb                   for smb protocol support
      opts --without-gnutls
  category network/web
 attribute client console
 attribute broke
    source   stable http://elinks.or.cz/download/elinks-$VERSION.tar.bz2
    source unstable http://elinks.or.cz/download/elinks-$VERSION.tar.bz2
      info omit 20130101
      info last 20090822
      info home http://elinks.or.cz/
      info docs http://elinks.or.cz/documentation/index.html
      desc 'text mode http/ftp brwoser
It can render both frames and tables, is highly customizable,
and can be extended via Lua, Guile, Perl, or Ruby scripts.
It has limited support for CSS and Javascript.'


build(){
 export  CFLAGS+=" $( pkg-config --cflags mozilla-js )"
 export LDFLAGS+=" $( pkg-config --libs   mozilla-js )"

 # export   CFLAGS+=" -I/usr/include/mozilla/js"
 # export CXXFLAGS+=" -I/usr/include/mozilla/js"
 # export  LDFLAGS+=" -L/usr/lib/mozilla -Wl,-rpath-link,/usr/lib/mozilla -Wl,-rpath,/usr/lib/mozilla"
 # sed -i "s:smjs:jsj:" configure
 default_build
}

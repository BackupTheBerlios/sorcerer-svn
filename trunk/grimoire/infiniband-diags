# 1,6,1 1.6.0 requires rst2man in order to compile
# and yet does not give a location for it.
# an email post suggested that rst2man should be installed
# by docutils or python-docutils,
# but that does not seem to be so.

   version   stable 1.5.13
   version unstable 1.6.1 1.6.0
   require libibmad opensm perl
      opts --enable-static=no
  category network
 attribute console library makej multilib-concurrent
    source http://www.openfabrics.org/downloads/management/infiniband-diags-$VERSION.tar.gz
      info last 20120731
      info home http://www.openfabrics.org/downloads/management/README
      desc 'various diagnostic tools'

#build(){
# if   [[ $HOSTTYPE == x86_64 ]] && ! [[ $CFLAGS =~ -m32 ]]
# then export LDFLAGS+=' -L/usr/lib64'
# fi
# default_build
#}

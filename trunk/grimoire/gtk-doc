   VERSION=( "1.2" "1.1" "1.0" "0.10" )
  CATEGORY="editor"
# configure: error: could not find DocBook XSL Stylesheets in XML catalog
 ATTRIBUTE="broken library"
    SOURCE="sources/gtk-doc/${VERSION:0:3}/gtk-doc-$VERSION"
       URL="$GNOME_URL"
  HOMEPAGE="http://gtkspell.sourceforge.net"
       REQ="docbook-dtd,v4 perl libxslt openjade"
# docbook-dsssl?
     SHORT="gtk-doc is a DocBook documentation generator, may cause some spells to fail."
stream << EOF
gtk-doc may cause some spells to fail during compilation!
GTK-Doc is used to document C code.
It is typically used to document the public API of 
libraries, such as the GTK+ and Gnome libraries.
But it can also be used to document application code.
EOF

with info    good 20130131
with info    last 20120130
with version stable 1.2.12
with base    SDL jpeg libpng tiff
with also    --enable-static=no
with role    graphic/library
with trait   library makej multilib-concurrent x11
with source  stable http://www.libsdl.org/projects/SDL_image/release/SDL_image-$VERSION.tar.gz
with info    home http://www.libsdl.org/projects/SDL_image/
with info    docs http://www.libsdl.org/projects/SDL_image/docs/index.html
with info    freecode sdl_image
with info    cite 'library to load images of various formats as SDL surfaces
SDL_image is a simple library to load
images of various formats as SDL surfaces.
It supports BMP, PPM, XPM, PCX,
GIF, JPEG, PNG, and TGA formats.'

build(){
# if   [[ $HOSTTYPE == x86_64 ]] && ! [[ $CFLAGS =~ -m32 ]]
# then export LDFLAGS+=' -Wl,-rpath,/opt/libpng/1.4.x/lib64'
# else export LDFLAGS+=' -Wl,-rpath,/opt/libpng/1.4.x/lib'
# fi
 default_build
}

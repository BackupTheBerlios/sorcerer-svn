# Did not see a libjingle 0.4.0 on the sourceforge files project page.
   version stable 0.3.11
   require expat openssl
  category network/chat
 attribute library
 attribute broke
 attribute deprecated

#   source $SOURCEFORGE_URL tapioca-voip/libjingle/$VERSION/libjingle-$VERSION.tar.gz
#   source $SOURCEFORGE_URL tapioca-voip/libjingle-$VERSION.tar.gz
  homepage http://tapioca-voip.sourceforge.net
case $VERSION in
 0.3.11) use_gcc gcc-v4.2 ;;
esac
      desc 'library for the google talk voice protocol'

build(){
 case $VERSION in
  0.3.11) sed -i '1i#include <cstring>' talk/base/messagequeue.h \
                                        talk/base/physicalsocketserver.cc \
  ;;
 esac
 default_build
}

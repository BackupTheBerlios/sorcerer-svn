with info    last 20130924
with version stable 1.55 1.54 1.51
with base    perl--Module-Build-Tiny
with role    perl
with trait   perl-module
with source  stable http://www.cpan.org/authors/id/E/ET/ETHER/YAML-Tiny-$VERSION.tar.gz
with info    home http://search.cpan.org/search?dist=YAML-Tiny
with info    cite 'read/write YAML files with as little code as possible'

# build(){
#  perl Build.PL $OPTS &&
#  ./Build build &&
#  ./Build install --destdir "$DESTDIR"
# }

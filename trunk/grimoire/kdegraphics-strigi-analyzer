# broke because strigi is broke
with trait   broke
with info    edit 20131211
with inherit kde-v4
with base    kdelibs-v4 strigi tiff
with role    utility/search
with trait   library makej multilib-concurrent
with source    stable $KDE_URL   stable/$VERSION/src/$TECHNIC-$VERSION.tar.xz
with source  unstable $KDE_URL unstable/$VERSION/src/$TECHNIC-$VERSION.tar.xz
with info    cite 'kde interface library for strigi'

current(){ echo kdegraphics-strigi-analyzer | pipe_queue "$REAP_QUEUE"; }

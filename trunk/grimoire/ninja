   VERSION=( "0.1.2" "0.1.1" "0.0.8" )
  CATEGORY="utility"
 ATTRIBUTE="console new"
    SOURCE="ninja-$VERSION.tar.bz2"
       URL="http://forkbomb.org/ninja/src"
  HOMEPAGE="http://forkbomb.org/ninja/"
       REQ=( "" )
  ESTIMATE="18"
# This spell could use development
# The program has good potential for maintaining security
# unless a user gains root and kills ninja faster than ninja
# can kill the offending program.
      DESC="ninja a privilege escalation detection and prevention system.
It will detect and prevent unauthorized users from gaining root."

build()  {
make             &&
prepare_install  &&
make    install  DESTDIR=/usr/bin  MANDIR=/usr/man/man8
}

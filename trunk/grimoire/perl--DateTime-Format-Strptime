with info    good 20130827
with info    last 20130402
with version stable 1.54 1.51
with base    perl--DateTime perl--DateTime-Locale perl--Params-Validate
with role    perl utility/time
with trait   perl-module
with source  stable http://www.cpan.org/authors/id/D/DR/DROLSKY/DateTime-Format-Strptime-$VERSION.tar.gz
with info    home http://search.cpan.org/search?dist=DateTime-Format-Strptime
with info    cite 'replicates unix command strptime
Strptime is the unix command that is the reverse of Strftime.
While Strftime takes a DateTime and outputs it in a given format,
Strptime takes a DateTime and a format and returns the DateTime
object associated.'

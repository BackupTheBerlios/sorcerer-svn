   version stable 1.1000 1.0901
   require perl--DateTime perl--DateTime-Locale perl--Params-Validate
  category perl utility/time
 attribute perl-module
    source http://www.cpan.org/authors/id/R/RI/RICKM/DateTime-Format-Strptime-$VERSION.tgz
      info home http://search.cpan.org/search?dist=DateTime-Format-Strptime
      desc 'replicates unix command strptime
Strptime is the unix command that is the reverse of Strftime.
While Strftime takes a DateTime and outputs it in a given format,
Strptime takes a DateTime and a format and returns the DateTime
object associated.'

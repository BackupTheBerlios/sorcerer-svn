with info    edit 20131211
with inherit kde-v4
with base    kde-runtime
with role    utility/time
with source    stable $KDE_URL   stable/$VERSION/src/${TECHNIC%-v4}-$VERSION.tar.xz
with source  unstable $KDE_URL unstable/$VERSION/src/${TECHNIC%-v4}-$VERSION.tar.xz
with info    cite 'allows for delayed startup of an application

This might be a kde graphical frontend to the sleep command?'

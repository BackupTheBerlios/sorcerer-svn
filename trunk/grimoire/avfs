with info    good 20131111
with info    last 20120612
with version stable 1.0.1
with base    fuse xz
with also    --enable-static=no
with role    utility/file-manager
with trait   library makej multilib-concurrent
with source  stable $SOURCEFORGE_URL avf/avfs/$VERSION/avfs-$VERSION.tar.bz2
with info    home http://www.boomerangsworld.de/cms/avfs/index
with info    cite 'virtual filesystem library'

build(){ default_build && rmdir --ignore-fail-on-non-empty "$DESTDIR"/usr/sbin/; }

   VERSION="2.0.36"
  NVERSION="2.0.39"
# Hold apache at 2.0.36 until newer php than 4.2.1 becomes available, 
# because php 4.2.1 fails to build apache php module.
# note may need POST_INSTALL to recast php if installed.
  CATEGORY="web"
 ATTRIBUTE="console server"
 SOURCE[0]="httpd/httpd-$VERSION"
 SOURCE[1]="mod_perl-1.99_04"
  HOMEPAGE="http://httpd.apache.org"
    URL[0]="$APACHE_URL"
    URL[1]="http://perl.apache.org/dist"
       REQ="db mm"
    OPT[0]="openssl:::for https enabled server"
    OPT[1]="perl:::for mod_perl enabled server"
 CONFLICTS="apache"
  EPROVIDE="web-server apache"
 FRESHMEAT="apache"
 DISTROWAT="Apache"
     SHORT="apache is a popular HTTP server."
cat << EOF
Apache is the world's most popular HTTP server, being quite possibly
the best around in terms of functionality, efficiency, security and
speed.
EOF

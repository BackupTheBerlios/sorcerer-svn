   inherit compiz
 attribute broke
   require libcompizconfig kdelibs-v3
    source compizconfig-backend-kconfig-$VERSION.tar.bz2
      opts --with-qt-dir=/opt/kde3 --prefix=/opt/kde3
  estimate 300
      desc 'the kconfig backend for CompizConfig
It uses the KDE configuration system to store the compiz configuration and provides 
integration into the KDE desktop environment.'

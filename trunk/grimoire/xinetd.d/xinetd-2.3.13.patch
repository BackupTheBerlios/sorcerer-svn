diff -r -U2 xinetd-2.3.13/xinetd/service.c xinetd-2.3.13/xinetd/service.c
--- xinetd-2.3.13/xinetd/service.c	2003-11-16 05:44:10.000000000 -0700
+++ xinetd-2.3.13/xinetd/service.c	2003-11-16 05:44:10.000000000 -0700
@@ -765,6 +765,6 @@
 
             if ( last == NULL ) {
-               last = SAIN( SVC_LAST_DGRAM_ADDR(sp) ) = 
-		  SAIN( calloc( 1, sizeof(union xsockaddr) ) );
+               SVC_LAST_DGRAM_ADDR(sp) =  SAIN( calloc( 1, sizeof(union xsockaddr) ) );
+               last = SAIN( SVC_LAST_DGRAM_ADDR(sp) );
             }
 
@@ -792,6 +792,6 @@
 
 	    if( last == NULL ) {
-               last = SAIN6(SVC_LAST_DGRAM_ADDR(sp)) = 
-		  SAIN6(calloc( 1, sizeof(union xsockaddr) ) );
+               SVC_LAST_DGRAM_ADDR(sp) = SAIN6(calloc( 1, sizeof(union xsockaddr) ) );
+               last = SAIN6(SVC_LAST_DGRAM_ADDR(sp));
             }
 

   VERSION=( "1.1.0" "1.0.0" "0.9.2" )
    IGNORE="torrent"
  CATEGORY="audio-player"
 ATTRIBUTE="console"
  HOMEPAGE="http://moc.daper.net"
       URL="ftp://ftp.daper.net/pub/soft/moc"
       REQ="mad ncurses libao libvorbis"
      DESC="moc is a console audio player with ncurses interface.

moc (music on console) is a console audio player 
with a simple ncurses interface in playmp3list style.
It supports MP3 (with id3tag, VBR, and Xing header) 
and OGG formats, and the OSS mixer.
"


build() {
./configure  --build=$BUILD        \
             --prefix=/usr         \
             --sysconfdir=/etc     \
             --localstatedir=/var  \
             $OPTS                 &&
make                               &&
prepare_install                    &&
cp  moc    /usr/bin/music-on-console  &&
cp  moc.8  /usr/man/man8/music-on-console
}

   version stable 0.6.2 0.6.1 0.6.0 0.5.1 0.4.5
   require imake ncurses xorg-cf-files zlib
  category window-manager
 attribute console x11
    source $SOURCEFORGE_URL twin/twin/$VERSION/twin-$VERSION.tar.gz
      info last 20120225
      info home http://sourceforge.net/projects/twin
case $VERSION in
 0.5.1) use_gcc gcc-v3.4 ;;
esac
      desc 'twin is a text-mode window environment.

It turns a text terminal into a X11-style display with window manager,
terminal windows, and can also serve as display for remote applications.
Each terminal window provides the functions of a text-mode Linux console.
Twin runs on X11, libggi, itself, the Linux console,
and any termcap/ncurses-compatible tty.
It supports multiple simultaneous displays,
and can attach/detach each display on the fly.'

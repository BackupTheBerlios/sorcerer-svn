   version stable 2.24.0 2.14.3 2.14.2
   require Python ORBit2
  category development
 attribute x11 gnome
    source $GNOME_URL sources/pyorbit/${VERSION:0:4}/pyorbit-$VERSION.tar.bz2
  homepage http://freshmeat.net/projects/pyorbit
      desc 'ORBit2 bindings to Python
pyorbit is a Python language binding for the ORBit2 CORBA implementation.
It aims to take advantage of new features found in
ORBit2 to make language bindings more efficient.'

build(){
 if   [[ $HOSTTYPE == x86_64 ]] && ! [[ $CFLAGS =~ -m32 ]]
 then export LDFLAGS+=' -L/usr/lib64'
 fi
 default_build
}

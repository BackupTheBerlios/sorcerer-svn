   VERSION=( "0.17" "0.16.2" "0.16.1" "0.16" )
  CATEGORY="development"
 ATTRIBUTE="library"
    SOURCE="librep/librep-$VERSION"
  HOMEPAGE="http://librep.sourceforge.net"
       URL="$SOURCEFORGE_URL"
       REQ="gmp gdbm ncurses readline"
 FRESHMEAT="librep"
  GNOMEFTP="librep"
  ESTIMATE="366"
     SHORT="librep is a shared library implementing a Lisp dialect."
stream << EOF
librep is a shared library implementing 
a Lisp dialect that is lightweight, 
reasonably fast, and highly extensible.
It contains an interpreter, 
byte-code compiler and virtual machine.
EOF

# 0.91.1 failed to compile

    stable 0.91.0 0.90.6
  unstable 0.91.1
   require gmp gdbm libffi ncurses readline
  category development
 attribute library
#attribute makej multilib-concurrent
    source http://download.tuxfamily.org/librep/librep-$VERSION.tar.xz
#   source http://download.tuxfamily.org/sawfish/librep/librep-$VERSION.tar.xz
#   source $SOURCEFORGE_URL librep/librep/$VERSION/librep-$VERSION.tar.bz2
  homepage http://librep.sourceforge.net
  estimate 960
      desc 'shared library implementing a Lisp dialect

librep is a shared library implementing
a Lisp dialect that is lightweight,
reasonably fast, and highly extensible.
It contains an interpreter,
byte-code compiler and virtual machine.'

build(){
 export LDFLAGS+=" $CFLAGS"
 default_build
}

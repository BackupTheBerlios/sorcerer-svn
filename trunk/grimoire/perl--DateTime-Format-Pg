    stable 0.16004 0.16003
   require perl perl--DateTime perl--DateTime-Format-Builder
  category perl utility/time
 attribute perl-module
    source http://www.cpan.org/authors/id/D/DM/DMAKI/DateTime-Format-Pg-$VERSION.tar.gz
  homepage http://search.cpan.org/search?dist=DateTime-Format-Pg
  estimate 15
      desc 'perl--DateTime-Format-Pg understands formats used by PostgreSQL
for its DATE, TIME, and TIMESTAMP data types. It can be used to parse
these formats in order to create DateTime objects, and it can take a 
C<DateTime> object and produce a string representing it in the 
PostgreSQL format.'

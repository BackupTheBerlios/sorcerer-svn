   version   stable 1.2.1
   version unstable 1.7.0-beta
case $VERSION in
 1.2.1)
      opts --disable-image-loader-gif ;;
esac
   require eet fribidi giflib gnutls libgsf librsvg
  optional SDL --enable-gl-sdl
      opts --enable-gl-x11
      opts --disable-static
  category graphic
 attribute library makej multilib-concurrent x11
    source http://download.enlightenment.org/releases/evas-$VERSION.tar.bz2
      info good 20120716
      info last 20120609
      info home http://www.enlightenment.org/
      info docs 'http://www.enlightenment.org/p.php?p=docs&l=en'
      desc 'canvas library for e17
evas is a canvas library, designed to work with the X Window System.
It is designed to be able to take advantage of any graphics hardware
or CPU processing power you may have, or to adjust for the lack of it.
It has a simplistic API to access the features of a
canvas to aid in making development using evas easy.'

build(){
 link_fix(){
  if   [[ $HOSTTYPE == x86_64 ]] && ! [[ $CFLAGS =~ -m32 ]]
  then export LDFLAGS+=' -L/usr/lib64'
  fi
 }

 case $VERSION in
  1.2.1) link_fix ;;
  1.2.0) link_fix ;;
  1.1.0) link_fix ;;
 esac
 default_build
}

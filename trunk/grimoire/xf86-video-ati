# Version 6.14.6 is broke if using the unstable version of xorg-server

with version   stable 6.14.6 6.14.4
with version unstable 6.14.6
with base    xorg-server
with also    --disable-static
with role    xorg/driver
with trait   x11 X11R7 library makej multilib-concurrent optimize
#trait broke
with source    stable $XORG_URL driver/$SPELL-$VERSION.tar.bz2
with source  unstable $XORG_URL driver/$SPELL-$VERSION.tar.bz2
with info    good 20120713
with info    last 20120329
with info    home http://www.x.org

build(){
## fixing it is not that simple
# case $VERSION in 
#  6.14.6) grep -lr 'xaa\.h' *      | xargs -r sed -i '/xaa\.h/d'
#          grep -lr 'xaalocal\.h' * | xargs -r sed -i '/xaalocal\.h/d' ;;
# esac
 default_build
}

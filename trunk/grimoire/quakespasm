# Probably requires a spell for data files?

   version stable 0.85.4 0.85.3
   require SDL_net
  category game/3d
 attribute x11
    source $SOURCEFORGE_URL quakespasm/Source/quakespasm-$VERSION.tgz
      info last 20110405
  homepage http://quakespasm.sourceforge.net/
      desc 'engine for running Quake 1, first person shooter
This probably requires some data files in order to be amusing when run.'

build(){
cd Quake &&
make SDLNET=1 &&
mkdir -p $DESTDIR/usr/bin &&
install -m 0755 quakespasm $DESTDIR/usr/bin
}

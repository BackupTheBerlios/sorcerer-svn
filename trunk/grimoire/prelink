   VERSION=( "20060213" )
  CATEGORY="disk utility"
 ATTRIBUTE="console"
    SOURCE="prelink-$VERSION.tar.bz2"
       URL="http://people.redhat.com/jakub/prelink/"
  HOMEPAGE="http://people.redhat.com/jakub/prelink/"
       REQ="libelf"
   EXCLUDE="/etc/prelink.conf
            /etc/prelink.cache"
  ESTIMATE="640"
      DESC="prelink modifies ELFs to expedite runtime linking.

# augur prelink all
The above command prelinks all installed spell's
ELF libraries and ELF dynamic link executables."


build()  {
  default_build  &&
  touch  /etc/prelink.conf
}

   version stable 0.8.0 0.7.5 0.7.4 0.7.3 0.7.2
  optional qt-everywhere-opensource --enable-pinentry-qt4 --disable-pinentry-qt4 to build GUI using QT4
# optional libcap      ''                   --without-libcap      for POSIX capabilities
  category security/encryption
 attribute x11
    source ftp://ftp.gnupg.org/gcrypt/pinentry/pinentry-$VERSION.tar.gz
  homepage http://www.gnupg.org/
      opts --disable-pinentry-gtk --disable-pinentry-gtk2 --disable-pinentry-qt
      desc 'collection of simple PIN or passphrase entry dialogs
which utilize the Assuan protocol as described by the
aegypten project; see http://www.gnupg.org/aegypten/ for details.
There are programs for different toolkits available,
the default is to build a curses GUI dialog.'

build(){
 export PKG_CONFIG_PATH=/opt/kde4/lib/pkgconfig

 ./configure               \
  --sysconfdir=/etc        \
  --localstatedir=/var     \
  --prefix=${PREFIX:-/usr} \
 $OPTS &&
 make  &&
 make install DESTDIR=$DESTDIR
}

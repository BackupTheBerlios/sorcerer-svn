    stable 8.15.4 8.15.3 8.15.2 8.15.1 7.07.1 7.05.6
   require fontconfig ghostscript-fonts-other ghostscript-fonts-std glib-v1.2 jpeg libpng
  optional cups  --enable-cups --disable-cups for CUPS support
  optional libXt --with-x      --without-x for x11 support
  category printer
 attribute console x11
    source http://ftp.easysw.com/pub/ghostscript/$VERSION/espgs-$VERSION-source.tar.bz2
  homepage http://www.cups.org/espgs
      opts --with-omni --with-ijs
  eprovide ghostscript
  estimate 3400
      desc 'espgs is an enhanced version of GNU Ghostscript with better printer support.
espgs is ESP Ghostscript is a customized version of GNU
Ghostscript that includes an enhanced
autoconf-based configuration script, the CUPS
raster driver to support CUPS raster printer
drivers, and additional patches and drivers
from various Linux distributors.'

build(){
./configure          \
--build=$BUILD       \
--prefix=/usr        \
--sysconfdir=/etc    \
--localstatedir=/var \
$OPTS                &&
make                 &&
#make  so            &&
prepare_install      &&
make    install
#make  soinstall
}

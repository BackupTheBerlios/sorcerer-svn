   version   stable 1.32.1 1.30.1
#  version unstable 1.31.2
   require cairo glib gobject-introspection harfbuzz libXft xcb-util
  category graphic
 attribute gnome library lto makej multilib-concurrent optimize
    source   stable $GNOME_URL sources/pango/${VERSION:0:4}/pango-$VERSION.tar.xz
    source unstable $GNOME_URL sources/pango/${VERSION:0:4}/pango-$VERSION.tar.xz
      info last 20120928
      info home http://www.pango.org
      desc 'library for layout and rendering of text
pango has an emphasis on internationalization.'

build(){
 sed -i 's:hardcode_into_libs=yes:hardcode_into_libs=no:' configure
 default_build
}

post_install(){
 default_post_install
 echo pango-querymodules | pipe_queue $CAST_QUEUE
}

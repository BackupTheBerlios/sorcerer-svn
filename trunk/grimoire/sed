with version stable 4.2.1
with base    acl
with switch  -static '' to make static linked sed
with also    --bindir=/bin
with role    utility/editor
with trait   console lto optimize solo
with source  $GNU_URL sed/sed-$VERSION.tar.bz2
with info    good 20120704
with info    last 20090627
with info    home http://www.gnu.org/software/sed/sed.html
with protect /bin/sed
with info    cite 'stream-oriented non-interactive text editor'

build(){
 if   [[ $OPTS =~ -static ]]
 then LDFLAGS+="  -static";
      OPTS=${OPTS/-static}
 fi   
 default_build
}

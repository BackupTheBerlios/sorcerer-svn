if  !  grep  -q  "DRAC_BOOT="  $SPELL_CONFIG;  then

  if    query  "Invoke drac at boot?" n
  then  echo   "DRAC_BOOT=true"   >>  $SPELL_CONFIG
  else  echo   "DRAC_BOOT=false"  >>  $SPELL_CONFIG
  fi

fi

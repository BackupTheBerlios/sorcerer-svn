#!/bin/sh

   VERSION=( "1.6.0" )
  CATEGORY=""
 ATTRIBUTE="new"
    SOURCE=("mkvtoolnix-${VERSION}.tar.bz2")
       URL=("http://www.bunkus.org/videotools/mkvtoolnix/sources")
			 SVN=("command to execute before anything else is done like login"
			      "co https://svn.bunkus.org/mosu/trunk/prog/video/mkvtoolnix"
						"up https://svn.bunkus.org/mosu/trunk/prog/video/mkvtoolnix")
			 CVS=("command to execute before anything else is done like login"
			      "command to execute to leech the sources"
						"command to execute to update the sources")
 FRESHMEAT=""
  HOMEPAGE="http://www.bunkus.org/videotools/mkvtoolnix/index.html"
#this is an unofficial variable, put here only what is required to buil
#not what is required to run"
 BUILD_REQ=""
       REQ=("$BUILD_REQ libmatroska expat libogg libvorbis zlib"
            "wxGTK:::if you want to use mmg (the mkvmerge GUI) or mkvinfo's GUI."
						"flac:::flac support"
						"lzo2:::for matroska lzo compressed files"
						"bzip2:::fo matroska bzip2 compressed files"
           )
  ESTIMATE=""
      DESC="MKVToolnix is a set of tools to create, alter and inspect Matroska files under Linux"

#
			
#configure()  {  :;  }


#pre_build()  {
#  default_pre_build;
#}


#build()  {
#  default_pm_build
#  default_build
#  prepare_install
#}

#post_build() { 
#  default_post_build; 
#}

#post_install()  { 
#  default_post_install;
#}


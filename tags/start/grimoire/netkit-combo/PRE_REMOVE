RUNLEVEL=$( runlevel | cut -d ' ' -f2 )

if    [  -x  /etc/rc$RUNLEVEL.d/K60inetd  ]
then         /etc/rc$RUNLEVEL.d/K60inetd  stop
fi

if    [  -x  /etc/rc$RUNLEVEL.d/K60rpc.bootparamd  ]
then         /etc/rc$RUNLEVEL.d/K60rpc.bootparamd  stop
fi
